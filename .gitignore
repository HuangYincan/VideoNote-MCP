# 运行时数据（根锚定，避免误伤 app/db/models 等源码目录）
/data/
/models/
/note_results/
/config/

# 数据库
*.db
*.sqlite3

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/
.ruff_cache/

# uv
.uv/

# 编辑器 / 系统
.DS_Store
.idea/
.vscode/
*.log

# Claude Code 机器本地配置（插件/marketplace 安装状态，不入库）
.claude/settings.json
.claude/settings.local.json
# 项目 CLAUDE.md（本机开发上下文，不提交）
/CLAUDE.md
# 项目开发专用 skills（只在本机开发用，不随插件分发/入库）
.claude/skills/

# 本地安全扫描脚本（codex-security-local.sh 从 auth.json 读 key 并经命令行
# 传 experimental_bearer_token，进程列表可见——只在本机用，不入库）
/scripts/

# node_modules（本地工具/前端依赖，不入库，误提交风险）
node_modules/
/package-lock.json

# LaTeX 编译产物（模板预览 main.pdf 除外，需入库）
*.aux
*.fdb_latexmk
*.fls
*.out
*.synctex.gz
