# 05 — 优化清单（2026-08-17 审计）

> ⚠️ **历史档案（141KB，只追加）** —— 这是 16 轮全库审计的累计清单，✅=已落地，`#N`=条目号。
> 新 Agent **不要通读**。当前状态 / 开放候选见 [00-新手上路.md](00-新手上路.md)「当前状态」节；
> 查具体条目用 `grep -n "132" docs/05-优化清单.md`。
> 登记新候选 / 新轮次时在文末追加（编号递增，如 `#133`），并同步 `06-执行进度.md` 与 `CHANGELOG.md`。

全栈对照 `videonote_mcp/`、`app/`、Skill/插件、测试与文档后的优化点。**按严重度排序，不一次全改。** 落地建议见文末三波。

代码里实际有 **32** 个 `@mcp.tool()`（含 `inspect_video`）。**Wave A 已于 2026-08-17 落地**（见文末勾选）。P0 条目保留原文作审计记录，实现以代码为准。

---

## P0 — 正确性 / 安全（先修）

### ✅ 1. 步骤任务永不 SUCCESS（三个工具实质不可用）



`_run_step_task`（`videonote_mcp/server.py:256-269`）写完 `result.json` 后**不** `_write_status(SUCCESS)`。`get_task_status` 只在 `status == "SUCCESS"` 时读结果（`:628`）。

因此 `transcribe_media` / `extract_frames` / `summarize_note` 会一直停在 `INITIALIZING`/`PENDING`，Agent 按文档轮询永远等不到。完整 `generate_note` 没事，因为 `NoteGenerator` 自己写 SUCCESS。

### ✅ 2. 截图目录读错环境变量



`app/services/note.py:57`：

```python
IMAGE_OUTPUT_DIR = os.getenv("OUT_DIR", "./static/screenshots")
```

`config.py` 设的是 `IMAGE_OUTPUT_DIR`。非便携截图落到 CWD，`_absolutize_images` / cleanup 按数据目录找 —— 图丢、目录泄漏。

### ✅ 3. `generate_note` / `prepare_note_material` 不认 `file://`



`_coerce_local_path`（含 `unquote`）只用于 `transcribe_media` / `extract_frames`。主入口用 `Path(video_url).expanduser().exists()`（`server.py:445, 555`）。`file:///Users/.../foo%20bar.mp4` 会误报「本地文件不存在」。Windows `file:///C:/x` 解析出 `/C:/x` 也会假阴性。

### ✅ 4. 未知 `task_id` 被报成 PENDING



`get_task_status` 在 `status.json` 不存在时返回 `{status: PENDING}`（`server.py:615-618`）。拼错 / 已 cleanup 的 id 会被 SKILL 一直轮询。应返回 `NOT_FOUND`。

### ✅ 5. 凭证仍可经 MCP 进对话



安全红线写「key 只走 `! videonote providers set`」，但：

- `add_provider(api_key=...)` **必填** key
- `update_provider` 仍收 `api_key`
- `set_downloader_cookie` 收完整 cookie
- `diarize_media(hf_token=...)` 收 HF token

Cookie / SESSDATA / HF token 进 tool call = 进对话上游。应收紧：MCP 拒绝敏感字段，或明确返回「请用 CLI」；`add_provider` 允许先建空 key。

### ✅ 6. 已保存的 `hf_token` 不被 `diarize_media` 读取



setup 把 token 写进 `app_config.json`（`cli.py:464`），`diarize_media` 只看参数和 `HUGGINGFACE_HUB_TOKEN`（`diarization.py:52`）。用户按向导配完，工具仍要再要一次 token。

### ✅ 7. `wait_for_note` 阻塞 stdio 事件循环



工具体内 `time.sleep` 轮询（默认 120s）。SKILL 已禁止，工具还在。Agent 一旦调用会卡住同进程其它工具。删掉、立刻返回、或真正异步 + MCP progress。

### ✅ 8. 独立步骤任务不进 `video_tasks`



`transcribe_media` / `extract_frames` / `summarize_note` 只写 `status.json`，不 `insert_video_task`。`_write_status` 的 `update_task_status` 对不存在行直接 skip。`list_tasks` 看不到这些任务。

### ✅ 9. 并发门禁 TOCTOU



检查 `_task_futures` 再 `submit` 不是原子的（`server.py:485-515` vs `_guard_concurrency`）。两个并行 `generate_note` 都能过门禁，超过 `VIDEONOTE_MAX_WORKERS`。`generate_note` / `prepare_note_material` 还复制了一份门禁，没走 `_guard_concurrency()`。

### ✅ 10. Cookie 临时文件泄漏



Bilibili / generic 把 Netscape cookie 写到 `/tmp`（`NamedTemporaryFile(delete=False)` / `mkstemp`），默认 umask、从不 unlink。抖音 `__init__` `print(self.headers_config)` 含 Cookie，会进 `mcp_stderr.log`。secret 文件（`downloader.json`、`app_config.json`、SQLite）无 `chmod 0600`。

### ✅ 11. `xiaoyuzhoufm_download.py` 导入即 HTTP



硬编码 Next.js `_next/data` URL，模块 import 就 GET + `print`。不在 `SUPPORT_PLATFORM_MAP` 里，但任何误 import 都会打网。应删或惰性化。

**✅ 已落地（2026-08-17，小项批）**：整个 stub + 测试已删除 —— 从未接入，yt-dlp generic 提取器已覆盖小宇宙（`_match_platform_host` 兜底 `generic`）。VENDOR.md 已记录。

### ✅ 12. 预处理清理文件名算错

**✅ 已落地**：cleanup_preprocess_files 重写——正确识别 `_16k` / `_part_` / `_denoised` 命名（含误传源路径时只清旁边临时文件，绝不碰源文件）。


`cleanup_preprocess_files(wav)` 用 `foo_16k.wav` 的 stem 再拼 `_16k`，找不到真正的 `foo_16k.wav` / `foo_part_*.wav`（`audio_preprocess.py:136-152`）。本地路径还会把中间 wav 写到用户源文件旁边。

### ✅ 13. CI 不跑测试



`.github/workflows/ci.yml` 只 `import` + `tools/list` + `providers list`。`pyproject.toml` 无 pytest/ruff extra。回归全靠人手。应 `uv run pytest`，并加 ruff。

### ✅ 14. 文档代差（会误导后续改动）



`docs/02-架构设计.md` 仍写：14 个工具、扁平 `note_results/{task_id}.json`、无 pipeline/generic/FunASR。`docs/00` 写「11 个工具」。`VENDOR.md` 缺 `generic_downloader` / `funasr` / `pipeline` / `merge` / `diarization` / `export`，章节编号重复「5.」。Skill `tools.md:16`「一次一个任务」vs 代码 `MAX_WORKERS=3`。README / docs/04 同样有串行残留。

### ✅ 15. `__version__` 漂移



`videonote_mcp/__init__.py` = `0.1.0`，`pyproject.toml` = `0.1.4`。08-16 的插件 userConfig / setup 已上 `main`，版本未 bump。

---

## P1 — 工具面 / Agent DX

### ✅ 16. 37 个工具已过载，返回值全是 JSON 字符串

**✅ 已落地（2026-08-17，6f）**：transcript 拆为 MCP Resource（`videonote://task/{id}/transcript`，时间轴纯文本）+ `_load_task_transcript` 公共函数；provider 省略 / 参数默认化 / wait_for_note 废弃此前已落地。**未做**：返回 dict 化、`mode=note|material` 变体（工具数 37→38）。


FastMCP 本可返回 dict/Pydantic。现在 `json.dumps` 再被协议包一层，Agent 要 `loads` 两次。重叠路径：

| 用户意图 | 可走路径 |
|---|---|
| 一条视频出笔记 | `generate_note` **或** `prepare_note_material` + Agent **或** 四步拆开 |
| 查进度 | `get_task_status` / `wait_for_note` / `list_tasks` |
| 转写 | 任务结果 / `get_task_transcript` / `export_transcript` |

建议：**保留端到端 + 素材包 + 状态/清理**；步骤工具降为高级/内部，或拆 MCP Resource（`videonote://task/{id}/transcript`）。`generate_note` 16 个参数应变体（`mode=note|material`）+ 配置默认。

### ✅ 17. `generate_note` 强制 `provider_id`



即使 setup 已有默认供应商/模型，不传就抛（`server.py:438`）。docstring 却写「省略时取默认」。应取唯一已填 key 的供应商 + `default_model:{id}`。

### ✅ 18. `agent_direct` 写了没人读

**✅ 已落地（2026-08-17，Wave B 9 决策）**：server 保持不读 `agent_direct`，SKILL 只当编排选择，docs/04 已澄清。


setup 持久化 `app_config.agent_direct`（`cli.py:680-685`）。没有任何 MCP 工具读它。Skill/docs/04 当成 `generate_note` 的默认。要么 server 读，要么删掉这个键，Skill 只当编排选择。

### ✅ 19. `list_models` 返回形状不一致



实时 API → `{source, models: [str, ...]}`；DB 回退 → `{source, models: [{model_name, ...}, ...]}`。统一为 `{id, name}[]`。

### ✅ 20. 文档里的 `fetch_comments_danmaku` 不是工具



`pipeline.fetch_comments_danmaku` 存在；`summarize_note` docstring（`server.py:1021`）和 Skill `tools.md:76` 当 MCP 工具写。Agent 会调一个不存在的名字。暴露它，或改文档为 `fetch_comments` + `fetch_danmaku`。

### ✅ 21. 无 MCP progress



后台任务只写 `status.json`。Agent 只能盲轮询。应在 PARSING→DOWNLOADING→TRANSCRIBING→SUMMARIZING 用 `ctx.report_progress`。

**⏸ 已判定不可行 + 替代已交付（2026-08-17，小项批）**：FastMCP 3.4.5 stdio 下 session 只存在于单次 `run()` 作用域内，后台线程（ThreadPoolExecutor）没有可用的 session 级 API —— 推式 progress/logging notification 架构上做不到（`Context.report_progress` 绑定在请求内；`Server.run()` 内部创建的 ServerSession 无全局注册表，stdio 流非线程安全）。已交付的替代：`get_task_status` 的 `stage` / `elapsed_secs`（Wave B），Skill 轮询汇报「转写中，已 3 分钟」。若未来改 multi-transport（sse/streamable-http）或换自管 session，可再启用推式进度。

### ✅ 22. 配置工具不对称



DAO 已有 `delete_provider` / `delete_model`，MCP 未暴露。没有 `get_app_config`。没有 `test_provider`（CLI 有 `providers test`）。`set_transcriber` / `plugin.json` userConfig 漏 `funasr`，也漏 `video_interval` / `comments_limit` / `default_export_formats` / preprocess。插件 MCP 启动命令没 `--with mlx-whisper`。

**已落地（2026-08-17，Phase 2d）**：MCP 新增 `delete_provider` / `delete_model(provider_id, model_name)` / `test_provider(provider_id)`（用已存 key 探测，不接受 key 参数）/ `read_app_config()`（返回 setup 默认值，过滤 hf_token 等敏感项）。Wave B 已把 `set_transcriber` docstring 与 tools.md 对齐 funasr。

### ✅ 23. SKILL 摩擦过大 + handoff 过时



强制先问全自动/手动，再列 6 项，再强制后续优化。和「给个链接出笔记」冲突。规则 6：未知 URL 现在是 `generic`，只有显式 `platform=unsupported` 或 generic **下载失败** 才该 handoff；server 失败时也常不返回 `handoff:true`。

### ✅ 24. 插件更新路径脆弱



MCP（uvx）跟 git HEAD；Skill 必须 disable+install。`plugin.json` 故意无 version。用户经常「MCP 新、Skill 旧」。`health_check` 应返回 `server_version` + 刷新命令。考虑 pin `@vX.Y.Z`。

### ✅ 25. `export_transcript` 丢错误、默认不一致



exporter 跳过失败格式，工具仍返回 `errors: {}`（`server.py:1405-1408` vs `exporter.py:69-83`）。手动导出未配格式时发明 `["srt","vtt","json"]`；自动导出空配置 = 跳过。`out_dir` / `merge_audio` 可写数据目录外（merge 默认 `cwd`）。

### ✅ 26. `get_task_transcript` 默认灌全文



空 `segment_range` = 全量。长视频一次撑爆 context。默认应截前 N 段，显式 `"all"` 才全文。`get_task_status` 先整份读 `result.json` 再 pop transcript，同样浪费。

### ✅ 27. 错误形状不统一



有的 `raise ValueError`，有的 `{ok:false}`，`fetch_subtitles` 成功甚至没有 `ok`。Agent 难写统一处理。

**✅ 已落地（2026-08-17，小项批）**：`fetch_subtitles` 成功改为 `{ok: true, language, full_text, segments}`，成功/失败都有 `ok`。其余工具的成功形状（json.dumps 字符串）统一仍属 #16。

---

## P2 — 流水线可靠性 / 性能 / 笔记质量

### ✅ 28. 跨任务零缓存



每次 `generate_note` 新 `uuid4`。`gen/transcript.json` 只对**同一 task_id 重跑**有效。同一 BV 再生成会重下+重转写。应按 `platform+video_id`（或文件 hash）内容寻址。

**已落地（2026-08-17）**：`app/services/note_cache.py` —— 身份 `platform:video_id`（B 站 BV+p / YouTube v= / 抖音 / TikTok / 本地 sha256；b23 短链解析、快手、generic 解析不出则不命中），转写按来源分键（`subtitle` / `transcriber_type[:model_size]`，切换引擎/尺寸不误用）。命中把缓存 transcript 拷进 `gen/transcript.json`，下游 `has_transcript → skip_download` 即跳过下载与转写。`cleanup_all` 连 `note_cache/` 一起清。无 LRU；kuaishou/generic 的 URL→id 映射（别名表）仍未做。

### ✅ 29. `quality` 是死参数

**✅ 已落地（2026-08-17，小项批）**：quality 已接上（仅 B 站转码场景）+ yt-dlp 统一 retry。


`QUALITY_MAP`（32/64/128）存在，**没有下载器读 `quality`**。B 站写死 `preferredquality: '64'`。base.py `:19` 还 `#TODO 需要修改为可配置`。要么接上 yt-dlp format，要么从 API 拿掉。

**✅ 已落地（2026-08-17，小项批）**：base.py 写死的 `self.quality` 与 TODO 已删；bilibili postprocessor 现在按 `quality` 映射码率（fast=32 / medium=64 / slow=128，未知值回退 64）。**取舍**：youtube/generic 保持 `bestaudio` 不转换 —— 转码会降质（音频本就是原始流），`quality` 只作用于 B 站这种需要转码的场景。

### ✅ 30. 播放列表 / 分 P / 合集

**✅ 已落地（2026-08-17，6e）**：`inspect_video` 拆分 P / 播放列表为可提交 url；新增 `batch_generate_notes`（服务端展开 + 逐个排队，max_entries 截断，单集退化为单个任务）。


所有下载器 `noplaylist: True`。字幕层已能解析 `p=`，MCP 没有「列出分 P / 批量提交」。应：`inspect_video` 返回分 P/时长/是否有字幕；`generate_note` 接受 `p` 或 `pages=[]`。

### ✅ 31. 说话人分离没接入主路径

**✅ 已落地（2026-08-17，6d）**：`pipeline.apply_diarization` 转写后自动打 speaker（generate_note 全自动生效，配置开关）；2+ 说话人时 prompt 渲染 `[SPEAKER_00]` 前缀。


`diarize_media` 是独立工具；`generate()` 即使配置了 `diarization` 也不会给 segment 打 speaker。会议纪要风格空转。

### ✅ 32. LLM 按 45MB JSON 切块，不是 token

**✅ 已落地（2026-08-17，6c）**：RequestChunker 双约束（字节 + token 估算，汉字≈1 token），默认 12k token/chunk，合并分组同步；`fix_markdown` 确认无引用。


`universal_gpt.py` 默认 `OPENAI_MAX_REQUEST_BYTES=45MB`。2 小时转写往往一整块。中文 3 字节/字，结构容易碎。应按窗口（8–16k token）+ overlap，合并时带章节大纲。`fix_markdown` 已 import 从未调用。

### ✅ 33. 视频理解成本失控

**✅ 已落地（2026-08-17，6b）**：帧组封顶 ≤24（`effective_frame_interval` 自适应拉大间隔）；视频已下载时从本地 ffmpeg 提取音频，免第二次网络下载。


`VideoReader` 每 6s 一帧、最多 1000、3×3 网格，**整组 data URI 进第一次 LLM**。1 小时 ≈ 67 张网格图。应封顶、落盘、不要 base64 全集。截图模式会 **下载两次**（视频 + 音频）。

### ✅ 34. Whisper 默认 `tiny` + HF 超时 10s

**✅ 已落地（2026-08-17，6a）**：whisper 默认 small、HF 超时 60s、health_check 模型行区分 downloaded/downloading/failed（含 error）、`engine_advice` 建议。


中文质量差。`HF_HUB_DOWNLOAD_TIMEOUT=10` 防卡住，但首次下大模型会误杀。`health_check` 应区分「未下载」vs「下载超时」；默认至少 `base`/`small`。`get_transcriber(..., device="cuda")` 在 Mac 上先探测再回退，应 Darwin 直接 `cpu`/`mps`。

### ✅ 35. `load_dotenv()` 在 vendored 模块里乱跑



`note.py` / `engine.py` / 抖音 / 快手 / groq / `video_helper` 各自加载。CWD `.env` 会在 import 后覆盖 `setup_environment()` 的 setdefault。

**✅ 已落地（2026-08-17，小项批）**：6 处 `load_dotenv()` 全部加守卫 `if not os.environ.get("VIDEONOTE_DATA_DIR"): load_dotenv()` —— `setup_environment()` 先于 `app.*` import 运行，一旦设了数据目录 env，vendored 模块不再加载 ambient `.env`，不会被 CWD 的 .env 覆盖。

### ✅ 36. 下载器脆弱点



- YouTube **不读** CookieConfigManager（年龄墙/机器人检测过不去）；音频常是 webm/m4a，路径按 `info['ext']` 猜
- Generic cookie Netscape host 写成 `example.com`，yt-dlp 不会带到真域名
- 无 yt-dlp `retries` / `fragment_retries`
- Douyin 硬编码 `strData` + ABogus，API 一漂就死
- Kuaishou 出错仍 `return video_details['data']` → TypeError；永远先下整段 mp4 再转码
- Bcut 轮询 500×1s 无退避
- `audio.json` 不检查文件是否还在
- `_get_transcript` 会把 `generate()` 已拉过的字幕再拉一次
- `insert_video_task` 吞掉 DB 异常

**✅ 已落地（2026-08-17，小项批，重试部分 + E3 后 YouTube cookie）**：新增 `app/downloaders/common.py` 的 `ytdlp_retry` —— 只重试网络类错误（timed out / urlerror / connection / eof / HTTP 5xx / 429 / network / socket / reset by peer / remote end / 502-504），指数退避 1.5s×2^i，3 次；业务错误（404 / 登录失败）立即抛。6 处 `extract_info` 全部接入。**YouTube cookie（E3 后补）**：youtube_downloader 读 setup ③ 填的 youtube cookie → Netscape 临时文件（0600、`__del__` 清理）→ ydl_opts `cookiefile`，匿名时照常降级。**下载器健壮性（commit f5bdc8b，见 #90）**：kuaishou TypeError、Bcut 退避、generic cookie 域名、audio.json 悬空实体。仅剩 abogus（Douyin 签名硬编码，依赖上游 TikTokDownloader，改不动）。

### ✅ 37. 提交前无预检



没有时长、磁盘、ffmpeg、模型就绪、供应商 key。长视频跑到 TRANSCRIBING 才因模型未下载 / 磁盘满失败。

**已落地（2026-08-17，Phase 2）**：新增 `preflight(url?, platform?, provider_id?)` 工具 —— ffmpeg / 磁盘剩余（≥1GB）/ 转写器就绪 / 供应商 key+模型（与 generate_note 解析逻辑一致）/ 队列；url 非空时预解析时长（失败不拦截）。`{ok, checks:[{name, ok, detail}], duration_secs}`。

### ✅ 38. `cleanup_all` 不动 DB + 无确认



磁盘空了、`list_tasks` 全是幽灵。`include_config=True` 清 keys/cookies，无确认 token。活着的 `mcp_stderr.log` 会被抽走 fd。

**✅ 已落地（2026-08-17）**：`task_manifest` 的 `cleanup_all_files` 已同步清 `video_tasks` 索引（含 docstring + 测试），`list_tasks` 不再有幽灵任务。设计红线：**卸载/升级 MCP 一律不动配置**，只有显式 `cleanup_all` 才清 —— 该行为保持。确认 token 仍待做。

### ✅ 39. 笔记 prompt 质量

**✅ 已落地（2026-08-17，6a/6d + Wave E2）**：语言自动选引擎评估后**不做自动切换**（需先推理才知语言、funasr 非默认安装），改 health_check `engine_advice` 显式建议；默认喂 speaker 由 6d 落地；分块间共享大纲由 Wave E2 落地（后续 chunk 注入已生成章节标题，`extract_outline` 提取/去重/截断，MERGE_PROMPT 同义章节合并，commit af4a198）。**未做**：风格 prompt 打磨。


风格如小红书往「专业笔记」里灌 emoji/标题党。分块之间不共享大纲，标题漂移。默认不喂 speaker。`tiny` 中文讲座质量差，有 FunASR 也不按语言自动选。

---

## P3 — 工程卫生 / 分发 / 测试

### ✅ 40. 测试债

**✅ 已落地（2026-08-17，Wave A / 批 5 + #92）**：server 契约测试补齐（file:// / 未知 task / 省略 provider / 状态机）、conftest 按 pid 隔离、ruff F-class 入门禁、mock 下载器集成测试（test_pipeline_steps）、**真实下载器类集成测试（test_downloader_integration.py 15 项，见 #92）**。**未做**：whisper 真实推理集成测试（需下载模型，成本高；云端引擎无 key 不可测）——标记为已知限制。


- unittest + 可直接 `python tests/foo.py`，docstring 留着旧 worktree 路径
- `conftest.py` 用全局 `/tmp/videonote_pytest`（并行/多 checkout 会撞）
- **没有** `NoteGenerator.generate` 编排测试，**几乎没有** `server.py` 契约测试（`file://`、未知 task、默认 provider、SUCCESS 步骤任务）
- 无下载器 / UniversalGPT chunk+retry / cookie 权限测试
- 无 ruff/mypy；`enmus` 是上游拼写错误

### ✅ 41. 死代码 / FastAPI 残渣



可删或隔离：`app/gpt/openai_gpt.py`、`deepseek_gpt.py`、`qwen_gpt.py`、`tools.py`（空）、`db/sqlite_client.py`（CWD 相对库）、`validators/video_url_validator.py`、`services/task_serial_executor.py`（MCP 自有线程池）、local `/uploads`。blinker `on_finish` 在各转写器里被注释掉，`events.register_handler()` 空转。

**✅ 已清理（2026-08-17，小项批）**：`note.py` 的 `API_BASE_URL` / `BACKEND_PORT` / `BACKEND_BASE_URL` 已删（config.py / video_helper.py 同步）；`save_cover_to_static` 不再伪造后端 URL，返回 `file://` 绝对路径；顶层 `events/` 已迁入 `app/events/`（VENDOR.md 已更新）；根目录泄漏的 `video_note.db` 已删；`xiaoyuzhoufm_download.py` 已删（见 #11）；`downloaders/common.py` 从本清单划掉 —— 现在是 `ytdlp_retry` 的宿主（见 #36）。其余（openai/deepseek/qwen_gpt、sqlite_client 等）仍待做。

### ✅ 42. Vendor 同步不可执行

**✅ 已落地（2026-08-17，6h）**：docs/02 增 vendor 同步纪律 + 冻结分叉清单 + 惰性 import 约定；VENDOR.md 同步（含 abogus GPL-3.0 许可标注）。


本仓库已在 `app/` 上长出 pipeline / FunASR / 数据层。再按 VENDOR「diff -r + 重打 4 个补丁」必冲突。应列冻结分叉清单，或把分叉抽回 `videonote_mcp/`。

### ✅ 43. 安装 / 数据目录故事不统一



- checkout → `<repo>/data/` + `<repo>/models/`
- 安装包 → `~/.local/share/videonote-mcp`
- `docs/04:35` 说「所有安装方式共用 ~/.local/…」——对 clone/`uv run` **假**
- 用户级 `claude mcp add` 空 env 会遮蔽插件 userConfig（install.sh 已修，旧用户未清）
- Skill/插件不随 uvx 自动更新
- hatch wheel **不含** skills/templates（pip 装的人没有模板）
- `requires-python <3.14`，README badge 写 `3.11+`

### ✅ 44. 可观测性

**✅ 已落地（2026-08-17，6a/6i + Wave E1）**：health_check 含 server/plugin 版本、队列长度、whisper 模型状态行、engine_advice；Wave E1 补 stderr 日志轮转（>50MB → .log.1）、dup2 失败可见、退出摘要（atexit 记录进行中任务数，commit 1ced97a）。**未做**：结构化日志、task_id 贯穿字段、graceful shutdown（已知限制：daemon 线程 + SIGKILL 边界）。


- `dup2` 失败 `except: pass`（`server.py:50`），stderr 死锁会回来
- `health_check` 不含：包版本、队列长度、是否有已填 key 的供应商、mlx/funasr 是否已装、模型是否正在下
- `ThreadPoolExecutor` 无 shutdown hook，客户端杀进程时 ffmpeg/whisper 可能残留
- 进程级 `builtins.print` 劫持，依赖真实 stdout 的库会坏

### ✅ 45. 安全边角

**✅ 已落地（2026-08-17，6g）**：Fernet 机器级加密（crypto.py，`enc:` 前缀，明文兼容迁移，key 丢失回退 None）；api_key/hf_token 落盘加密；out_dir/notes_dir 写数据目录外 warning；CLI `providers add --api-key` 改 getpass 交互输入。


- `notes_dir` / `export out_dir` / `merge_audio` 未走 cleanup 那套 `_safe_resolve`
- `abogus.py` 来自 TikTokDownloader，GPL-3 和本仓库 MIT 混用
- builtin Claude `base_url: "https://"` 是坑
- CLI `providers add --api-key` 出现在进程列表 / shell history（向导 `getpass` 更安全）

---

## 第二轮全库扫描（2026-08-17 · 4 个并行审计代理，关键项已代码级核实）

小项批之后的整仓扫描（工具层 / vendored 流水线 / 下载器 / 分发·文档·测试·CI）。**无新增 P0**。以下为清单 #1-#45 之外的新发现。

**状态（2026-08-17 收尾）：本批 #46-#76 共 52 项已全部落地** —— Wave D 批 1-5（commits c5c65d2 / 6c3ffa1 / 5a9755b / 9ed1b47 / aaee3a8）与大型改动 6a-6i（commits 8b78f65 / 3147ed3 / 5e620d7 / 5ade3ed / 35f900e / fa2c89c / 3dca7b1 / ce51514 / 864fe9c / 5745a84），各批详情与 commit 对照见 docs/06-执行进度.md。

### P1 — 正确性 / 安全

### ✅ 46. 凭证经 print 泄漏进 mcp_stderr.log（#10 同类未除尽）



- `douyin_downloader.py:186` `print("@kwargs:", kwargs)` 含 douyin Cookie；`kuaishou_helper/kuaishou.py:57` `print(is_exist)` 打印 Cookie —— Wave A 只删了 `__init__` 一处
- `bcut.py:131` / `kuaishou.py:43` print 完整 API 响应体（含 download_url 与整段转写）；`note.py:532` 每次状态写都 print
- douyin/kuaishou/bilibili/youtube 残留整包响应体、完整 URL（含 a_bogus）的调试 print，日志体积爆炸

建议：删 Cookie 处 print；全层统一 `logger.debug` 摘要级。

### ✅ 47. tiktok 映射到 DouyinDownloader



`constant.py:11` `'tiktok': DouyinDownloader()` —— tiktok.com 链接走抖音 API（aweme/detail）必然查不到 id → RuntimeError。平台声明支持 tiktok 实际必失败；GenericDownloader（yt-dlp TiktokIE）本可处理。建议改映射或删声明与 note_cache 的 tiktok 分支。

### ✅ 48. requests 调用普遍无 timeout



douyin（:134/202/299）、kuaishou（helper:48/60/70、downloader:72）、`url_parser.py:46` b23 短链 `requests.head` —— 网络黑洞时任务永不返回，长占线程池 worker 槽位。建议统一 `timeout=(5,10)`~30s + `raise_for_status`。

### ✅ 49. 下载器模块级实例化 → `__del__` 永不触发，#10 修复实际未生效



`constant.py` 模块导入即实例化全部下载器，`bilibili_downloader.py:45` `__init__` 写 /tmp Netscape cookie 文件、`:55` `__del__` 负责 unlink —— **Python 解释器退出不为模块级对象调 `__del__`**，单例驻留进程全程，kill -9 / 正常退出都在 /tmp 留明文 SESSDATA。建议惰性实例化（get_downloader 时创建），或 cookie 文件走数据目录 + atexit 清理。

### ✅ 50. Dockerfile 在 events 迁移后不可构建



`Dockerfile:19` `COPY events/ events/` —— 08-17 已迁入 `app/events/`，目标目录不存在，`docker build` 必失败；`pyproject.toml:64` wheel include 的 `events/**` 同步失效（app/** 已覆盖）。Glama 部署路径整体不可用。

### ✅ 51. release.yml 无测试门禁



tag 发布不跑任何测试（ci.yml 仅 main/dev/PR 触发），且只核对 pyproject version、不核对 `videonote_mcp/__init__.py` 的 `__version__` —— 坏 commit 打 tag 即发版。建议 release job 依赖测试 job，并校验 tag == pyproject == `__init__.__version__`。

### ✅ 52. `_resolve_default_provider_id` 的 default_model 分支不查 key



`server.py:406-407`：配置了 `default_model:{pid}` 就直接返回该供应商，**不检查 api_key 是否非空**（docstring 写「唯一一个已填 key 的供应商」自相矛盾）。key 被清空/过期后 generate_note 省略 provider_id 仍选中它，整轮下载+转写后才在 SUMMARIZING 报 401。建议该分支先校验 key 非空且不含 `*`，否则落到 keyed 列表逻辑。

### P2 — 可靠性 / 一致性

### ✅ 53. cancel_note 竞态覆盖终态



`server.py:898-901`：`future.done()` 检查之后仍无条件 `event.set()` + `_write_status(CANCELLED)`。任务恰好完成（SUCCESS 已写、registry 未弹）的窗口内取消会把 SUCCESS 改写为 CANCELLED；get_task_status 因 status!=SUCCESS 永不读 result.json —— 已成功的任务被报成「已取消且无结果」。建议 `future.done()` 时只弹 registry 不写状态，或写前重读 status.json 跳过终态。

### ✅ 54. result.json 非原子写 + 与 SUCCESS 的写序窗口



`server.py:179-182`：NoteGenerator 先写 SUCCESS 状态、`_run_note_task` 之后才 `write_text` result.json（非原子）。轮询在 SUCCESS 后、写盘完成前读到半截 JSON → json.loads 异常 → 返回 `SUCCESS + result:null`，Agent 误判结果丢失（慢盘/网络盘放大）。`_run_step_task`（:261-271）顺序正确但同样非原子。建议统一为 tmp+replace 原子写，且 result.json 落盘完成后再置 SUCCESS。

### ✅ 55. `_parse_segment_range` 非法 spec 静默回退全文



`server.py:789-791`：`segment_range="abc"` / `"50--60"` 拼写错误直接回退 (0, total) 返回全量转写，绕过「默认前 50 段」的防 context 撑爆设计。建议非法 spec 抛 ValueError（工具层转 `{ok:false}`）。

### ✅ 56. set_app_config 非原子读-改-写



`config.py:162-195`：无锁、无 tmp+replace。MCP 多线程（delete_provider 与 generate_note 默认读取并发）或 CLI 与 server 双进程同时写 app_config.json → 丢更新；write_text 中断损坏 JSON → get_app_config 静默回退 {}，全部默认失效。建议进程内锁 + 原子写。

### ✅ 57. preflight 的 queue 检查恒 ok=True



`server.py:1643`：`"ok": True` 写死。队列满（3/3）时预检仍放行，Agent 提交后立刻被并发门禁拒绝，预检误导。建议 `ok = queue_len < _MAX_WORKERS`，满时 detail 给等待/cancel 提示。

### ✅ 58. ffmpeg 调用无 timeout + 失败静默返回不存在路径



`video_helper.py:30` generate_screenshot、`groq.py:19` compress_audio、kuaishou/local 的 `subprocess.run` 全无 timeout —— 损坏/卡死媒体让 ffmpeg 永久挂起，worker 线程占死（merge/audio_preprocess 特意加了 1800s 超时，唯独漏这几处）。截图失败只 print 仍返回不存在的路径 → 笔记嵌死图链接、视频理解静默缺帧。建议 `timeout=120`（转码 600s）+ 失败时删半成品并抛/返回 None。

### ✅ 59. note_cache 媒体无大小上限



`note_cache.py:207-235` promote_media 把「音频」整份拷进 `<ident>/media/`，无上限无 LRU —— 抖音/快手/generic 的「音频」实际是完整 mp4（数百 MB），每部新视频永久复制一份，仅 cleanup_all 能清，磁盘无界增长。建议超阈值（如 100MB）不缓存媒体只缓存转写。

### ✅ 60. note_cache 身份/分键含冒号，Windows 不可用



`note_cache.py:62` `f"{transcriber_type}:{size}"`、ident 为 `platform:video_id`（目录名）—— 冒号是 Windows 非法文件名字符，mkdir/写入抛 OSError 被吞 → 缓存静默永不命中（macOS/Linux 正常）。建议做文件系统安全编码（冒号→`-` 或 URL 编码）。

### ✅ 61. 时间戳正则只支持 2 位分钟



`screenshot_marker.py:6`、`note_helper.py:39` 均为 `(\d{2}):(\d{2})` —— ≥100 分钟视频的 `*Screenshot-120:30*` / `*Content-120:30*` 不匹配，标记原样留在最终笔记正文（video_reader 已修成 `\d+`，这两处漏了）。建议分钟位改 `(\d+)`。

### ✅ 62. 下载器细节组



- `local_downloader.py:33-36,144` extract_cover 默认输出源视频目录 → 本地任务在用户媒体文件夹留 `{title}_cover.jpg`（与已修的 convert_to_mp3 同类污染，且无清理）
- `douyin_downloader.py:244-302` `response.content` 全量内存缓冲（GB 级峰值；kuaishou 已用 stream 分块，未对齐）
- `douyin_downloader.py:236-247` API 失败/aweme_id 为空不拦截，仍写 `{aweme_id}.mp3` 即 `.mp3` 垃圾文件
- `generic_downloader.py:140` 返回 `{id}.mp4` 拼路径不做存在性校验（bilibili/youtube 均有）
- `bilibili_downloader.py:290-291` SRT 解析正则硬编码 `\n` 行尾，CRLF 字幕整段失配 → 判「无字幕」回退转写

### ✅ 63. 2 处 extract_info 未接入 ytdlp_retry



`inspect.py:173`（inspect_video）、`bilibili_downloader.py:225`（字幕 yt-dlp 回退）—— 小项批覆盖 6 处后遗漏这 2 处，瞬时网络错误无退避。建议补包。

### ✅ 64. install.sh 重跑有制造遮蔽条目的反效果



`install.sh:32-49`：插件已装时 marketplace install 分支失败 → 回退路径 `claude mcp add videonote`（空 env），恰好制造 docs/04:31 警示的「遮蔽插件 env」问题；也不清理旧用户级条目。建议回退前查 `claude plugin list`，已装则跳过 mcp add；成功路径主动 `claude mcp remove videonote`。

### ✅ 65. plugin.json userConfig 只覆盖 7/12 env



`.claude-plugin/plugin.json` 漏 `VIDEONOTE_VIDEO_INTERVAL` / `COMMENTS_LIMIT` / `DEFAULT_EXPORT_FORMATS` / `ENABLE_PREPROCESS` / `DIARIZATION`（config.py `_USER_CONFIG_MAPPED_ENV` 12 项）；transcriber_type 描述漏 funasr（:62）。`/plugin configure` 配不了这些默认。建议补全 5 个 env + funasr 文案，CI 校验两键集合一致。

### ✅ 66. 测试固定 task_id 共享目录



`test_status_tools.py:82-84` 等用固定 `"abcdef123456"` / `"t10"` 写共享 `/tmp/videonote_pytest/note_results` —— pytest-xdist 并行或多 checkout 同跑互踩假红（#40 未修部分）。建议 task_id 拼 uuid/pid 后缀或 tmp_path。

### ✅ 67. fastmcp>=2.0 下界过宽



`pyproject.toml:15` —— progress 不可行等结论按 3.4.5 实测，但 uvx git 安装不读 uv.lock，会解析出未验证的 2.x。建议收紧 `fastmcp>=3,<4`；高频变动的 yt-dlp / youtube-transcript-api 考虑上移 lower-bound。

### ✅ 68. kuaishou_downloader 同一行调两次 download



`kuaishou_downloader.py:106`：`print(self.download(...))` 的参数与 return 各调一次 self.download() —— 每轮下载做两轮 GraphQL API + 可能两轮下载/ffmpeg，浪费翻倍。建议先算一次。

### P3 — 工程卫生 / 文档

### ✅ 69. 文档代差残留



- `docs/03-预期效果.md:19,29` 仍写「32 个工具」，实际 37（新增 5 个配置/预检工具）
- `docs/03:18` 平台支持仍写小宇宙「不变，全部保留」（下载器已删，generic 覆盖）
- `docs/02-架构设计.md:44` 保留清单仍写顶层 `events/`（VENDOR.md 已写迁入 app/events/，同仓矛盾）
- `skills/videonote/reference/tools.md:68` fetch_subtitles 成功形状写 `{language, full_text, segments}`，实现已是 `{ok: true, ...}`（#27 落地后未同步）

### ✅ 70. CI 一致性无门禁



- `ci.yml:35` tools/list 只断言 generate_note 存在，不断言工具总数（本次扫描即抓到 docs/03 32 vs 实际 37）
- ruff 用 `uvx ruff` 不 pin 版本，lint 结果逐日漂移
- 仅 ubuntu 单 Python 3.12，声明 `>=3.11,<3.14` 无矩阵覆盖
- `release.yml` 不跑 `uv build`，wheel 内容（app/**、模板资产）无自动验证（07-31 出过 wheel 缺包 bug）

### ✅ 71. CLI 与 MCP 校验不一致



- `cli.py:1326` export 的 task_id 无校验直接拼路径读 JSON（与 MCP 层 `_validate_task_id` 不一致，可读 NOTE_OUTPUT_DIR 外任意 JSON）
- `cli.py:1268` 扫码登录取 SESSDATA 失败时打印完整 `data['url']`（crossDomain ticket 含敏感参数）

### ✅ 72. server.py 细节组



- `server.py:91` `_MAX_WORKERS` 裸 `int(os.environ.get(...))`，非数字 env 在 import 期抛 ValueError 起不来（应 config.env_int + 下限 1）
- `download_transcriber_model` 的 model_size 不校验（任意字符串进模型 map，后台线程抛错无即时反馈）
- `generate_note` 的 video_interval / comments_limit 无下限校验直接透传（0/负值行为未定义；setup 向导有钳制）
- `extract_frames` 内联复制 `_submit_step_task` 整套样板（:1069-1087），两处 _write_status/_index_step_task 顺序已不一致
- `transcribe_media` / `extract_frames` 用 `p.exists()` 校验，传目录也能通过（应 `is_file()`）

### ✅ 73. universal_gpt 的 content=None 会绕过 checkpoint



`universal_gpt.py:269,359`：`response.choices[0].message.content.strip()` 在 try 块外 —— 模型返回 None（refusal / content_filter / reasoning 模型）时 AttributeError，不保存 checkpoint 且错误不可读。建议判空并对 None 抛带 finish_reason 的明确错误。

### ✅ 74. openai_client 从不 close



`openai_client.py:42`：`httpx.Client(proxy=...)` 每次构建从不 close —— 每任务新建 client，连接/fd 跨任务累积（进程级长跑）。建议复用单例或用完 close。

### ✅ 75. transcriber 单例重建不释放旧模型



`transcriber_provider.py:53-70`：尺寸切换时重建单例，旧实例/旧模型不主动释放 —— 任务 A 转写中任务 B 触发 rebuild，新旧两个 whisper 模型同时驻留（large-v3 约 3GB），仅靠 GC。建议引用计数/延迟重建。

### ✅ 76. install.sh 步骤编号错乱



`install.sh:26,61` 输出 1/4 → 2/3 → 3/3（纯观感）。

---

## 第三轮（E3，Wave E 后全库扫描，2026-08-17 · 4 个并行审计代理，代码级核实）

> 本轮聚焦 Wave D/E 引入的回归 + 全仓一致性。4 个 P0 全部当场修复；CI 从数量断言升级为**精确名单断言**杜绝工具名漂移再次逃逸。落地批次与测试见 docs/06「Wave E」；本批 commit 见 git log。

### ✅ 77. get_task_status 装饰器丢失（P0）

`3c0c9a67` 重构随 `_stage_label` 抽取把 `get_task_status` 的 `@mcp.tool()` 删了——工具从 38 降到 37，却随 v0.1.5 发布。CI 原 38 数量断言被后来新增的 `batch_generate_notes` 补足，**数量断言未红**。修复（F1）：装饰器恢复；CI 改为精确 39 名单（#81）。

### ✅ 78. 图片 token 估算把 base64 按字符计（P0，6c 回归）

6c token 级切块引入 `_estimate_tokens`，对 `image_url` 消息走 `json.dumps` 全量计数——base64 图片按**字符**算（一张图 ≈ 数十万 token），video_understanding 被静默切碎/失效。修复（F2）：结构感知递归 `_count_tokens`——`image_url.url` 按 `_TOKENS_PER_IMAGE=1105` 固定估算（OpenAI detail=auto 上限），其余文本汉字 1:1 + 其他字符 4:1。

### ✅ 79. merge 分组共用 max_tokens → 永不收敛死循环（P0）

`_merge_partials` 对合并轮次复用 `max_tokens_per_chunk`——partials 总字数超过单 chunk 上限时，每组都超限、每轮都再分，**无限递归**。修复（F3）：merge 场景 `merge_chunker` 用 `max_tokens=None`（只受 45MB 字节约束，partials 总量远小于此），另加 `len(groups) >= len(current_partials)` 时 raise 明确错误防退化；`source.video_img_urls` 非空时丢图会 logger.warning（不再静默）。

### ✅ 80. wheel include 缺 skills/** → 发布通道锁死（P0）

`pyproject.toml` 的 wheel include 只有 `videonote_mcp/**`，而 release.yml 门禁断言 wheel 含 skills——`uv build` 出包不含 skills，发布必失败。修复（F8）：include 加 `"skills/**"`，`rm -rf dist` 重建后 148 文件（含 skills/）验证通过。

### ✅ 81. CI 数量断言 → 精确名单（P0 配套）

#77 说明数量断言对名字漂移失效。修复（F9）：`.github/workflows/ci.yml` 改为 39 工具**精确名单**（sorted 比对），改名/增删工具必须同步 CI。

### ✅ 82. 并发门禁语义修正（F7）+ batch 排队字段（H5）

`_guard_concurrency` 原 `not f.done()` 把**排队中**的任务也算作占用（batch 批量排队承诺「等待不占名额」不成立）。改为 `f.running()` 只统计正在执行；新增契约测试 `test_guard_allows_queued_not_yet_running`。batch_generate_notes 补 `total` / `remaining` 字段（H5）。

### ✅ 83. note_dir 契约（G2）+ 素材包转写保留（G3）

**G2**：`note.md` 恒在 `{task_id}/gen/`，但 `result.note_dir` 此前指向 `task_dir`（与文档「note_dir 指向 note.md 所在目录」矛盾）。改为：`note_dir` 指向 `gen/`（存在时），指定 `notes_dir` 时从 manifest（`get_task_paths`）定位便携副本并补 `result.portable_note_dir`。**G3**：`transcribe_media` / `prepare_note_material` 的转写是主产物，此前被 `get_task_status` 默认剥离只剩空壳。改为：仅 note 任务（无 `kind`）默认剥转写/评论；素材包默认保留（`raw` 恒剥）。output-formats.md / tools.md / docs/02/04 契约同步。

### ✅ 84. _absolutize_images 存量 no-op bug + 路径穿越（H4）

**存量 bug**：正则只有 1 个捕获组但代码用 `m.group(2)`——必然 IndexError → 走 except → 原样返回。截图 absolutize **从未生效**（静默 no-op，无测试所以没暴露）。**同时**加路径穿越防护：解析结果必须 `relative_to(base.resolve())`，逃出数据目录的路径原样保留（不生成 `file://` 泄露）。补 2 个契约测试。

### ✅ 85. H 组快修（8 项）

- H1 `download_transcriber_model` docstring 在 raise 之后（死代码，FastMCP 工具描述丢失）→ 移到首行；
- H2 `_local_video_exists` 空串→`Path(".")` 误判、目录误算「存在」→ 改 `is_file()`；
- H3 `get_task_status` 的 `float(started_at)` 损坏时抛到 MCP 层 → try 保护；
- H5 stderr env 非法值（如 `abc`）→ 回退默认阈值而非日志重定向失败；
- H6 `generate_note` handoff/本地校验前移到 provider 解析前（unsupported 链接不再先撞 provider 报错）；
- H7 `install.sh` 生产安装 `uv sync --no-dev`（不带 pytest/ruff）；
- H8 `_stage_label` 补文档；G2 后 generate_note docstring 的 note_dir 描述同步。

### ✅ 86. G2 收尾——video_tasks 列 note_dir 与 result.json 一致

`list_tasks`（video_tasks 表）与 `get_task_status`（result.json）对同一任务曾返回不同 note_dir（task_dir vs gen/）。note.py `_save_metadata` 的 note 任务分支改存 `_note_dir`（gen/），material/step 任务保留 task_dir（无 note.md）。补 NoteDirContractTest 2 项（note_dir→gen/、portable_note_dir 经 manifest 定位）。

### ✅ 87. extract_frames 参数校验

`video_interval=0` 曾被 `or 6` 吞成 6（用户传 0 想立即截帧得到 6s 间隔）；负数/非数字透传到 ffmpeg。改为入口钳制 `max(1, int(interval))`。`grid_size` 此前 `[0,3]`/`[3]`/`['a']` 直接透传——现要求两个正整数，否则 ValueError。补 3 项契约测试。

### ✅ 89. _absolutize_images 支持便携笔记 Assets/ 相对引用（#84 修复闭环）

#84 修的 `_absolutize_images` 只匹配 `static/screenshots/` 旧全局模式——但 note.py 数据层重构后截图走 `gen/Assets/` + `Assets/xxx.jpg` 相对引用，旧正则**不命中**：`result.markdown` 里的图片对 Agent 是死路径（Read 不到）。修复：`_absolutize_images(markdown, base_dir)` 新增 base_dir 参数处理 `](Assets/...)` → `file://{note_dir}/Assets/...`（同样 resolve + 目录内校验防穿越）；`get_task_status` 传 `base_dir=result.note_dir`（G2 后指向 gen/）。旧 `static/screenshots` 路径保留兼容旧任务。补 3 项测试。

### ✅ 90. 下载器健壮性（#36 剩余子项，commit f5bdc8b）

#36 四个剩余弱点的落地 + 顺带发现的新问题：

- **kuaishou TypeError**：`KuaiShou.run()` 在 URL/cookie/photo_id/details 解析失败时只 `logger.error` 继续走，`video_details['data']` 在 None 时抛 TypeError；`get_photo_id` 的 `match.group()` 在无匹配时 AttributeError；`_extract_kuaishou_link` 空列表 IndexError。改：各失败点抛带 URL 的 `RuntimeError`（调用方 `_handle_exception` 已有统一处理），match 无匹配返回 None，`get_photo_id` 的 requests.get 补 timeout=(5,10)。
- **Bcut 退避**：轮询 `time.sleep(1)` 固定 1s ×500 次。改指数退避 `min(1 << i, 5)`（1→2→4→5s 封顶）；5 处 session HTTP 调用全部补 timeout（上传 10/30、分片 10/120、查询 5/10）。
- **generic cookie 域名**：Netscape 行 host 写死 `example.com`，yt-dlp cookiejar 对真实站点永不匹配——cookie 实际无效。改 `http_headers: {"Cookie": ...}` 直接注入（generic 站点域名无法预知，header 对目标站及 CDN 分片请求统一生效）；删 Netscape 文件逻辑。
- **audio.json 悬空实体**：缓存 JSON 存在但 `file_path` 文件被清理/删时直接返回，下游转写 FileNotFoundError。改：需要真实音频（非 skip_download）时实体缺失视为缓存失效重新下载；skip_download（有字幕/元信息场景）悬空无害照常返回。

新测试 tests/test_downloader_robustness.py 16 项。**252 passed + ruff clean**。仅剩 abogus（Douyin 签名硬编码上游）不修。

### ✅ 91. DB 默认路径固定到数据目录（#33 仓库垃圾的真实根因，commit 5ba8497）

`app/db/engine.py` 的默认 `DATABASE_URL = "sqlite:///video_note.db"` 是**相对 CWD** 路径——任何不经过 `videonote_mcp.config` 的 import（裸脚本、`python tests/foo.py` 单文件跑法——项目文档推荐且不加载 conftest）都会在进程 CWD 分裂出一个新 DB。仓库根目录的 `video_note.db`（gitignore 忽略的旧泄漏）就是这么来的，且不同启动目录的 MCP/脚本各写各的库互不相通。修复：默认值改 `sqlite:///{get_data_dir()}/video_note.db`（path_helper 稳定定位，与 config.py 的 setdefault 同值）；同时 `base.py` 的 `cache_data` 从 vendored 旧 `DATA_DIR` env 改为 `get_data_dir()`（旧 env 是第二个泄漏面）。顺带清理根目录残留 db 与 -wal/-shm。已验证：裸脚本 import 不再创建 CWD 库。

### ✅ 92. 真实下载器类集成测试（#31 剩余，commit 364e7ad）

`tests/test_downloader_integration.py` 15 项——对 bilibili/youtube/local/generic 四个下载器类跑**真实 download / download_video / skip_download 流程**，只 mock yt-dlp（side_effect 捕获 opts + 模拟 outtmpl 文件产物）与 ffmpeg（local 转码），验证：AudioDownloadResult 字段契约（file_path 指向 output_dir/{id}.ext、平台/时长/标题）、bilibili quality→preferredquality（32/64/128）、skip_download 语义、bilibili 多 P glob 缓存命中（{BV}_pN 前缀）、ytdlp_retry 网络错误重试/业务错误不重试（DownloadError 语义）、download_video 下载后文件缺失抛错、local 转码与元信息模式。

**顺带发现 3 个真实问题一并修**：
- `extract_video_id` 解析失败返回 None 时，youtube/bilibili 的 `download_video` 不检查 → `None.mp4` 的误导性 FileNotFoundError。改：解析失败抛 `ValueError(f"无法从链接提取…视频 ID")`。
- douyin `raise ValueError("请求失败:", e)` 双位置参数 → tuple 错误信息。改 `f"抖音下载请求失败: {e}"` + `from e`。
- ruff F 全绿。

### ✅ 94. batch_generate_notes 参数面补全（commit 2c1ad9f）

Schema 审计发现 `batch_generate_notes` 只有 10 个参数，`_submit` 只透传 quality/provider_id/model_name/format/style/screenshot/extras——缺 `link` / `video_understanding` / `video_interval` / `grid_size` / `include_comments` / `comments_limit` / `notes_dir`，与 docstring 宣称的「与 generate_note 一致」不符（批量做视频理解/弹幕整合做不到）。补全 7 个参数透传 + docstring 对齐。**CI 门禁立功**：plugin.json env 一致性测试拦截了本批的 userConfig 扩展（`_USER_CONFIG_MAPPED_ENV` 需同步新增 `VIDEONOTE_MAX_WORKERS` / `VIDEONOTE_STDERR_LOG_MAX_MB`）。新增透传契约测试 1 项。**268 passed + ruff clean**。

### ✅ 95. 死代码清理（Wave C 13 收口，commit 3cf63ea）

`app/gpt/` 的 `openai_gpt.py` / `deepseek_gpt.py` / `qwen_gpt.py` / `utils.py` / `tools.py` 全仓**零引用**——gpt_factory 走 `OpenAICompatibleProvider` 直连，上游这些直连类/工具模块是死代码。AST 引用图核实后删除；VENDOR.md 分叉清单同步；另跑全仓死模块普查（app/ + videonote_mcp/，含相对导入解析）确认无其余残留（E3 批已清过一轮）。**268 passed + ruff F-clean**。

### ✅ 96. CLI 契约测试 + update_provider 假成功缺陷（commit 6dad3b7）

**缺陷（P1）**：`ProviderService.update_provider` 对**不存在的供应商**返回 `{'id':…, 'enabled': None}`（恒真 dict）——DAO 更新返回 None，Service 包装层却构造了非 None 返回值，CLI `providers set` 与 MCP `update_provider` 的 `if not updated` 判空**永不生效**，不存在被报成「已更新」。修复：`updated_provider is None` 时返回 None（CLI 走 stderr「不存在」+ exit 1，MCP 抛 ValueError）。

**新测试**（tests/test_cli.py 18 项，CLI 此前零覆盖）：providers add/set/list（加密落盘 enc: 前缀、掩码首尾、交互取 key、缺参 SystemExit(2)、不存在 SystemExit(1)）；transcriber set/list/preprocess/diarization 配置闭环；export list/找不到任务/真实任务渲染 transcript.srt；main 未知子命令 + 全新 DB seed 内置供应商（subprocess）。探索中排除了两个假警报：export 的 `NOTE_OUTPUT_DIR` 已被 `setup_environment` 设为数据目录绝对路径（无 CWD 漂移）；`builtins.print` 重定向是 MCP 场景防 stdio 污染的设计（server 不 import cli，无泄漏面）。另清 2 处旧 worktree 路径 docstring（test_task_manifest/test_bilibili_comment）。**286 passed + ruff F-clean**。

### ✅ 98. fetch_comments limit 钳制（commit db93dbe）

`limit<=0` 时 fetcher 的 `len(seen) >= limit` 恒真（0 >= 0），第一页就停止——**静默返回空评论**（ok:true 但无数据，Agent 误以为视频没评论）。工具入口钳制 `max(1, int(limit))`。+3 契约测试。**296 passed + ruff F-clean**。

### ✅ 99. notes_dir 数据目录外提示（完整路径沙箱收口，commit 68d8a62）

export/merge 早有「数据目录外只提示不拦截」warning（#45 口径），唯一漏网的是 `notes_dir`——便携笔记写任意绝对路径静默无声。generate_note 解析完缺省链（参数 → app_config → VIDEONOTE_NOTES_DIR）后统一校验，数据目录外打 warning（便携笔记是显式用户意图，不拦截；batch_generate_notes 委托 generate_note 自动继承）。+3 契约测试（目录外警告 / 目录内静默 / env 缺省链解析后仍校验）。**299 passed + ruff F-clean**。

### ✅ 100. grid_size 入口显式校验（commit 6b68d3f）

`extract_frames` **工具**校验 grid_size（两个正整数），但 `generate_note` / `prepare_note_material` 的 grid_size 直接透传——非法值（[0,0] / [1] / [1,2,3]）在流水线深处的 VideoReader 才炸成「视频处理失败」泛化错误（note.py 668-671 的兜底注释本身就记录过空 tuple 的此类事故）。修复：校验提取为共享助手 `_check_grid_size`（与 style/format 同口径：None/空不拦、入口显式报错），接入 generate_note / prepare_note_material / extract_frames 三处；顺带清理 note.py 2 处裸 `except:`（错误兜底路径吞 KeyboardInterrupt 的卫生问题）。+7 契约测试。**306 passed + ruff F-clean**。

### ✅ 101. num_speakers 无效值显式提示（commit 5736b47）

`diarization.diarize_audio` 的 `kwargs = {"num_speakers": n} if n else {}`——0 / 负值 / 非 int 静默回退自动检测（用户显式传了无效值却无声无息）。修复：入口校验，无效值打 warning 后回退（合法值照常透传）。+4 单元测试。**310 passed + ruff F-clean**。

### ✅ 102. list_tasks 分页（commit a8ffb19）

`list_tasks` 无上限返回全量——每行含 200 字 summary，长跑用户任务上百条时响应膨胀。加可选 `limit` / `offset`（缺省全量，向后兼容；limit 钳制 ≥1，offset 钳制 ≥0）。tools.md 同步。+4 契约测试。**314 passed + ruff F-clean**。

### ✅ 103. set_transcriber 引擎白名单（commit 25cdd4e）

未知 `transcriber_type` 会被持久化，运行时 `get_transcriber` 的 `TranscriberType(...)` 解析失败**静默回退 fast-whisper**——用户以为配了 groq/bcut 云端引擎，实际在跑本地 whisper（慢、模型未下载还会失败）。修复：set_transcriber 入口白名单校验（与 style/format 同口径，CLI 侧 argparse choices 本就安全）；白名单与 TranscriberType 枚举同源防漂移。+3 契约测试。**317 passed + ruff F-clean**。

### ✅ 104. 静默错误族收尾：transcript 形状 / video_interval 非数值 / export 格式白名单（commit e5d0eaf）

「静默错误」排查（#97-#103 同族）剩余三处入口：

1. **summarize_note 的 transcript 形状**：`_coerce_transcript` 对垃圾输入（空 dict、纯 `{"language": ...}`、传了 fetch 结果外层 `{"ok": ...}`、非 dict 值）静默规整成空素材——LLM 拿到零转写**凭空生成笔记**（幻觉内容还烧配额）。入口显式报错（在 provider 解析前，H6）；只查字段存在不查内容（静音视频的 `segments: []` 是合法转写，是否总结由用户决定）。
2. **extract_frames 的 video_interval 非数值**：`int("abc")` 失败静默回退默认 6（用户设了间隔却无声生效默认值）。与 num_speakers 同口径（#101）：打 warning 后回退；数字字符串照常透传。
3. **export_transcript 未知格式静默丢弃**：底层 exporter 对未知格式只写 stderr 警告后跳过——Agent 请求 `["srt","pdf"]` 拿到 `ok:true` 实际缺 pdf。入口白名单校验（srt/vtt/json，与 exporter 的 FORMATS 同源）；缺省链（config/env 默认）解析后同样校验，配置有垃圾直接显式报错。

+14 契约测试。**331 passed + ruff F-clean**。

### ✅ 105. 误导错误收口：fetch_subtitles 平台白名单 / merge_audio file:// / diarize_media 目录输入（commit 5cab61f）

「静默错误」同族的**误导错误**三处（报错但不准，Agent 误诊）：

1. **fetch_subtitles 的 platform 拼错被吞**：`pipeline.fetch_subtitles` 把 `get_downloader` 的 `ValueError("不支持的平台")` 吞掉转成 `None`——工具报「该视频没有可用平台字幕」，用户以为视频没字幕实际是拼错平台名（其余平台参数工具如 generate_note 的平台错误在任务结果里显式 FAILED，唯独这里静默转换）。入口白名单校验（与 `SUPPORT_PLATFORM_MAP` 同源防漂移；空串曾因 falsy 静默走自动检测，一并显式报错）。
2. **merge_audio 不认 file://**：全工具面唯一漏网——`_coerce_local_path` 只接入 transcribe_media/extract_frames/diarize_media，merge_audio 直传 `file:///.../a%20b.mp3` 被 app 层 `Path("file:///...")` 判不存在，误报「文件不存在」（%20 未 unquote）。入口统一规整。
3. **diarize_media 目录输入穿透**：`.exists()` 对目录为 True——目录路径穿透到 ffmpeg 深处炸「转换失败」泛化错误；与其余本地文件工具同口径改 `.is_file()`。

+9 契约测试。**340 passed + ruff F-clean**。



### ✅ 106. app 层静默降级加固（E3 后第二轮全库扫描，4 并行代理 → 7 项，commit b6e36cc）

对 `app/` 全部 8 个目录的 except 块 / falsy 回退 / 成功形状返回做第二轮通读（第一轮 E3 已清 4 P0），无 P1，修复 7 项：

1. **配置管理器损坏静默当空（最严重，cookie 会被抹）**：`cookie_manager` / `transcriber_config_manager` / `proxy_config_manager` 的 `_read` 对损坏 JSON 静默返回 `{}`（引擎悄悄回退 fast-whisper、B 站 cookie 悄悄消失，全程无日志）；`cookie_manager.set()` 读损坏文件得 `{}` 再写回会把**其它平台 cookie 永久抹掉**（数据丢失）。修复：新增 `app/utils/json_store.py`（`read_json`：损坏 → warning + 备份 `<file>.corrupt` + 空配置；`write_json_atomic`：tmp + replace + 0600，与 config.py 的 app_config 同款），三个管理器全部接入。
2. **模型名解析失败谎报「未下载」**：`check_whisper_model_exists` 对 `resolve_whisper_model` 的任何异常返回 False——自定义映射坏 target / size 拼写错被门禁报成「模型未下载」，用户反复 `download` 永远下不完。区分 ValueError（无法解析）与其它异常，均打 warning 留痕。
3. **probe 失败分段时间轴漂移**：`pipeline.chunk_duration_guess` 在 `probe_duration` 失败时无日志回退 1800s——预处理分块时间偏移按 1800 累计漂移数十秒，任务照常 SUCCESS。打 warning（含 exc）留痕。
4. **全局索引同步失败 debug 级**：`note.py _update_status` 的 video_tasks 同步失败只记 debug——任务状态静默不进全局索引（list_tasks 查不到、cleanup 找不到）。升 warning。
5. **started_at 读取失败静默**：`note.py _update_status` 读旧 status.json 失败 `except: pass`——elapsed_secs 失真。打 warning。
6. **checkpoint 损坏静默弃用**：`universal_gpt._load_checkpoint` 对损坏 checkpoint 静默 unlink 从零重总结——已消耗的 LLM 配额白烧且无从察觉。打 warning。
7. **播放列表坏条目成功形状**：`inspect._inspect_ytdlp` 把非 http 的 page_url 条目原样塞进 `ok:True` 的 entries——Agent 拿无效 URL 去下载阶段才失败。跳过坏条目 + warning，全坏时返回显式错误。

+17 测试（tests/test_json_store.py 10 项 + tests/test_app_silent_fallbacks.py 7 项）。**357 passed + ruff F-clean**。VENDOR.md 冻结清单同步（json_store / cookie_manager / proxy_config_manager / universal_gpt）。

### ✅ 107. 输出目录 file:// 规整 + 导出格式垃圾配置回退（commit 12ab0d4）

「静默错误」家族入口校验的最后一类缺口——输入文件已有 `_coerce_local_path`，**输出目录**参数没有：

1. **输出目录 file:// 直传建字面 `file:` 目录**：`generate_note(notes_dir)` / `export_transcript(out_dir)` / `merge_audio(out_dir)` 对 `file:///…` URI 直接 `Path("file:///…")`——POSIX 下解析成 CWD 下的相对路径 `file:`，在笔记库根创建垃圾目录；且「数据目录外」提示基于未规整值，URI 形式恒误报。三处统一 `_coerce_local_path` 规整后再校验/使用（batch 委托 generate_note 自动继承；输出文件参数是「自己主动找活干」扫描的收口项，与 #105 输入侧对应）。
2. **`default_export_formats` 垃圾配置遮蔽 env 回退**：app_config 里非列表 truthy 值（如字符串 `"srt,vtt"`）令 `or` 短路——工具入口炸「必须是列表」、自动导出静默失败，用户设的 env 永远不生效。抽 `_resolve_default_export_formats()`（app_config 须为列表，否则 warning + 回退 env）接入 `export_transcript` 与 `_auto_export_transcript` 两处缺省链。

+7 契约测试（OutputDirFileUriTest 4 项 + DefaultExportFormatsJunkTest 3 项）。**364 passed + ruff F-clean**。真实 stdio 冒烟：merge_audio 传 file:// out_dir 无字面 file: 目录，ffmpeg 收到规整后的真实路径。

### ✅ 108. set_transcriber 的 whisper_model_size 入口校验（commit b380a08）

「静默错误」家族 #103 的收口——引擎类型已白名单，**模型尺寸**没有：

1. **非法尺寸被持久化，任务跑到 TRANSCRIBING 才炸**：`set_transcriber("fast-whisper", whisper_model_size="bogus")` 成功返回后，配置落盘；运行时 `get_transcriber` 用该尺寸加载模型（#106 之后 preflight 也只报「未下载」）——用户以为配好了，几分钟后任务才失败。用运行时同源的 `resolve_whisper_model`（whisper_models 注册表：内置档位 / 自定义名 / 含 `/` 的 HF repo_id / 已存在本地目录）入口校验，未知尺寸立即报错。
2. **CLI `transcriber set --size` 同漏洞**：`download` 子命令的 size 有 argparse choices，`set --size` 是自由串——同样入口拒绝（与 MCP 同口径）。

+7 契约测试（SetTranscriberSizeValidationTest 5 项，含 repo_id/本地目录直通不被误伤 + CLI 2 项）。**371 passed + ruff F-clean**。

### ✅ 109. merge_audio 目录输入穿透 + batch 单集退化裸传（commit 5f81093）

「误导错误」家族再收 2 个边角：

1. **merge_audio 目录输入到 ffmpeg 深处才炸泛化错误**：merge.py 用 `os.path.exists`（对目录为 True）——目录/不存在与普通文件的检查等价，目录穿透后 ffmpeg 报「转换失败 <目录>」误导（真因是「是目录不是文件」）。入口 `is_file` 校验（与 diarize_media 同口径），返回 `{ok: false, error: "文件不存在或不是文件: ..."}`；file:// 规整（#107）与 is_file（#109）叠加生效。
2. **batch_generate_notes 单集退化路径异常裸传**：多条目循环有 try/except「单条失败收集继续」，但 `kind == "single"` 退化路径的 `_submit` 无保护——provider/模型/并发门禁错误 raise 裸传中断整个调用（且与多条目 `{ok, errors}` 形状不一致）。收进 errors 返回同形状（`ok: false, submitted: 0`），契约统一。

+5 契约测试（MergeAudioInputValidationTest 3 项 + BatchSingleEntryErrorTest 2 项）。**376 passed + ruff F-clean**。

### ✅ 110. SKILL/文档与 batch 工具面对齐（commit 16c853a）

6e 落地 `batch_generate_notes`（服务端展开+排队）后，SKILL 仍是 subagent 时代的编排：`SKILL.md` 规则 2 只教「多集/多链接：每个 url 一个 subagent」、默认路径步骤 4 的 `kind=multi` 分支没提 batch、工作流一节「多集：subagent 各自跑」；`troubleshooting.md` 并发段与 `docs/04` 同样。而 batch 工具 docstring 明确「与 SKILL 纪律不同——展开+排队收敛到服务端」。Agent 读 SKILL 会为 200 集播放列表开一堆 subagent。修 4 处口径：

- **SKILL.md**（3 处）：规则 2 拆分「合集/分P/播放列表 → 一条 batch_generate_notes（同门禁，超出排队）；互相独立的多个链接 → 各一个 subagent」；默认路径步骤 4 `kind=multi` 分支补 batch；工作流「多集」一行同步。
- **troubleshooting.md**（2 处）：并发上限表行与长段补 batch 路径。
- **docs/04**：agent 速览后的编排说明同步。

纯文档批次，无代码改动。**376 passed + ruff F-clean**。

### ✅ 111. cleanup 对运行中任务无防护（commit 23ac4f8）

`cleanup_task_files` / `cleanup_all_files` 不检查任务是否还在运行：对 `_task_futures` 中未完成的 note/步骤任务调 `cleanup_note`，会删掉下载器正在写的 `raw/`、转写器正在写的 `gen/` 缓存——任务中途 `FileNotFoundError` 失败（被 FAILED 吞成「转写失败」误导错误），或状态文件在删除后被重建出幽灵任务；`cleanup_all`（恢复出厂）同样会把运行中任务的目录连根拔掉。此前唯一的防线是「cancel 再清理」靠 agent 自觉。修复（MCP 入口，`_task_futures` 本进程精确判定）：

- **`cleanup_note`**：task_id 在 `_task_futures` 且 future 未完成（含排队）→ 拒绝，返回 `{ok: false, task_id, error: "任务仍在运行（或排队中）：先 cancel_note，或等待终态后再清理"}`。
- **`cleanup_all`**：存在任一未完成 future → 拒绝，返回 `{ok: false, running, running_task_ids, error}`，agent 可按 ids 逐个 cancel。
- CLI 向导不动：交互式已有 `[status]` 展示 + 双重 confirm，人看得见状态；且 CLI 是独立进程，看不到 MCP 的 `_task_futures`，用 status.json 判「非终态」会在 server 崩溃残留 PENDING 时误锁清理。

+4 契约测试（CleanupRunningTaskGuardTest：拒绝运行中/放行终态/全清拒绝/全清放行，mock `cleanup_task_files` / `cleanup_all_files` 断言未调用）。**380 passed + ruff F-clean**。

### ✅ 112. cleanup_note(include_note=True) 便携笔记副本漏删/孤儿化（commit fd6ce47）

`cleanup_task_files(include_note=True)` 只删 `task_dir`——便携笔记副本（`<notes_dir>/<标题>/note.md`，用户指定 `notes_dir` 时写**数据目录外**，正是 #99 的场景）不在删除范围：**数据目录内**的副本（`notes_dir` 指向数据目录子目录时）漏删；**数据目录外**的副本随 manifest 删除而永久失联——`list_tasks` / `get_task_files` / `list_task_files` 全部查不到，磁盘上却留着含笔记+Assets 的目录，docstring 却声称「连最终笔记一起删」。修复（`cleanup_task_files`）：

- **数据目录内的便携副本**（manifest 记录 + `_safe_resolve` 沙箱校验通过，含 note.md 文件或含 note.md 的目录）→ 随 `include_note=True` 一起删（同名冲突有 task_id 后缀，目录按任务隔离）。
- **数据目录外的副本** → 沙箱红线不删（与 cleanup_all 同口径，绝不删用户目录外的数据），但路径经新增返回字段 `notes_kept_outside` 列出——Agent 可转告用户「笔记副本保留在 <路径>」，不再静默孤儿化。
- 返回形状新增 `notes_kept_outside`（空列表兜底）；server cleanup_note docstring、CLI 向导（清理后黄字列出未删路径）、skills tools.md 同步。

+2 测试（数据目录内副本删除 / 目录外副本报告）。**382 passed + ruff F-clean**。

### ✅ 113. LLM/转写调用超时收敛（commit 6f3b977）

`build_openai_client` 的 timeout 参数缺省 None → openai SDK 默认 **600s**（代理路径还另写死 600s）：上游 LLM API 挂死（连接黑洞/死读）时，任务卡在 SUMMARIZING 10 分钟才超时，且 universal_gpt 有 3 次重试（指数退避）——最坏 ~30 分钟/chunk，期间 worker 槽被占死，`_guard_concurrency` 把后续所有提交拒之门外（「已有 3 个任务在同时执行」），cancel_note 也因请求挂起无法在 chunk 边界生效。全项目外部调用都有超时（#58 ffmpeg 家族），唯独 LLM 调用是裸 SDK 默认。修复（构造点单点收敛，`app/utils/openai_client.py`）：

- 新增 `DEFAULT_TIMEOUT = httpx.Timeout(300.0, connect=10.0)`——**连接 10s 快速失败**（DNS/拒绝连接不再拖 600s），读写 300s 兜底死读；timeout 缺省即用它。
- LLM 主路径（`gpt_factory` → `OpenAICompatibleProvider`）与 groq 转写（`app/transcriber/groq.py`）两个未传 timeout 的调用点同时收敛；显式传参不受影响（连通性测试 15s 照旧）。
- 代理路径的 `httpx.Client` 与客户端超时同源（删掉另写 600s 的旧分支）。

+4 测试（tests/test_openai_client.py：默认超时/显式覆盖/代理路径同源/空 key 拒绝）。**386 passed + ruff F-clean**。

### ✅ 114. 「无转写」文案误报：运行中任务被说成「可能未成功」（commit 936a3c4）

三个读转写入口在拿不到转写时都笼统报「尚未成功或已清理 / 任务可能未成功」——任务还在 DOWNLOADING/TRANSCRIBING 时，Agent 据此向用户误报「任务失败了」，实际只需再等等。`get_task_status` 有 `stage`，但 `get_task_transcript` / MCP Resource `transcript_resource` / `export_transcript` 三个**读产物**的入口不看 status.json。修复（新增共享原因函数 `_transcript_unavailable_reason`，读 status.json 区分四态）：

- **运行中**（PENDING/…/SUMMARIZING 等非终态）→「任务仍在运行（{status}）：先 get_task_status 等终态」；
- **未成功**（FAILED/CANCELLED）→「任务未成功（{status}）」；
- **成功但无转写** →「任务成功但没有转写」；
- **状态不可读**（目录不存在/损坏）→「任务状态不可读（可能已清理）」。

三处接入：`export_transcript` 的 error、`transcript_resource` 的文本、`get_task_transcript` 的 message（结构化 status 字段保留）。+5 契约测试（TranscriptUnavailableReasonTest：导出运行中/失败/不存在 + Resource 运行中 + get_task_transcript 运行中）。**391 passed + ruff F-clean**。

### ✅ 115. preflight 队列检查与并发门禁不同源（commit 900b9c3）

`preflight` 的 queue 检查用 `len(_task_futures)`（**含排队**任务）且详情写「N/3 进行中」——`_guard_concurrency`（F7）只统计 `future.running()`，排队任务不占名额（batch_generate_notes 的展开+排队语义依赖此判定）。不一致后果：提交 10 集 batch 后（3 运行 + 7 排队）再对新视频跑 preflight，会报「10/3 进行中（已满，请等任务完成）」——门禁实际会放行排队的提交，preflight 却给 Agent 错误建议「等全部跑完」。修复：与门禁同源——只统计 running()，详情分「N/M 运行中（另 K 排队）」；运行中占满 `_MAX_WORKERS` 才报已满。+2 契约测试（排队不占名额不误拦 / 运行中占满才拦）。**393 passed + ruff F-clean**。

### ✅ 116. app_config 整数配置垃圾值遮蔽回退链 + falsy 0 优先级倒挂（commit bc61251）

`video_interval` / `comments_limit` 的缺省链（参数 → app_config → env → 默认）用
`get(...) or env_int(...)` 的 truthy 短路——两处缺陷（与 #107 default_export_formats 同族）：
① 垃圾值（手动编辑 app_config.json 写入 `"abc"`）让 `int()` 在工具入口裸 ValueError，
env 回退被遮蔽；② `0`（显式关闭，如 video_interval=0 关视频理解）被当 falsy 吞掉，
app_config 优先级倒挂给 env（env=6 时配置的「关闭」失效）。修复：抽 `_resolve_int_config`
（`is not None` 判断 + 防御性 `int()`，垃圾值 warning 后回退 env/默认）接入 generate_note /
prepare_note_material 的 4 处。+6 契约测试。**399 passed + ruff F-clean**。

### ✅ 117. format 字符串/混型被拆字符集 + sorted 裸炸（commit 108a3e6）

`_check_style_and_format` 用 `set(formats) - set(_FORMAT_VALUES)`——字符串 `"toc"` 穿透到
`set()` 被拆成字符集，报「收到: ['c','o','t']」把合法格式说成非法（误导错误）；int/str
混排（如 `[1, "toc"]`）还让 `sorted()` 裸 TypeError。入口显式要求字符串列表（与
export_transcript #104 口径一致），未知元素字符串化后报出。接入 generate_note /
summarize_note 两处（batch 委托 generate_note 自动受益）。+3 契约测试。
**402 passed + ruff F-clean**。

### ✅ 118. 预处理分块转写全失败静默产空笔记 + 状态写失败静默 + 终态 elapsed 归零（commit 10940aa）

第三轮并行扫描（transcriber 失败形状 + note.py 状态机）→ 5 项：
① **P0** `_transcribe_with_preprocess` 分块失败只 warning 跳过——全块失败返回空转写，上层当成功缓存 → 任务 SUCCESS 产空笔记；全失败改 raise RuntimeError（任务显式 FAILED 带块数与首个错误），部分失败汇总 warning + 返回 dict 附 `truncated` 标记。② **P0 收敛** `_write_status` 写盘无保护——磁盘满/只读时裸抛进后台线程被吞、FAILED 重写循环同样失败；加保护（写失败 logger.error 不裸抛）+ `_status_memory` 内存快照，get_task_status 读盘损坏时回退快照（「状态文件读取失败」误导文案消除）。③ **P1** `_write_status` 每次重打 started_at——PENDING→INITIALIZING→…→SUCCESS 全由本函数写，成功任务终态 elapsed≈0；改保留旧值。④ **P1** funasr 空结果显式成功分支无留痕——warning 提示（静音合法，留痕排查）。⑤ **P2** `probe_duration` ffprobe 失败静默返回 0（未知时长被当「不长」跳过超长分块）→ warning；`_index_step_task` 无日志 pass → warning。+6 测试。**408 passed + ruff F-clean**。

### ✅ 119. 素材包 audio_path 悬空误导 + 分块转写英文连词 + 死代码（commit 1862c04）

并行扫描延续（素材包契约 + 预处理路径细节）→ 3 项：
① **P2** skip_download 字幕路径的 `audio.file_path` 是下载器拼出来的假路径（文件不存在）——跨任务媒体缓存 miss 是常态（字幕路径从不写媒体缓存），此前原样透传给素材包 → `audio_path` 指向不存在的文件，Agent Read 失败；改为 miss 时置 None + info 留痕（诚实契约：无真实媒体不谎报路径，promote_media 对 None 安全跳过）。② **P3** `_transcribe_with_preprocess` 的 `full_text = "".join(...)` 无分隔拼接——英文 chunk 边界连词（hello+hello → hellohello），改空格连接。③ **P3** `universal_gpt._is_insufficient_quota_error` 全仓零引用死代码删除。+2 测试。**410 passed + ruff F-clean**。

### ✅ 120. #119 回归链 + screenshot/format 双向闭合 + 向导加固（commit 619ae45）

第 4 轮并行扫描（CLI 12 项 + gpt 总结路径 11 项，无 P0）→ 聚焦可修可测 14 项 + 修复期间发现的回归链：
① **R1（回归链）** #119 把媒体缓存 miss 的 `file_path` 置 None 后，缓存加载分支的 falsy 检查把 None 当「缓存有效」直接返回 → 切换转写引擎后重跑同视频时 `Path(None)` 抛误导性 TypeError；改 `file_path is None or not is_file()` 视为失效重新下载。② **G1 双向闭合** screenshot/link 布尔开关从不下发到 format 列表：screenshot=True 时视频白下载但 prompt 无标记指令 → LLM 不输出 `*Screenshot-[mm:ss]`、笔记无图；反向 format=["screenshot"] 但布尔 False 时有字幕就不下载视频 → 标记残留。server 层布尔并入 `_format` + note 层 `_format` 回并布尔（`_download_media.need_video` 只认布尔），`need_full_download` 含 format 检查——两个方向都闭合，矛盾输入有确定行为。③ **G2** chunker 估算每 chunk 都计评论（summarize 只把评论注入首 chunk）→ 评论大时 chunk 数成倍膨胀、极端抛误导性「single segment too large」；估算与注入同语义（仅首 chunk 计评论）。④ **G4** `_post_process_markdown` 截图插入失败只记「跳过该步骤」无异常详情（同文件 link 分支带 `{e}`）。⑤ **C1-C5/S4 向导加固**：wizard add 重名 ValueError 就地消化不崩（已填 key 不浪费）；`_edit_provider` 更新失败如实报「⚠ 更新失败」（此前失败也打印「已更新」，用户以为 key 已换）；`resolve_int_config` 上移 config.py，CLI 6 处裸 `int(get_app_config().get(...) or N)` 接入（垃圾值不再每次进向导循环就崩）；`export` 未知格式入口拒绝（此前 exporter 只 warning 丢弃后仍打印「✓ 已导出 N 个格式」）、转写源与 server 同源（gen/transcript.json 优先、损坏 warning 后回退 result.json、result 损坏分开报）；`login` 失败路径 `exit_on_fail=False` 供向导调用（登录失败不再让整个向导带 traceback/退出，已配设置不丢失）；纯文本兜底选默认模型回车=保持（此前 `if not sel` 把「跳过」当「清除」，管道/EOF 场景跑一次 setup 就误删已设默认，清除要显式 clear）。⑥ **死代码** `app/services/task_serial_executor.py` 全仓零引用删除（VENDOR.md 冻结清单同步）；`universal_gpt` 死属性 `self.screenshot/link`、`_chat_completion_create` 尾部不可达 raise、`last_exc` 变量。⑦ **quota 判定前移** `_is_retryable_error` 在 429 重试判定前先识别配额耗尽（insufficient_user_quota / quota exceeded / 预扣费额度失败）——此前命中 429 判定可重试，白等 backoff + 白烧尝试；`_merge_partials` 组循环前检查取消（cancel 后仍在烧配额）；temperature 降级提示 print → logger（stdio 模式 stdout 被吞，用户看不到）。+28 测试（新建 tests/test_gpt_resilience.py 6 项）。**438 passed + ruff F-clean**。

### ✅ 121. 第 5 轮并行扫描（代理 A 13 项 + 代理 B 9 项，commit 84fd38c）

第 5 轮双代理扫描（server/CLI 工具面 + vendored app 层）→ 22 项验证过的候选全部落地：
① **C1 batch 并发门禁**：batch_generate_notes 文档承诺「排队等待」但 worker 毫秒级把任务置 running，第 4 条起逐条被拒——thread-local 旁路（批量语义靠线程池承担，max_entries ≤ 50 封顶），批量后复位。② **C2 diarize_media 临时文件泄漏**：归一化产物写源文件旁，每次调用永久残留 <原名>_16k.wav（小时级数百 MB）——`finally: cleanup_preprocess_files(wav)` 成功/异常都清。③ **C3 cleanup_all 误删 logs/**：MCP 进程持有 mcp_stderr.log 的打开 fd，unlink 后日志写入进入已删除 inode——文件消失、磁盘不回收、无报错直到重启；app 层跳过 logs/ 并记入 kept。④ **C4 export_transcript 失败形状**：失败返回补 `ok: False`（此前 Agent 判读 KeyError）。⑤ **C5 cancel_note 竞态收窄**：运行中任务取消改为只发协作信号返回 CANCELLING，终态由 worker 在阶段边界写——此前直接写盘 CANCELLED 会把 worker 刚写的 SUCCESS 覆盖成「已取消」，而 result.json 已有完整笔记；排队中（future.cancel() 成功）仍由 cancel 收尾写 + pop registry。⑥ **C6 inspect 失败归一**：batch 的 inspect 失败归一为批量形状（total/submitted/errors[]/tasks 统一，此前透传另一形状）。⑦ **C7 validate_url 死分支**：删除 `platform == "unsupported"` 死分支（_detect_platform 永不返回）。⑧ **C9 内存快照弹出**：_write_status 终态落盘成功后 pop `_status_memory`（写盘失败保留——快照是读盘损坏时的唯一回退），防长生命周期 server 无界增长。⑨ **C10 模型下载去重**：download_transcriber_model 尺寸大小写宽容（strip+lower 后校验）+ 进行中去重（is_downloading 命中返回「跳过重复提交」，此前排队重下 ~75MB-1.5GB）。⑩ **C12 delete_model 默认配置**：只清「删的就是默认模型」的 default_model（此前无条件清空，删非默认也把默认抹掉）。⑪ **C13 update_provider 返回 changed** 字段。⑫ **B1 短视频零帧**：video_reader 组不满整批 continue——短视频只有一组残组，产出 0 张网格图静默成功；残组在「还没有任何网格图」时按单组拼接兜底。⑬ **B2 空转写成功欺骗**：whisper 对静音/黑屏返回空——曾当成功缓存，任务 SUCCESS 后 LLM 拿空素材凭空生成笔记；两个分支（直转/预处理）统一 `_ensure_transcript_content` 抛错。⑭ **B3 local cover 目录**：extract_cover 从不 makedirs → ffmpeg 写失败被吞、cover_url 恒空。⑮ **B4 B 站 p 越界**：显式 p 越界静默回退 pages[0]——调用方以为拿到了第 p 集内容实际是第 1 集；改返回 None 走转写路径。⑯ **B5 分 P 缓存 glob 误配**：`{BV}*.mp4` 前缀匹配会把 BV12345_p1.mp4 命中给 BV1234 的查询；精确枚举 `{BV}.mp4` / `{BV}_pN.mp4` 两种形态。⑰ **B6 字幕回退落数据根**：yt-dlp 字幕文件写数据根从不清理；调用方没给 output_dir 时落专属临时目录、finally 清理。⑱ **B7 groq 压缩泄漏**：mkstemp 后 ffmpeg 抛错，调用方拿不到 temp_file 无从删起——compress_audio 内清理后重抛。⑲ **B8 bcut 上传 code 检查**：业务错误（code≠0）直接取 resp["data"] 裸 KeyError——显式 RuntimeError 带业务 message。⑳ **B9 youtube embed id**：url_parser 正则漏 `embed/{id}` 形态。+30 测试（新建 test_bilibili_subtitle / test_bilibili_downloader / test_groq_compress / test_bcut / test_url_parser + 既有文件扩充）。**469 passed + ruff F-clean**。

### ✅ 97. style/format 白名单 enum 化（commit fd9a5e2）

`get_style_format` / `get_format_function` 对未知 style/format 返回空串——Agent 拼错枚举名（schema 是自由字符串）得到**静默无风格笔记**。修复：`generate_note` / `prepare_note_material` / `summarize_note` / `batch_generate_notes` 签名改 `Literal` 白名单（schema 呈现 enum，Claude 端只生成合法值）+ `_check_style_and_format` 入口显式校验（FastMCP 不做运行时参数校验，直接调用/老客户端仍会传非法值；校验放在 provider 解析前——不需要 provider 的错误先报，H6 原则）。默认值路径（setup 配置）不拦（用户自持配置）。+7 契约测试。**293 passed + ruff F-clean + 39 工具**。

### ✅ 93. 插件 userConfig 与全仓 env 面对齐（commit 05cb87a）

全仓 15 个 `VIDEONOTE_*` env：plugin.json userConfig 此前覆盖 10 个，缺 `VIDEONOTE_MAX_WORKERS`（并发任务上限）与 `VIDEONOTE_STDERR_LOG_MAX_MB`（日志轮转阈值）——两者都是用户会想调的行为配置。补入 userConfig（number 类型，默认 3 / 50）+ mcpServers.env 引用。剩余 3 个（CONFIG_DIR/DATA_DIR/MODEL_DIR）是内部路径 env，暴露会破坏安装，刻意不提供。docs/02、docs/04 的 env 表已含这两项，无需改。

**G5**：`_exit_summary` 在记录进行中任务数的同时 set 所有 cancel_event——解释器退出前 join 非 daemon 线程池线程时，任务在阶段边界检查取消尽快收敛，缩短 ffmpeg/whisper 子进程残留窗口（纯增益，任务已能处理取消）。**#34 YouTube cookie**：youtube_downloader 读 setup ③ 填的 youtube cookie → Netscape 临时文件（0600、`__del__` 清理）→ `ydl_opts['cookiefile']`，download/download_video 两路径注入，匿名时照常降级（高清/年龄限制视频受益）。补 4 项测试。



### ✅ 122. 第 6 轮双代理扫描 13 项（2026-08-17 实施，commit 59e01a4）

代理 A（server/CLI 工具面 8 项）+ 代理 B（app 层 5 项）。全部经行号核对与关键项抽查验证。修复记录：

- **A2 ✅ 数据损坏** export_transcript 的 json 格式丢 speaker 并覆盖转写缓存：双修复——① json 渲染 `_seg_to_dict` 保留 speaker 字段（无则不出该键）；② 导出文件名统一 `transcript.export.json`（exporter `_FORMAT_FILENAME`），note.py 转写缓存规范来源 `gen/transcript.json` 不再被自动导出/工具缺省 out_dir 覆盖（含 raw 等完整字段保留）。
- **B1 ✅ 回归** #119「媒体缓存 miss 置 None」缺存在性检查：本地文件 + 跨任务转写缓存命中时真实存在路径被无条件置 None——置 None 前加 `Path(audio.file_path).is_file()` 检查，存在保留、悬空才清。
- **B2 ✅ 拿错集** download_video 分 P 缓存跨 P 通配：`extract_bilibili_p_number` 解析 p 后精确匹配 `{BV}_p{p}.mp4`；无 p → `{BV}.mp4` 或 `{BV}_p1.mp4`（仍无通配，绝不误配）。
- **A1 ✅ 静默成功** export_transcript 全部格式落盘失败仍 ok:True——工具层 `ok = not errors`，与 CLI 对 not written 显式 exit 1 对齐。
- **B3 ✅ cookie 静默失效** inspect_video 的 cookiefile 绑死 `.example.com`（cookie 塞 generic 字段 yt-dlp 永不发送）——改 `http_headers.Cookie` 直接注入（与 generic_downloader 同口径），不再写 Netscape 临时文件。
- **A7 ✅ TOCTOU** download_transcriber_model 去重：`mark_downloading` 前移到 submit 前（提交即占位），worker 内不再 mark——连续两次提交第二次立刻命中「下载中」。
- **A3 ✅ 状态门禁** `_load_task_transcript` 加 SUCCESS 门禁：非 SUCCESS（FAILED/运行中）返回 None，get_task_transcript 报「尚未成功或已清理」。
- **A5 ✅ 标记残留** summarize_note 剥离 screenshot/link format（无视频文件/video_id 无法后处理，prompt 不再要求输出标记）+ `strip_media_markers` 兜底清洗残留字面标记。
- **B5 ✅ 尾部星号** note_helper/screenshot_marker 正则消费闭合星号：`*Content-[04:16]*` 替换后不再残留尾部 `*`。
- **A4 ✅ 空列表重解释** exporter `if formats is None: formats = ["srt"]`——显式 `formats=[]` 零导出。
- **A8 ✅ 误导措辞** cancel_note 终态措辞区分：FAILED →「任务已失败，无需取消」；CANCELLED →「任务已取消」；SUCCESS → 原「任务已完成」。
- **A6 ✅ 入口不一致** setup 向导清理路径加「运行中任务」守卫：CLI 独立进程读磁盘 `status.json` 判终态（SUCCESS/FAILED/CANCELLED 外均视为运行中），cleanup-one/all 均有守卫提示 + 二次确认（交互式允许用户强清，区别于 MCP 硬拒绝）；顺带 `_press_any_key` 捕获 OSError（非交互 stdin 不崩）。
- **B4 ✅ 死代码** ytdlp_retry：删不可达 `raise last_exc`（循环内每分支必 return/raise），attempts<=0 显式 ValueError（此前 raise None 误导性 TypeError）。

+31 测试（test_export / test_status_tools / test_server_contracts / test_bilibili_downloader / test_inspect_video / test_downloader_integration / test_material_mode / test_cli）。**500 passed + ruff F-clean**。

## 建议落地顺序

**Wave A — 修对 + 可回归（2026-08-17 已落地）**

1. ✅ `_run_step_task` 写 SUCCESS；`IMAGE_OUTPUT_DIR`；`file://`；`NOT_FOUND`；步骤任务入索引；统一 `_guard_concurrency`
2. ✅ CI 跑 pytest + server 契约测试
3. ✅ 重写 `docs/02` + `VENDOR.md`；对齐 31 工具；`__version__` = 0.1.4
4. ✅ MCP 拒绝 key/cookie/hf_token；`generate_note` 可省略 `provider_id`；`diarize_media` 读 `app_config.hf_token`
5. ✅ 停掉 xiaoyuzhou 导入时 HTTP；cookie tempfile unlink + 0600；抖音别 print Cookie

**Wave B — Agent 更好用（2026-08-17 部分落地）**

6. ✅ 工具合并 / Resource 读 transcript（6f 已落地：`videonote://task/{id}/transcript` MCP Resource + `_load_task_transcript`，commit fa2c89c/3dca7b1）；✅ `wait_for_note` 已废弃（不再 sleep）；✅ `list_models` 统一 `{id, name}[]`
7. ✅ `inspect_video` 拆分 P / 播放列表为可提交 url（Agent 按单集跑）；✅ 内容寻址转写缓存（`note_cache`，按 `platform:video_id` + 引擎分键，同视频不重下/重转写）
8. ⏸ progress notification —— **已判定架构不可行**（FastMCP 3.4.5 stdio 下后台线程无 session 可用，见 #21 结论）；✅ `health_check` 含 version / 队列 / `skill_refresh`
9. ✅ SKILL 默认全自动、少问；handoff 仅 `handoff:true`；`agent_direct` 只当编排选择（server 不读）
10. ✅ `export_transcript` 回传 `errors`；✅ transcript 默认前 50 段；✅ `merge_audio` 默认写数据目录 `note_results/merged`（完整路径沙箱仍待做）

**小项批（2026-08-17，7 项全部完成）**：cleanup_all 清 DB 索引（#38）；小宇宙死代码删除（#11）；quality 可配置 + yt-dlp 重试（#29/#36）；load_dotenv 守卫（#35）；仓库垃圾清理（#41）；progress 判定不可行 + stage/elapsed 替代（#21）；错误形状统一 ok（#27）。

**Wave C — 产品能力**

11. 播放列表/系列、~~小宇宙（真接入或删）~~ → **已删**（2026-08-17，generic 覆盖）、diarization 进 `generate()`、token 级 chunk、视频网格封顶
12. ~~接上或删除 quality~~ → **已接上**（2026-08-17，仅 B 站转码场景）；YouTube cookie；generic Netscape 域名
13. ✅ keyring（→ 机器级 Fernet，6g 已落地）、✅ vendor 边界（6h）、✅ 删死代码（2026-08-17：`app/gpt/` 的 openai_gpt / deepseek_gpt / qwen_gpt / utils / tools 全仓零引用——gpt_factory 走 OpenAICompatibleProvider 直连，上游直连类移除；VENDOR.md 分叉清单已同步；AST 全仓死模块普查无其余残留）

**下一轮（Wave D，2026-08-17 第二轮扫描产出，按优先级）**

1. **安全快修（P1，半天级）**：46 删 Cookie print 全层清 print；47 tiktok→generic；48 全部 requests timeout；49 下载器惰性实例化（#10 修复实际未生效，优先级最高之一）
2. **发布路径（P1）**：50 Dockerfile/wheel 修 events 引用；51 release 测试门禁 + tag/`__version__` 三处核对；67 收紧 fastmcp 版本
3. **状态机（P2）**：52 默认供应商 key 校验；53 cancel_note 竞态；54 result.json 原子写 + 顺序统一；55 非法 segment_range 报错；56 app_config 原子化；57 preflight 队列检查
4. **可靠性（P2）**：58 ffmpeg timeout + 失败反馈；59/60 note_cache 媒体上限 + Windows 键编码；61 时间戳 `\d+`；62 下载器细节组；63 补 2 处 retry；68 kuaishou 双调
5. **分发/门禁（P2/P3）**：64 install.sh 回退修复；65 plugin.json userConfig 补 5 env + funasr；66 测试隔离；70 CI 工具数断言 / env 键集 / wheel 内容三项一致性检查；69 文档代差收尾；71-76 按顺手程度清

---

## 关键文件

| 区域 | 路径 |
|---|---|
| 工具面 | `videonote_mcp/server.py` |
| 环境 | `videonote_mcp/config.py` |
| 编排 | `app/services/note.py`、`app/services/pipeline.py` |
| 截图 bug | `app/services/note.py:57`、`app/utils/video_helper.py` |
| 下载 | `app/downloaders/*.py`、`app/services/constant.py` |
| 插件/Skill | `.claude-plugin/plugin.json`、`skills/videonote/SKILL.md` |
| CI | `.github/workflows/ci.yml`、`tests/`、`conftest.py` |
| 过期文档 | `docs/02-架构设计.md`、`VENDOR.md`、`docs/00-文档索引.md` |

可复用：`_coerce_local_path`、`_guard_concurrency`、`_validate_task_id`、`provider_probe`、`pipeline.detect_platform` / `handoff_result`、CLI `providers test`、`task_manifest` 的 `_safe_resolve`。

---

## 验证（落地某一波时）

- `uv run pytest` 全绿；新增契约测试覆盖 file://、未知 task、步骤任务 SUCCESS、省略 provider_id
- `tools/list` 数量与文档一致（现在 32，含 `inspect_video`）
- 本地：`generate_note(file:///...%20....mp4)` 不再误报；截图落在数据目录；错 task_id 返回 NOT_FOUND
- `transcribe_media` 轮询能拿到 `SUCCESS` + result
- `health_check` 含 version；敏感工具不再要求把 key 当参数

### ✅ 123. 第 7 轮双代理扫描候选（2026-08-17 登记；commit 87dee10，实施完成 532 passed）

代理 A（工具面/CLI/分发 10 项）+ 代理 B（app 流水线 11 项），全部经行号核对。按严重度排序：

- **A1 ⚠ 数据损坏** `cleanup_all(include_models=True)` 与进行中模型下载竞态：运行守卫只查 `_task_futures`，模型下载在独立 `_dl_pool` + `dl_state`——删 models/ 时下载线程继续写残缺目录、`mark_done` 后 health_check 报「已下载」实际损坏。修复：include_models=True 时检查 `dl_state` 是否有下载中 key，有则拒绝/提示（CLI 向导同查）。
- **B7 ⚠ 数据损坏** 便携笔记同名并发 TOCTOU（note.py `_note_dir_name` `if not target.exists()` + 写路径 `rmtree assets + copytree`）：两个同 notes_dir+同标题任务选同一目录，互相清掉对方 Assets/。修复：`mkdir(exist_ok=False)` 原子占用，冲突回退 `-{task_id}` 后缀。
- **A3 ⚠ 误导状态** 步骤任务（transcribe/extract_frames）cancel 无效但返回 CANCELLING：`_step_*` 只在开头 `_check_cancel`，pipeline.transcribe/extract 无 cancel_event；任务跑完报 SUCCESS，与返回状态矛盾。修复：`_run_step_task` 在 step_fn 返回后复查 cancel → 命中写 CANCELLED。
- **B1 ⚠ 重复调用** 无字幕视频字幕 API 调 2 次（generate L269-285 + _get_transcript L799-819 各 fetch 一次）。修复：generate 已试过字幕时 `skip_subtitle` 透传给 _get_transcript 直接走 ASR。
- **B2 ⚠ 数据损坏** `lookup_media` 可把 `.tmp` 半成品当音频返回（promote 的 tmp→replace 之间进程被杀，遗留 <name>.tmp 被 iterdir 选中）。修复：过滤 `*.tmp` 与非常见媒体后缀。
- **B5 ⚠ 安全/泄漏** `constant.py _created` 无界增长：每个任务 append 下载器实例、从不 prune，强引用令 `__del__` 不触发 → bilibili/youtube 明文 SESSDATA cookie 文件滞留 /tmp 至进程退出。修复：任务完成后 pop（或 `_cleanup_cookie_file` 时移除）。
- **B10 真实 bug** douyin `output_path % {...}` 格式化：output_dir 含字面 `%`（如 /tmp/100%off/）→ ValueError 下载失败（bilibili/youtube 用 yt-dlp 模板不受影响）。修复：`Path(output_dir) / f"{aweme_id}.mp4"`。
- **A7 孤儿行** `add_model` 不校验供应商存在（无 FK，SQLite 弱类型静默写入孤儿行）；`Model.provider_id` 是 Integer 列存字符串 id（换真 DB 即炸）。修复：add_model 先 `get_provider_by_id` 校验（类型迁移标注暂缓）。
- **A6 行为不一致** CLI `export` 缺省格式 `["srt","vtt","json"]` vs MCP `export_transcript` 缺省 `["srt"]`（同为「缺省取 setup 默认」）。修复：统一收敛 `["srt"]`。
- **A8 误导状态** `get_task_transcript` 非法 segment_range 硬编码 `status:"UNKNOWN"`（SUCCESS 任务被误判）。修复：复用 `_transcript_unavailable_reason` 读真实状态。
- **B4 性能** 本地文件全量 sha256 每任务 2-3 次（lookup_transcript/lookup_media/promote_transcript 各自 derive_video_id）。修复：每任务 memoize 一次。
- **B8 静默吞错** `ProviderService.update_provider` 全异常 → print(去 stdout 被 stdio 吞) + 返回 None，DB 锁等真实原因不可见。修复：logger.error + 显式错误返回。
- **B6 非原子写** `WhisperModelRegistry._write_custom` 直接 write_text（中断 → config/whisper_models.json 截断，自定义模型静默消失仅 warning）。修复：用 `write_json_atomic`。
- **B9 悬空路径** kuaishou cached-mp3 fast path 返回未校验的 video_path（有 mp3 无 mp4 时 download_video 拿不存在路径 → VideoReader 炸 FAILED）。修复：mp3 存在时校验 mp4，缺失重下。
- **B11 泄漏** `BcutTranscriber` requests.Session 从不 close（每任务新实例，连接池泄漏）。修复：`__del__`/close。
- **A9 内存增长** `_status_memory` 终态快照只在写盘成功后才 pop——持续磁盘故障时无界积累。修复：数量上限/TTL。
- **A10 注释过度承诺** `config.set_app_config` 注释称「双进程不丢更新」实为 `threading.Lock`（仅单进程）。修复：改跨进程文件锁或修正注释。
- **A4/A5 文案漂移** summarize_note docstring 宣传 screenshot/link（实际剥离）；cleanup_all/CLI/SKILL 声称清空 logs/（实际保留，fd inode 陷阱）。修复：统一文案。
- **A2 大改暂缓** merge_audio 唯一同步长跑工具（ffmpeg 1800s/步，不可取消，占 handler 线程）。修复方向：改 step task 或加预检——本轮不实施。
- **B3 大改暂缓** RequestChunker 每候选重建整条消息 O(n²)（长视频转写前 CPU 卡顿数十秒）。修复方向：增量字节/token 记账或段数封顶——本轮不实施。

### ✅ 124. 第 8 轮双代理扫描候选（2026-08-17 登记；commit 19ac155）

代理 A（工具面/server/cli/config 12 项）+ 代理 B（app 流水线 19 项），全部经行号核对。按严重度排序：

- **A1 ⚠ 配置丢失** 转写向导默认值元组漏 `"funasr"`（cli.py:446）：当前引擎为 funasr 时回车确认会把引擎无意识切回 fast-whisper（choices 里明明有 funasr，与 `_TRANSCRIBER_ENGINES` 同源可修）。修复：复用 `_TRANSCRIBER_ENGINES`。
- **A2 裸 traceback** `providers add` 重名供应商抛 ValueError 无捕获（向导有、CLI 无）。修复：try/except → 打印原因 + exit 1（与 `providers set` 同风格）。
- **A3 重复行** `add_model` 无去重（向导 `_set_default_model` 写前查重、MCP 直插）→ models 表重复行 + list_models 显示重复。修复：`get_model_by_provider_and_name` 查重幂等返回。
- **A4 字符集误报** CLI `export` 对非列表 `default_export_formats` 无守卫：字符串 `"srt"` 被 `set()` 拆成字符报「未知导出格式 'r','s','t'」（MCP 侧 #107 有 `_resolve_default_export_formats` 守卫，CLI 出口漏网）。修复：CLI 复用同源守卫（非列表 → warning + 回退 `["srt"]`）。
- **A5 垃圾值语义** `config.env_bool` 对垃圾值（`"maybe"`）返回 False 而非 default（`env_int` 同族回退 default）——将来 default=True 的调用点会被手滑值静默翻反。修复：解析失败回退 default。
- **A6 布尔解析不一致** `transcriber_config_manager.get_config` 的 env 布尔解析与 config.env_bool 规则不同（大小写敏感、不接受 yes/on）+ `bool("false")` 为 True（手动编辑 JSON 字符串值 → 预处理/分离被误开）。修复：局部同源 helper 规整。
- **A7 跨线程竞态** 模型下载去重 check-then-mark 不原子（`is_downloading` + `mark_downloading` 两步，FastMCP 每请求一线程，两个并发请求都通过检查各起一个 worker 重下整个模型）。修复：`model_download_state` 加模块级锁，新增原子 `try_mark(key)`。
- **A8 零文件假成功** `export_transcript(formats=[])` 显式空列表 → `ok:true + formats:{}`（#122 A1「失败必须 ok:false」口径在零格式分支漏网；CLI 版对 not written 显式 exit 报错）。修复：空列表显式返回 ok:false。
- **A9 不可操作错误** `summarize_note` 外来 segments 含未知键（`words`/`id`）→ `TranscriptSegment(**s)` TypeError → 后台 FAILED 信息费解。修复：pipeline.summarize_material 段清洗——dict 过滤已知键、缺 start/end/text 跳过留痕。
- **A10 成功假象** `get_task_status` SUCCESS 但 result.json 损坏 → `result:null` 无提示（status.json 有 #118 内存快照回退，result.json 对偶路径无兜底）。修复：读取失败加 `result_error` 字段。
- **A11 形状不齐** `batch_generate_notes` 单集退化分支缺 `truncated`/`remaining`（多集有），单集失败还缺 `platform`/`kind`（inspect 失败分支有）→ Agent 按多集形状取值 KeyError。修复：补齐同键。
- **A12 误导预检** `preflight` 强制检查供应商 key/模型，但 `prepare_note_material` 明确不需要 LLM——material-only 流程 preflight 得到「无已填 key 的供应商」误导结论。修复：加 `need_provider` 参数（material 流程传 False 跳过）。
- **B1 ⚠ 损坏缓存** 快手 ffmpeg 转 mp3 失败残留半成品（`-y` 落盘后超时）→ 下次 `os.path.exists` 命中把损坏 mp3 当成功产物（转写空/乱码被误导成「转写结果为空」）。修复：转换前 unlink + exists 分支校验非零字节。
- **B2 ⚠ 成功变失败** `universal_gpt._save_checkpoint` 无异常保护：checkpoint 只是恢复辅助，写失败（磁盘满）却在 summarize/merge 循环 raise → 任务 FAILED；且在 except 处理器里保存会替换掉原始 LLM 异常。修复：try/except + warning。
- **B3 重试盲区** `ytdlp_retry` 关键词漏 DNS 类错误（`urlopen error [Errno -2] Name or service not known` 不含任何现有关键词）→ DNS 抖动从不重试。修复：补 `"urlopen"`/`"name or service"`/`"temporary failure in name resolution"`。
- **B4 错误失真** 抖音 `raise ValueError("请求失败:", e)` 元组参数（str() 输出 `('请求失败:', ...)` 且无 `from e` 丢链）+ `download()` 裸 `except: raise e` 截断 traceback。修复：f-string + `from e`，删裸 re-raise。
- **B5 cookie 失效** 抖音音频流下载用类级 `DouyinConfig.HEADERS`（Cookie 恒 None）——用户配的 cookie 对音频请求不生效。修复：统一 `self.headers_config`。
- **B6 单位错** 抖音 `video.duration` 是毫秒按秒返回（比 bilibili/youtube 偏大 1000 倍，展示/时长统计错乱）。修复：/1000 归一（注释标注口径）。
- **B7 title 不一致** 快手 skip_download 分支清洗截断标题、正常分支原样透传（caption 带换行/空格进 DB 与 prompt）。修复：统一清洗。
- **B8 连接泄漏** 快手 `_download_mp4` 裸 `requests.get` 不关响应（每次下载泄漏一个连接）+ 非 200 裸 Exception。修复：`with` + `raise_for_status`。
- **B9 死代码** 快手 `get_video_details` 的 `raise_for_status()` 在 200 分支内永不触发——非 200 静默返回 None 掩盖真实状态码。修复：提位到 if 外。
- **B10 天书错误** bcut 分片上传 `resp.headers.get("Etag", "").strip('"')`——header 存在但值为 None（异常网关响应）→ AttributeError。修复：`(get("Etag") or "")`。
- **B11 封面互踩** `extract_cover` 输出 `{base_name}_cover.jpg` 落在共享 covers 目录：两个不同目录同名视频的封面互相覆盖，先前笔记已嵌入的 file:// 引用静默变成另一视频的封面。修复：文件名加输入路径短 hash。
- **B12 毁整批** `video_reader._extract_single_frame` 只捕 CalledProcessError——单帧 ffmpeg 超时（120s）把整个抽帧任务打成「视频处理失败」，已抽出的几百帧全丢。修复：`except TimeoutExpired: return None` 同处理。
- **B13 非原子写** note.py 8 处缓存/产物 `write_text` 非原子（转写缓存截断 → 下次任务重下+重转写，小时级成本；note.md 截断 → 半残不可恢复）。修复：复用 `write_json_atomic` + 新增 `write_text_atomic`。
- **B14 limit 不生效于 SQL** `list_tasks` 工具先全表 `all()` 再 Python 切片——video_tasks 只增不减，长跑用户每次 list 全量加载/序列化。修复：DAO 加 limit/offset 下推 SQL，工具透传。
- **B15 并发丢更新** `CookieConfigManager.set` 读-改-写竞态（两个并发 set 不同平台互相覆盖）。修复：类级写锁。
- **B16 日志双轨** `get_logger` 系 propagate=False 挂 app.log；stdlib 系（note/pipeline/downloader 等）只到 root（server basicConfig 只挂 stderr）→ app.log 缺关键模块 WARNING、时间线对不上。修复：文件 handler 挂 root（一次），root 级别不动防第三方 INFO 泛滥。
- **B17 弃用 API** 抖音 `datetime.utcnow()`（3.12+ DeprecationWarning，msToken 签名路径每次调用刷警告）。修复：timezone-aware。
- **B18 误删模型** whisper 加载失败一律 `_purge_cache` 整目录 rmtree——OOM/网络抖动/磁盘瞬时满也触发，下次重下数 GB。修复：仅缓存类异常（OSError 等）purge，其余告警重试一次。
- **B19 帧目录互踩** `pipeline.extract_frames` 默认 save_dir 按文件名 stem 命名：不同目录同名视频并发抽帧互相覆盖 + 跨任务累积。修复：默认带 uuid 后缀。
- **核对后排除 1 项**：merge concat list 单引号转义——list 文件只含自家生成的 `tmpdir/part_NNN.wav` 路径（mkdtemp 随机后缀），用户路径不进 concat list，不成立。

### ✅ 125. 第 9 轮双代理扫描候选（2026-08-17 登记；commit dfddb9b）

代理 A（工具面/server/cli/config/skill 11 项）+ 代理 B（app 流水线 16 项），全部经行号核对。按严重度排序：

- **A1 ⚠ 配置抹除** `get_app_config` 损坏 JSON 静默返 `{}`（config.py:216-225），`set_app_config` 读 `{}` 写回会把 default_model/notes_dir 等全部抹掉——#106 修过 cookie/transcriber/proxy 三管理器，app_config 自己留了口子。修复：warning + `.corrupt` 备份 + 空配置（与 json_store.read_json 同口径，config 层不跨依赖）。
- **A2 ⚠ 假成功** CLI `export` 把 `_errors` 当导出格式计数：全部落盘失败时 `written={" _errors":...}` 仍 truthy，「✓ 已导出 N 个格式」误导（server.py:2548 同口径已剥离，CLI 漏网）。修复：CLI 先 `pop("_errors")` 再判空。
- **A3 静默无结果** `get_task_status` SUCCESS 但 result.json 缺失 → `result:null` 无提示（note.py:458 先写 SUCCESS、server.py:422 结果落盘在其后，窗口真实存在）。修复：缺失时加 `result_pending: true`。
- **A4 Agent 引导冲突** `inspect_video` docstring 仍教「多集用 subagent」，与 #110 的 batch_generate_notes 口径冲突（工具描述即 Agent 可见）。修复：改「多集全出笔记 → 一条 batch」。
- **A5 天书错误** 6 处显式数值参数裸 `int()`（generate_note 835/842、prepare 939/946、list_tasks、batch max_entries）——`"abc"` 报 invalid literal；extract_frames 已是 warning 回退。修复：统一 `_coerce_int`。
- **A6 docstring 漏 ok** `fetch_subtitles` 成功形状缺 `{ok: true}`（实现已是 #27 口径）。修复：docstring 对齐。
- **A7 设得了下不了** `download_transcriber_model` 白名单 6 档 vs `set_transcriber` 按 #108 放行自定义名/HF repo_id——自定义尺寸 preflight 报未下载且无预下载路径。修复：下载校验改 `resolve_whisper_model` 同源。
- **A8 默认值漂移** `transcriber_config_manager` 兜底 `tiny` + docstring 过期，与 #34 的 `small` 默认矛盾（MCP/CLI 因 env 先设才掩盖）。修复：兜底与 docstring 同步 small。
- **A9 SKILL 文档缺参** tools.md `preflight` 签名缺 `need_provider`（#124 A12 落地后未同步）。修复：补参数与 material 用法。
- **A10 绕过过滤工具** `/videonote-setup` 第 4 步教 Read 原始 `app_config.json`，绕过专用 `read_app_config`（过滤敏感字段）。修复：改用工具。
- **A11 形状不对称** `cleanup_task_files` 成功路径无 `ok` 键（task_manifest.py:320-327），守卫路径有 `{ok:false}`——Agent 统一按 ok 判读拿 None。修复：成功路径补 `ok: true`。
- **B1 ⚠ 缓存永不命中** 抖音 v.douyin.com 短链 `url_parser` 只匹配 `/video/(\d+)` → derive_video_id 返回 None → lookup 键 `douyin:None`，promote 用下载器 aweme_id——同一视频每次都重下重转写（App 分享默认短链）。修复：解析失败回退解短链，或 promote/lookup 共用 id 来源。
- **B2 白做转码** kuaishou `download_video` 走完整 `download()` 必然转一次 mp3，need_video 路径之后从 mp4 再提音频——每次任务多一次全量转码 + 双份磁盘。修复：download_video 直接 `_download_mp4`。
- **B3 覆盖终态** note.py:597-599 `_update_status` 原子写失败回退 `open('w')` 写 "Error writing status..."——把上次已落盘的 SUCCESS 终态永久抹掉，重启后快照失效只能显示损坏。修复：回退分支不截断原文件（tmp+replace 失败保留原文）。
- **B4 目录无界增长** local 封面 `{data}/covers/` + `{data}/static/cover/` 两处，cleanup_all 只清 screenshots——每个 local 任务 2 个文件永久累积。修复：cleanup_all 同口径清理。
- **B5 零测试覆盖** KuaishouTranscriber / YouTubeSubtitleFetcher / BilibiliDmPatch / screenshot_marker 无任何用例（生产转写引擎 + 笔记产物正确性）。修复：至少补 kuaishou_transcriber 失败/空结果路径 + screenshot_marker 标记替换。
- **B6 双 HEAD** b23.tv 每次 lookup `extract_video_id` + `extract_bilibili_p_number` 各解一次短链（各 timeout 5-10s），慢网 30s。修复：p 参数直接读原 URL `?p=`（短链不含 p）。
- **B7 误重试** ytdlp_retry `except OSError` 含 FileNotFoundError（OSError 子类）——yt-dlp 缺失/路径不存在指数退避白等 3 次。修复：显式排除 FileNotFoundError。
- **B8 空 choices 裸崩** universal_gpt:340/456 `response.choices[0]` 空列表 IndexError（#73 只修 content=None）。修复：先判空抛明确错误。
- **B9 旧格式兼容** cookie_manager `data.get(platform, {}).get("cookie")` 对旧格式字符串值 AttributeError（json_store 容错只覆盖文件层）。修复：isinstance 兼容。
- **B10 O(n²) 拼接** 5 个转写器 `full_text += text + " "`（whisper/groq/bcut/kuaishou/mlx），万段级累计拷贝（pipeline:309 已用 join）。修复：统一 `" ".join`。
- **B11 丢链** kuaishou mp3 转换失败裸 `raise Exception`（kuaishou_downloader:102-103）。修复：`from e` + 退出码。
- **B12 N+1 清理** cleanup_all `list_tasks()` 全量 + 逐条 `delete_task`（每条 SQL）。修复：DAO 批量删除。
- **B13 <1s 静默 0 帧** video_reader:107 `range(0, int(duration), interval)`——duration<1s 时 timestamps 空，run() 静默返回 []。修复：空时至少取 t=0 或显式 warning。
- **B14 每次重载** diarization.py:67 `Pipeline.from_pretrained` 每次调用重建（token 校验 + 反序列化数秒）。修复：模块级单例缓存，首次失败不缓存。
- **B15 内存峰值** bcut `_load_file` 整文件 read + 分片切片复制——大音频 2×+ 内存。修复：文件对象/分段读。
- **B16 连接泄漏** youtube_subtitle 代理 Session 从不关闭（bcut #123 B11 同款已修，此处漏网）。修复：`__del__`/模块级复用。
- **核对后排除**：pipeline.extract_frames `.exists()` 目录穿透（server 工具层 is_file 已 4 处保护）；抖音多 P 命名一致性（交叉验证一致）。

---

### ✅ 126. 第 10 轮双代理扫描候选（2026-08-17 登记；commit 413e7d5）

代理 A（工具面 9 项）+ 代理 B（app 流水线 9 项），全部经行号核对。**两代理一致判定：无 P0**——前 9 轮已把正确性/安全/数据丢失面收割干净；残余画像为「半途修复收尾」（门禁只盖一半、close 没人调、异常被静默吞）+ 长尾边界。按严重度排序：

- **C1 ⚠ 门禁不对称** `export_transcript`（server.py:2533-2560）读转写不看 status.json，任务 FAILED/运行中也能导出 `ok:true`；`get_task_transcript`（#122 A3）同任务却返回 ok:false——两个读转写工具给 Agent 相反结论（CLI export 同病）。修复：非 SUCCESS 返回 `{ok:false}`。
- **C2 ⚠ 形状不对称** `cleanup_all` 成功路径无 `ok:true`（#125 A11 只给 cleanup_note 补了）。修复：成功返回补 `ok:true`。
- **B1 ⚠ 并发互毁** 预处理临时产物固定命名 `<name>_16k.wav` + 任务结束无条件清理——两个并发任务处理同一本地文件时互相覆盖/删除对方正在转写的 wav（pipeline.py:190/265/301-306）。修复：临时产物落独立目录，清理只删自己的。
- **B3 版本守卫缺失** dm_img patch 无条件把 `fatal` kwarg 透传给 `original`——旧版 yt-dlp 签名无 fatal 时所有 B 站 playurl 下载全挂 TypeError。修复：TypeError 时降级无 fatal 重调。
- **C3 _coerce_int 漏网** `fetch_comments` 的 `limit` 仍裸 `int()`（#125 C5 扫描漏了此点），垃圾值报天书错误。修复：`_coerce_int`。
- **C4 mlx 尺寸后验** `download_transcriber_model` mlx 分支把 `MLX_MODEL_MAP.get(size)` 放后台，非法尺寸返回 started:true 后静默失败（CLI 同场景同步报错）。修复：提交前校验。
- **C5 数值参数漏网** `set_transcriber` 的 `diarization_speakers` 直传底层 `int()`，垃圾值工具内裸 ValueError。修复：`_coerce_int` 回退 None。
- **C6 解密边界漏网** `get_app_config` 解密循环遇非字符串敏感值（手改 JSON）整包 `return {}`——下一次 set 抹掉其余配置（#125 C1 只兜 JSON 损坏）。修复：per-key try/except，失败只 drop 该 key。
- **C7 文案漂移** CLI `transcriber set --size` help 漏 `large-v3-turbo`；切自定义尺寸后的 download 提示必被 argparse 拒。修复：help 补全 + 提示对齐。
- **C8 file:// 不一致** CLI `export --out-dir` 不规整 file:// URI（MCP 侧 #107 已修），会建字面 `file:` 目录。修复：CLI 同款解码。
- **C9 validate 语义矛盾** `validate_url` 对不存在本地路径返回 supported:true，generate_note 随后拒绝——SKILL 流程多一轮无效往返。修复：local 分支加存在性检查。
- **B2 close 无人调** #125 B16 的 `YouTubeSubtitleFetcher.close()` 在唯一生产调用路径（youtube_downloader.py:175-177）上直接 return，无 finally close。修复：try/finally。
- **B4 短链无 memo** b23.tv/v.douyin.com 解析无进程内缓存，单任务 3-5 次 HEAD（慢网 3-5 个 10s 窗口）。修复：`lru_cache`。
- **B5 None 裸崩** bcut/kuaishou 段文本 None 时 `.strip()` AttributeError；bcut per_size=0 上传空块。修复：`(u.get(...) or "")` + per_size/clips 守卫。
- **B6 文案不一致** `_first_choice_content` 两处空内容错误信息不一（merge 带 finish_reason，summarize 不带）。修复：同口径。
- **B7 异常静默吞** cleanup_all 的 `delete_all_tasks` 失败 `except: pass` 零痕迹（DAO 契约是「抛给调用方显式处理」）。修复：warning + `index_error` 字段。
- **B8 日志 TypeError** `insert_video_task` commit 后 `title[:40]` 对 None 崩 TypeError，行已入库却记「插入失败」（note.py:373 真会传 None）。修复：`(title or "")[:40]`。
- **B9 无锁 RMW** `TranscriberConfigManager.update_config` read-modify-write 无锁（cookie_manager #124 B15 同款已修，此处漏网）。修复：class-level RLock。

### ✅ 127. 第 11 轮双代理扫描候选（2026-08-17 登记；commit 3cced7c）

代理 A（工具面 8 项）+ 代理 B（app 流水线 10 项），全部经行号核对。**两代理一致判定：无 P0**——第 10 轮已把门禁/并发互毁面收割干净；残余画像仍为「半途修复收口未闭环」的漏网兄弟（step 入索引了 note 没入、add_model 校验了 delete_model 没校验、bcut 改分片读了 kuaishou 没改）+ 长尾边界。按严重度排序：

#### A 组（工具面 8 项）

- **A1 ⚠ 索引时序漏网** note/material 任务提交（server.py:872/971）只 `_write_status(PENDING)` 不 `insert_video_task`——note.py:369/445 仅 SUCCESS 才 `_save_metadata` 入库；运行期每次 `_write_status`（约 6-7 次）都触发 dao:66「不在全局索引」warning 刷屏，且 **FAILED 任务永不入库** → list_tasks 永久缺失、server 重启后孤儿目录。另 `_submit_step_task`（server.py:575-576）顺序反了（先写状态再入索引，首写仍刷一条）。修复：提交时先 `insert_video_task(PENDING)`（`insert_video_task` 幂等，SUCCESS 再更新 title/status 安全）再 `_write_status`。
- **A2 校验漏网** `delete_model`（server.py:1812-1815）对不存在供应商报「模型不存在」——provider_id 拼错时误导排查方向；`add_model`（:1775-1778）已有先 get_provider_by_id 的正确先例。修复：先校验供应商。
- **A3 来源优先级反了** `export_transcript`（server.py:2598-2612）先读 result.json 再退 gen/transcript.json，与 CLI（cli.py:1569-1584）和 `_load_task_transcript`（server.py:1167-1178）相反——#122 A2 声明 gen 为规范来源但 server 出口漏改，两文件不一致时两个读转写工具给不同内容。修复：gen 优先 result 兜底。
- **A4 死参数误导** `add_provider` 的 `type` 参数（server.py:1690）任何值都被 provider.py:81-84 静默改写成 custom——Agent 以为建了 typed provider 实际是 custom，schema 上出现永不生效参数。修复：docstring/CLI help 显式标注「type 恒为 custom」。
- **A5 失败形状不对称** `validate_url` 本地缺失分支（server.py:2184-2186）带 `platform:"local"`，ValueError 分支（:2197-2198）不带 platform——Agent 统一按 `r["platform"]` 判读时 KeyError。修复：两失败分支都带 platform。
- **A6 分页边界** `list_tasks(limit=0)`（server.py:1357）被钳成 1——显式「取 0 条」返回 1 条，误导「没有任务」判断。修复：limit==0 返回空列表。
- **A7 形状漏网** `get_task_files`（server.py:1373）透传 `list_task_files` 返回无 `ok` 键——cleanup/export/get_task_transcript 均已带 ok（#121 C4 同类坑），唯独漏此。修复：成功路径补 `ok: true`。
- **A8 同字段不同值** `get_task_transcript` 切片路径（server.py:1253-1258）`full_text` 用 `"\n".join` 重拼，全量路径用缓存 full_text（空格分隔）——同一字段两分支不同分隔符/字节数，Agent 对比字数时失真。修复：切片统一空格 join。

#### B 组（app 流水线 10 项）

- **B1 ⚠ device 漏兜底** `FunASRTranscriber` 收 `device="cuda"` 无 CUDA 探测（funasr_transcriber.py:53-60）——transcriber_provider.py:108 默认 device="cuda"，note.py:499/pipeline.py:127 不传 → Mac 全系/无 N 卡 Linux 构造 AutoModel 即崩，无「请用 cpu」提示；对照 whisper.py:43-48 有 `is_cuda()` 兜底，FunASR 漏网。修复：仿 whisper 加 cuda 探测回退 cpu。
- **B2 无锁 RMW** `WhisperModelRegistry.add_custom_model/remove_custom_model`（whisper_models.py:139-155）read-modify-write 无锁——cookie_manager（#124 B15）、transcriber_config_manager（#126 B9）都补了 RLock，此处漏网。修复：class-level RLock。
- **B3 close 静默 no-op** whisper 家族（WhisperTranscriber/MLXWhisperTranscriber/FunASRTranscriber）无 `close()`——transcriber_provider.py:67-74 的「防御性释放」`getattr(old, "close")` 恒 None，切模型尺寸时旧 large-v3（~3GB）双驻留。修复：三个转写器实现 close() 释放模型引用。
- **B4 整文件读** `KuaishouTranscriber._load_file`（kuaishou.py:20-23）一次性 `f.read()` 全部字节——bcut 已改分片读（#125 B15），此处漏网；长音频内存峰值 ~2× 文件。修复：分段读/文件对象。
- **B5 None 裸崩** `kuaishou_downloader.py:50` `photo_info['caption'].strip()`——GraphQL 返回 caption:null（无文案）时下载开始前 AttributeError，整任务 FAILED。修复：`(photo_info.get('caption') or '')`。
- **B6 裸索引** `douyin_downloader.py:295-298` 多层裸索引 `video_data['aweme_detail']['video']['download_addr']['url_list'][0]`——截图/视频理解路径 API 异常给天书错误，与 download() 的 `.get()` 口径不一致。修复：`.get()` + 显式错误。
- **B7 裸 int 读 env** `UniversalGPT.__init__`（universal_gpt.py:59-67）四个 env 裸 `int()`/`float()`——手滑配非数字 env → 总结前 FAILED。修复：复用 config.env_int 解析 + 回退默认。
- **B8 print 打错误** `ProviderService.add_provider`（provider.py:90-91）`print('创建模式失败')`——MCP 下 stdout 被劫持、CLI 打 stdout 无堆栈；同文件 update_provider 已改 logger（#123 B8）。修复：`logger.error(..., exc_info=True)`。
- **B9 默认落源目录** `LocalDownloader.convert_to_mp3`（local_downloader.py:78-80）`output_path` 缺省在源文件同目录产出 mp3 且无清理——封面提取已修到 data_dir/covers（#121 B3），此处漏网；note.py 主路径总传 output_dir 故仅外部直接调用时触发。修复：默认输出数据目录。
- **B10 日志 None** `note.py:1129` 日志 f-string 裸切 `title[:40]`——#126 B8 修了 DAO 漏了此日志行；material_only 路径传 `(audio_meta.title if audio_meta else None)`，标题缺失时 TypeError 被 except 吞、误报「保存任务记录失败」。修复：`(title or '')[:40]`。

### ✅ 128. 第 12 轮双代理扫描候选（2026-08-17 登记；commit 05855b3）

代理 A（工具面 8 项）+ 代理 B（app 流水线 7 项），全部经行号核对。**两代理一致判定：无 P0/P1**——正确性/安全/数据丢失面已收割干净；残余为「文档死点 + 默认值分叉 + 半收口」，多数是跨层交互（seed vs add_provider 的 id 契约、download vs check 的 case 分叉、CWD 相对 vs 数据目录相对）。按严重度排序：

#### A 组（工具面 8 项）

- **A1 ⚠ SKILL 死点** troubleshooting.md:14/15/18/21/35 五处仍教 `set_downloader_cookie(platform=…, cookie=…)` 手动配 Cookie，server.py:2549-2561 对非空 cookie 一律拒绝（红线）——Agent 按 SKILL 逐字执行必失败且反复撞红线（同文件其余行已改引导 `login bilibili`）。修复：5 行统一改「引导用户 `! videonote login bilibili`」。
- **A2 ⚠ 预下载白下** `download_transcriber_model`（server.py:1983）`size = model_size.strip().lower()`——传 `Systran/faster-whisper-large-v3` 时 resolve 直通返回小写 target，snapshot 落 `models--systran--…`；而 set_transcriber 持久化原 case，`is_model_ready`/加载用 `hf_cache_dirname("Systran/…")`=`models--Systran--…` 找不到 → 1.5GB 白下、preflight 仍报「未下载」。CLI（cli.py:106）不 lowercase 正常。修复：仅内置档位名做 lowercase 容差，直通 repo_id/本地目录保持原 case。
- **A3 formats 混型裸崩** `export_transcript`（server.py:2594）`sorted({f for f in formats if f not in FORMATS})` 原始元素去重——`formats=[1,"pdf"]` 触发 `'<' not supported between int and str`，`[["srt"]]` set 构造 unhashable；#117 已把同款校验改 `sorted({str(f) ...})`（server.py:162），此处漏改。修复：元素先 `str(f)`。
- **A4 docstring 与行为矛盾** `cleanup_all` docstring（server.py:1436）称「数据库记录不动」，实际 `cleanup_all_files` 调 `delete_all_tasks()` 清空索引（task_manifest.py:378-384）；cleanup_note 的 `delete_task` 删索引行副作用也未注明。修复：docstring 同步。
- **A5 快照未接缺失分支** `get_task_status`（server.py:1034-1045）status.json 缺失直接 NOT_FOUND，不查 `_status_memory`——`_write_status` 先写快照再写盘，提交时首写失败（磁盘满）任务在跑但查状态报「不存在」，与 list_tasks 的 PENDING 矛盾（#118 快照回退只在损坏分支触发）。修复：缺失分支也查快照。
- **A6 模型列表硬编码** `list_transcriber_models`（server.py:1963-1970）遍历 `WHISPER_MODEL_SIZES` 仅 6 档——download 已放行 8 档 + 自定义/HF repo_id（#125 C7），下载 large-v1/自定义后状态永远「未下载」误判反复重下。修复：遍历 `WhisperModelRegistry.visible_model_names()`。
- **A7 SKILL 指引矛盾** tools.md:22 教 `kind=multi`「每条 url 当独立视频 subagent 逐条提交」，同文件 :23 和 SKILL.md 主张多集用 `batch_generate_notes`——按 :22 做会在 3-worker 上限下逐条被拒。修复：:22 改「多集全出用 batch；只一集用对应 url」。
- **A8 CLI/MCP 默认分叉** CLI `transcriber set`（cli.py:1313-1314）切本地引擎未带 `--size` 时静默重置 `"small"`；MCP `set_transcriber` 保留既有配置——用户先配 large-v3 → 切 groq → 切回 fast-whisper，CLI 版悄悄降级。修复：CLI 未指定 size 时保留现有配置（或明确提示）。

#### B 组（app 流水线 7 项）

- **B1 ⚠ groq 引擎不可配置** `GroqTranscriber`（groq.py:50）硬编码 `get_provider_by_id('groq')`——`add_provider`（provider.py:87）强制 uuid id；内置 groq 行只在 providers 表空时 seed 一次（builtin_providers.json:53 id='groq'）。按向导 `providers add` 新建 groq → uuid id 与内置行并存，转写器读空 key 内置行报「API Key 未配置」；库非空 seed 被跳过 → id='groq' 永不出现，groq 引擎无修复入口。修复：按名称 `get_provider_by_name('groq')` 查找。
- **B2 产物目录分裂** note.py:50 / pipeline.py:44 / universal_gpt.py:87 的 `NOTE_OUTPUT_DIR` 缺省 `Path("note_results")`（CWD 相对），task_manifest.py:37 缺省 `get_data_dir()/"note_results"`（数据目录相对）——不经 config 的裸脚本跑 note.py，产物写 CWD/note_results，清理/status/list_task_files 走 task_manifest 解析到 data/note_results → 任务产物对清理失明。修复：三处缺省统一 `get_data_dir()/"note_results"`。
- **B3 whisper 默认 base vs small** transcriber_provider.py:130 / whisper.py:37 / mlx:45 兜底 `"base"`，产品默认 small（#125 A8）——绕过 config manager 的直接调用静默用 base，质量降级无日志。修复：三处统一默认 small。
- **B4 临时目录泄漏** `apply_diarization`（pipeline.py:188-197）`created = True` 在 `normalize_to_wav` 返回后才置位——normalize 抛错（ffmpeg 失败）时 finally 跳过 `shutil.rmtree`，`/tmp/vn_dia_XXXX` 永久残留。修复：created 在 mkdtemp 后立即置位。
- **B5 kuaishou 父级裸索引** kuaishou_downloader.py:48/60-61 `video_raw_info['visionVideoDetail']['photo']`、`photo_info['photoUrl']` 等裸下标——接口形状变更/视频被删时 `visionVideoDetail` null → `None['photo']` 天书 TypeError（#127 B5 只修了 caption）。修复：`.get()` + 显式 RuntimeError。
- **B6 handler 数据删除炸弹** handlers.py:27-32 前缀删除：`file.startswith(video_id)` 删除 `dir_path` 内所有匹配文件，只挡了「目录==data/」一种——local 直接转写时 dir_path 是用户源目录，会连带删用户文件；所有转写器 `self.on_finish` 已被注释（whisper:179 等），但 server.py:2751-2753 仍注册 handler——只差一个 uncomment 就触发静默删用户文件。修复：删除该 handler 或改为任务目录内 `_safe_resolve` 沙箱清理。
- **B7 timeit print** timeit.py:12 `print(...)` 度量挂在 5 个转写器上——靠全局 print 重定向才不污染 stdio，裸脚本/新入口/pytest 直跑会打进 stdout。修复：改 `logger.info`（#127 B8 同口径）。
### ✅ 129. 第 13 轮双代理扫描候选（2026-08-17 登记；commit 12ec589）

代理 A（工具面 8 项）+ 代理 B（app 流水线，待返回），全部经行号核对。**代理 A 判定：无 P0 新增**——第 12 轮后代码层已干净；漏网集中在「文档死点 + 半收口残骸 + docstring/实现分叉」。按严重度排序：

#### A 组（代理 A 工具面 8 项）

- **A1 ⚠ SKILL 红线反例** troubleshooting.md:10 教「空 key 用 `update_provider` 填，自建用 `add_provider`」——两工具都拒绝 api_key（server.py:1731-1732/1754-1755），Agent 照做必失败且撞「API key 绝不经对话」红线（docs/02:88、docs/04:160 均写明拒绝）。修复：改「填 key 一律走 `! videonote providers set <id> --api-key '...'`」。
- **A2 文档默认值矛盾** docs/04:156 `get_task_transcript` 写「默认空=全部」，实现默认只返回前 50 段（server.py:733 `_TRANSCRIPT_DEFAULT_SEGMENTS=50`，:1150-1151 空串→(0,min(50,total))，docstring :1238 同口径）——Agent 按手册误以为拿到全文，忽略 meta.truncated。修复：改「默认空=前 50 段」。
- **A3 文档自相矛盾** docs/04:176 `cleanup_all` 写「清空 note_results/static/**logs**」，同文件 :254 与 task_manifest.py:370-373 / server.py:1444-1445 均「logs 不清」（#121 C3）。修复：176 行去掉「/logs」。
- **A4 文档漏改** docs/02:93 `cleanup_all 默认不动 DB` 与实现矛盾（task_manifest.py:378-384 调 delete_all_tasks 清索引）——#128 A4 只修了 server docstring，漏了 docs/02。修复：改「默认不动 DB」为「同步清空全局索引（config/models 默认保留）」。
- **A5 半收口残骸** health_check（server.py:2140）仍遍历 6 档 `WHISPER_MODEL_SIZES`，#127 A6 只改了 list_transcriber_models——自定义注册模型在 list 显示已下载、health_check 永远缺席，两个工具给 Agent 互相矛盾的就绪信号（preflight 依赖 health 口径）。修复：改 `get_registry().visible_model_names()`。
- **A6 preflight 批量子句冲突** server.py:2376 `detail` 写「建议逐条生成，分 P 用 entries[].url」，SKILL.md:18/30、tools.md:22-23、batch docstring（:2413）均主张一条 batch 全出。修复：改「建议一条 `batch_generate_notes` 服务端逐个排队」。
- **A7 导出目录 docstring 分叉** export_transcript docstring（server.py:2596）写「缺省为 note_results/{task_id}/」，实际默认 `task_dir / "gen"`（:2625）——Agent 按 docstring 去任务根目录找导出文件找不到。修复：docstring 改「缺省为 note_results/{task_id}/gen/」。
- **A8 ⚠ 自定义大写模型名下载被小写化** download_transcriber_model（server.py:2004-2005）对非「/」非目录一律 `lower()`——`resolve_whisper_model` 大小写敏感（whisper_models.py:130-132），`add_custom_model("MyModel")` 在 set_transcriber 能过、download_transcriber_model("MyModel") 却 lower 成 "mymodel" 报 unknown（注释 :2000 声言「仅内置档位名做小写容差」但代码没做到）。修复：仅当 size 命中内置档位（忽略大小写）时才 lower，其余保持原 case。


#### B 组（代理 B app 流水线 7 项）

- **B1 默认值分叉残留** transcriber_provider.py:86/97 `get_whisper_transcriber` / `get_mlx_whisper_transcriber` 默认 `"base"`，whisper.py:37 / mlx:45 / 统一兜底 :130 均 `"small"`（#128 B3 只改了构造器与 get_transcriber，两个类型级 getter 漏改）——绕过 get_transcriber 直接调这俩 getter 的代码（CLI/单测）静默拿比统一默认更低质量。修复：两处默认改 `"small"`。
- **B2 文档死点（app 层）** bilibili_downloader.py:201-203 / bilibili_subtitle.py:158-161 日志文案教「配置 `set_downloader_cookie(bilibili, SESSDATA=...)`」——MCP 工具对非空 cookie 直接拒绝，用户按提示操作必被拒、误以为配置被吞（#128 A1 只清了 MCP 工具面，没清 app 层日志）。修复：文案改「`! videonote login bilibili`（扫码）或 `videonote setup` 写入 SESSDATA」。
- **B3 缺省落 CWD** merge.py:50 `out_dir = Path(out_dir).expanduser() if out_dir else Path.cwd()`，docstring :40 同步过时——MCP 工具层总传 `NOTE_OUTPUT_DIR/merged` 不触发，但直接调 `merge_audio()`（vendored 核心/裸脚本）产物落不确定 CWD。修复：缺省改 `NOTE_OUTPUT_DIR`（与 pipeline.py:46 同源），docstring 同步。
- **B4 静默吞异常** task_manifest.py:315-320 `try: delete_task(task_id) except Exception: pass`——cleanup_note(include_note=True) 时 DB 删除失败磁盘已清但索引残留幽灵任务、连「哪个任务失败」上下文都丢（与 #126 B7 delete_all_tasks 显式 re-raise 口径不一致）。修复：捕获后 `logger.warning`，不阻断文件清理。
- **B5 半收口** note.py:405 仅 `if _format:` 才 `_post_process_markdown`，截图替换只认 `"screenshot" in formats`——直接调 `note.generate(screenshot=True, _format=None)` 时 markdown 里 `*Screenshot-[mm:ss]` 标记原样残留（#120 G1 只在 server 层做布尔→format，note.py 作为 vendored 核心公开的 screenshot 参数半生效）。修复：note.py 内自闭合 `if screenshot and "screenshot" not in (_format or []): _format=[*(_format or []), "screenshot"]`。
- **B6 空值裸崩** kuaishou.py:74 `result_data.get('data', {}).get('text', [])`——API 返回 `data: null`（键存在、值 null）时 `None.get('text', [])` AttributeError（#126 B5 防了段内 null，外层 data null 漏网）。修复：`(result_data.get('data') or {}).get('text', [])`。
- **B7 并发竞态残留** transcriber_provider.py:66-75 模型尺寸切换重建时在 `_cache_lock` 下对旧实例调 `close()`（whisper.py:188-191 置 `self.model=None`），未持旧实例 `self._lock`——另一线程正持旧实例转写（`with self._lock` 内 transcribe）时 model 被并发置空、进行中转写异常退出（窄窗口、仅 setup 切模型时触发，#127 B3 未完全闭合）。修复：close 前先 `lock = getattr(old, "_lock", None)`，有锁则 `with lock:` 内再置空 model。
### ✅ 130. 第 14 轮双代理扫描候选（2026-08-17 登记；commit 2c58ba694599e64240bfab78494e424b921d6cbf)

代理 A（工具面 6 项）+ 代理 B（app 流水线 3 项），全部经行号核对。**两代理一致判定：已非常接近完美，清完这批可收敛**——正确性/安全/数据丢失面已收割干净，残余为「truthy-swallow 家族最后漏网 + 死链残骸 + 复杂度优化」。按严重度排序：

#### A 组（工具面 6 项）

- **A1 ⚠ bool truthy-swallow 家族最后一窝** `generate_note`/`prepare_note_material` 的 `video_understanding`/`include_comments`/`screenshot` 五处 `bool(get_app_config().get(...))`——字符串 `"false"` 在 Python 里恒为 True（用户配 `VIDEONOTE_VIDEO_UNDERSTANDING=false` / app_config `"false"` → 永远开视频理解，白抽帧烧多模态 token）；#116 只修了 int 系（resolve_int_config），bool 系漏网。修复：config.py 抽 `resolve_bool_config`（bool 直通 / 词表 1-true-yes-on·0-false-no-off / 垃圾值 warning 回退 env/默认），server 五处接入。
- **A2 comments_limit 显式 0 被吞** `comments_limit = comments_limit or 20`——传 0（用户想关评论）被 falsy 吞成 20 条（#116 同族，int 系也漏了这处）。修复：`_coerce_int(comments_limit if comments_limit is not None else 20, 20, clamp_min=1)`。
- **A3 CLI 导出契约** `cli.py` export `--out-dir` help 写「缺省 note_results/{task_id}/gen/」与实际一致；且 CLI 不认 `file://`（URI 直传被 Path 判不存在误报——#105 只修了 MCP 工具面）。修复：help 修正 + file:// 解码/expanduser 规整。
- **A4 inspect 本地幻影路径** `inspect_video` local 分支不检查文件存在——Agent 拿 `ok:true` + 幻影路径喂 `generate_note` 才报「本地文件不存在」（#128 侧漏：validate_url 有检查，inspect 没有）。修复：`Path(raw).expanduser()` 后 `is_file()` 校验，不存在返回 `ok:false` + 明确错误。
- **A5 merge 目录穿透** merge_audio 文件参数 `os.path.exists` 对目录为 True——目录穿透到 ffmpeg 深处才炸「转换失败」泛化错误（#105 修了 diarize、#109 修了 merge 入口的 is_file，vendored merge.py 本体漏网）。修复：`Path(p).is_file()` + FileNotFoundError。
- **A6 死 import** server.py 残留 `env_bool` import（bool 解析改造后已无使用）。修复：删除。

#### B 组（app 流水线 3 项）

- **B1 sqlite_client 死引信** `app/db/sqlite_client.py` 全仓零引用 + CWD 相对 DB 路径（`sqlite:///video_note.db`）——#33 仓库垃圾清单的活引信，任何「照着 import 试试」都会在 CWD 建库。修复：整文件删除，VENDOR.md 同步。
- **B2 events 死链整链删除** blinker `transcription_finished` 信号 → 4 个转写器 `on_finish` 调用全被注释（#128 B6 修 handler 时只沙箱化没删）——`transcription_finished` 永不触发，server 的 `register_handler` 纯空转，events 包（signals/handlers）+ 4 个 on_finish 方法全是悬空接口。修复：整链删除（server 注册点 + events 包 + on_finish 方法），VENDOR.md / docs/02 同步。
- **B3 RequestChunker O(n²)** `chunk()` 主循环每加一段重建整条消息并 json.dumps 估算——长转写（数千段）线性逐段试累计 O(n²) CPU，单条 2 小时视频可达数秒级。修复：`_largest_fitting_prefix` 二分（fits 单调 → 最大可容纳前缀与线性扫描边界完全一致，_fits 调用从 O(段数) 降到 O(log 段数)）；`group_texts_by_budget` 同款二分；`_split_segment_to_fit` 本身已是二分不动。**400 随机 trial 差分验证 == 线性参考（含超长段 split / comments_danmaku 首块注入 / max_tokens 约束 / 异常一致性），diffs=0**。
### ✅ 131. 第 15 轮双代理收敛验证候选（2026-08-17 登记；commit 4effbb229293a4a3c0ecdda780e04b64621217a2)

第 14 轮（#130）判定「清完这批可收敛」后的**收敛验证轮**。代理 A（工具面）+ 代理 B（app 流水线）独立全库复扫。

**代理 B 判定：无新增 finding，可停** —— app/ 全库无 P0/P1、无值得做的 P2；上传者（bcut/kuaishou/groq）异常路径与 ffmpeg/网络失败行为全部确定（业务 code 检查、全调用带超时、失败清理/显式 raise、无 while True、无裸吞、无资源泄漏）。

**代理 A 判定：修掉 1 项即可正式收敛** —— 工具面无 P0/P1、无 Agent 操作死点、无形状矛盾、无测试/实现分叉；仅发现 #130 B2 的同波遗漏：

- **A1 P1 release.yml 门禁引用已删文件** `.github/workflows/release.yml:64` required 列表仍含 `app/events/__init__.py`（#130 B2 整链删除时漏改）——wheel 校验 `missing` 恒非空 → `sys.exit(1)`，打 v* tag 发布必失败。修复：删该行（其余 5 项均存在）。
- **A2 blinker 死依赖** pyproject.toml:16-17「转写完成事件（app/events/，blinker 信号）」+ `"blinker>=1.9.0"`——events 包已删、全仓零 Python 引用，注释描述已不存在的机制。修复：删两行 + `uv lock` 重新生成。
- **A3 docs/05 状态回写** #128/#129/#130 标题仍 `⏳`（批 16-19 均已实施完成、commit 已回填）——违反「每批完成：更新 docs/05 状态」纪律（#120-#127 均为 ✅）。修复：三处 ⏳ → ✅。

排除的疑点（无问题）：inspect local 校验、diarize num_speakers、merge is_file、plugin commands 相对路径、凭证红线全链路、工具返回形状对称、版本一致性（0.1.5×3 处 + ci 39 工具名单）。

---

### ⏳ 132. 第 16 轮全库扫描候选（2026-08-18 登记；实施完成待 commit）

三代理并行全库扫描（安全 / 正确性·健壮性 / 性能·依赖），关键项代码级复核并对照前 15 轮剔除已登记项（notes_dir 任意写 #45 ✅、crypto fail-open 有意设计、本地媒体读取为核心功能）。**691 passed + ruff F/I-clean**（较 #131 净增 24 测试）。

#### A 组（安全，1 高 / 1 中 / 3 低）

- **A1 高 SSRF** generic 下载器把任意 URL 直接交给 yt-dlp（`detect_platform` 对未知 URL 一律 generic → `GenericDownloader` 原文传入 `extract_info`），可打云元数据 169.254.169.254 / 内网服务 / 同网段容器。修复：新增 `app/utils/url_safety.py`（http/https scheme 白名单 + ipaddress 私网/环回/链路本地判定 + 域名解析校验，解析失败放行交给 yt-dlp 报错），接入 generic/youtube downloader、`inspect._inspect_ytdlp`、`validate_url`（拒绝时保留 platform 字段）；conftest 新增 `_mock_public_dns` 桩（沙箱 DNS 把域名解析到 198.18/15 保留段，测试确定化）。
- **A2 中 api_key 经 base_url 改向外泄** `update_provider` 可改任意 base_url，`list_models`/`test_provider`/`generate_note` 把库中已存 key 定向发往新端点——绕过「MCP 拒 key」红线。修复：`ProviderService._validate_base_url`（http/https + host）+ **base_url 变更且未带新 key → 已存 key 失效**（CLI/MCP 同源）+ MCP 返回 `key_invalidated` + notice 引导重新录入。
- **A3 低 Docker root** Dockerfile 无 USER 指令。修复：非 root `videonote` 用户 + /app 归属（SSRF/文件写被利用时容器内不放大）。
- **A4 低 代理凭据进日志** openai_client:57 / youtube_downloader `_apply_proxy` / youtube_subtitle:33 打完整 proxy_url。修复：`sanitize_url` 剥离 userinfo。
- **A5 低 敏感 URL 落日志** douyin:220 / bilibili:135 日志带完整 video_url（含签名 token）。修复：`sanitize_url` 剥 query。

#### B 组（正确性/健壮性，4 中 / 7 低实施，3 项评估免改）

- **B1 中 cancel 不打断进行中的下载/转码** worker 槽被占数分钟到数十分钟。修复：`ytdlp_cancel_hook`（common.py，progress_hooks 检查取消即抛）+ `stream_download` 下载循环内检查 + `_extract_audio_from_video` 改 Popen 轮询（cancel 时 terminate）+ 6 下载器 `download`/`download_video` 签名加 `cancel_event`（向后兼容，note.py `_download_media` 全调用点透传）。
- **B2 中 预处理分块部分失败仍 SUCCESS 且 truncated 不暴露** 修复：`TranscriptResult.truncated` 字段 + 重建/缓存加载透传 + `_summarize_text` 笔记尾部标注「转写不完整」；全成功保持旧契约形状（不含键）。
- **B3 中 note_cache 并发 promote 共享固定 `.tmp`** 交叉写坏缓存。修复：tmp 带 uuid 唯一后缀（transcript + media），replace 仍原子。
- **B4 中 抖音/快手直连下载 timeout=30 无重试** socket 停滞 30s 整体失败、已下内容作废。修复：`stream_download`（连接 10s / 读 300s 分离超时 + 瞬时错误指数退避重试 + 5xx 重试 + 404/403 不重试 + 取消检查），替换 douyin×2 + kuaishou×1。
- **B7 低「任务排队中」占住步骤任务 summary** 修复：占位文案不落 summary + SUCCESS 清占位。
- **B8 低 json_store/config 固定 tmp + 先写后 chmod 权限窗口** 修复：`_unique_tmp`（pid+uuid 后缀，跨进程不互截断）+ `_write_bytes_with_mode`（os.open 创建即 0600），json_store 两函数 + config `_write_app_config` 接入。
- **B9 低 status.tmp 双写者** note.py `_update_status` 与 server `_write_status` 共用固定 tmp。修复：两处统一唯一后缀。
- **B10 低 exporter 非原子写** 修复：`write_text_atomic`。
- **B11 低 download_video「已存在即复用」不校验完整**（#124 B1 残留）修复：bilibili/youtube/douyin 复用前 `getsize>0`，0 字节/半截删除重下。
- **B13 低 douyin/kuaishou 模块级 CookieConfigManager 仍在**（#49 根因未清）修复：惰性单例（import 不再写空 downloader.json；测试 mock `cfm` 兼容）。
- **B14 低 BcutTranscriber Session 仅 `__del__` close** 修复：`_transcribe_audio` finally 确定性 close（whisper/funasr/mlx 的 close 释放模型且实例被缓存，不动）。
- **评估免改**：B5 并发门禁 check→submit 窄窗口（瞬时多 1-2 排队任务，不阻塞不失败，batch 有 bypass）；B6 终态 SUCCESS 写盘失败卡 INITIALIZING（#118 内存快照先行 + #121 C9 写失败保留快照已覆盖）；B12 Netscape cookie 临时文件（mkstemp 0600 + `__del__` + atexit 兜底，SIGKILL 残留可接受）。

#### C 组（性能/依赖，2 P1 / 2 P2 实施 + 6 项建议后续）

- **C1 P1 `av` 死依赖** 全库零直接 import，从直接依赖移除（faster-whisper 1.2.1 的传递依赖，仍会安装）；「3.14 上界」注释修正为真实原因 ctranslate2。`uv lock` 重解析。
- **C2 P1 douyin 任务内 `fetch_video_info` 双调**（download_video + download(skip) 各一次 msToken POST + aweme GET + 签名）。修复：实例级 memo（按 aweme_id）+ msToken 复用 + ABogus 实例复用。省 ~0.3-0.8s/任务。
- **C4 P2 bcut 轮询无总时长上限**（最坏 500×5s ≈ 41 分钟占死 worker 槽）。修复：10 分钟总 deadline 显式报错。
- **C8 P2 DB 常用查询列缺索引** 修复：init_db 建 `video_tasks(video_id, platform)` / `providers(name)` / `models(provider_id)`（IF NOT EXISTS 幂等 + 表未建容错跳过）。
- **C10 P2 死代码三模块** `app/utils/status_code.py` / `app/models/video_record.py` / `app/models/provide_model.py` 全库零引用，删除。
- **建议后续（未实施）**：C3 video_reader 每帧一个 ffmpeg 进程单遍化（216 帧场景）、C5 本地大文件整文件 sha256 采样化、C6 note_cache 无 LRU/TTL、C7 帧 base64 全量驻留内存改按需读、C11 ffmpeg→mp3 三处重复抽取 `app/utils/audio.py`、C12 whisper/mlx 模型下载三处实现统一。

#### 测试

新增：`tests/test_url_safety.py`（21 用例）+ `test_server_contracts.py`（validate_url SSRF 6 / update_provider key 失效 6）+ `tests/test_round16_batch2.py`（B1-B4 共 12 用例）；更新：kuaishou 测试 mock 全局 `requests.get`、CLI update_provider 测试假行、exporter 失败测试 mock `write_text_atomic`、conftest `_mock_public_dns`。**691 passed + ruff F/I-clean**。

### ✅ 133. 第 17 轮四维全库扫描候选（2026-08-18 登记；commit fcf35d1 / bda7c74 + CI 修复 838396c）

四维并行扫描（安全 / 正确性·健壮性 / 工具面·CLI 契约 / 测试·依赖·文档漂移），代码级核实并对照前 16 轮剔除已登记项。实施 9 项 + 建议后续 8 项。**743 passed + ruff F/I-clean**（较 #132 净增 52：+14 契约/批量测试 + 36 test_url_safety 收集修复 + 2 batch）。

#### A 组（安全，1 高实施 / 1 低建议后续）

- **A1 高 SSRF 覆盖不全（已实施）** #132 A1 只覆盖 generic/youtube 下载器内部——显式传 `platform=bilibili/kuaishou/douyin` 时下载器/短链解析直接对 URL 发出站请求（yt-dlp `extract_info` / `requests.head`），恶意/被注入的 agent 可借 `generate_note` 等打内网/云元数据。修复：① url_parser 两个短链解析器内置 `assert_public_http_url`（调用方只按 b23.tv / v.douyin.com 子串分流，`http://169.254.169.254/?x=b23.tv` 原样直连）；② inspect_video 非 local 分支统一守卫（preflight / batch 同漏斗）；③ generate_note / prepare_note_material / fetch_subtitles 入口守卫（platform=None 时靠解析器/下载器内部守卫兜底）。
- **A2 低 固定 tmp 名 + 默认 umask 0644 写盘（建议后续）** `_atomic_write_json`（server:322）/ `task_manifest` 两处 / `universal_gpt._save_checkpoint` 仍是固定 `.tmp` + `write_text`（0644）——#132 B8/B9 只覆盖了 json_store/config/status.json。修复方向：三处复用 `json_store._unique_tmp` + `_write_bytes_with_mode(0o600)`。

#### B 组（正确性/健壮性，4 中 + 2 低实施，2 项建议后续）

- **B1 中 显式 provider_id 不校验 api_key（已实施）** 空 key 的内置行（openai/groq seed）显式传入放行，下载+转写全跑完才 SUMMARIZING 报「API Key 未配置」（#52 只修默认解析分支）。修复：显式传入时校验 provider 存在 + key 非空 + 不含 `*`（与 `_preflight_provider` 同口径；模型检查不做——model_name 可显式传入）。summarize_note 同构但影响小（LLM 阶段即失败），建议后续同修。
- **B2 中 inspect/preflight/batch 本地分支不认 file://（已实施）** 全工具面唯一漏网（#105/#107 输入规整系列）：`Path(raw).is_file()` 未 unquote，`file:///...%20...` 误报「本地文件不存在」。修复：local 分支用 `_coerce_local_path` 规整，entries[].url 透传纯路径。
- **B3 中 summarize_note full_text-only 放行（已实施）** 入口只查「segments 或 full_text 字段存在」（#104），但流水线只消费 segments——只传 full_text 时 LLM 拿零素材凭空生成还烧配额。修复：要求 segments 为列表（空列表=静音视频合法转写不拦），full_text-only 显式报错；旧契约测试 `test_full_text_only_accepted` 同步反转。
- **B4 低 diarize_media normalize 失败残留半成品 wav（建议后续）** `normalize_to_wav` 在 try/finally **之外**（server:2803），ffmpeg 失败残留 `<原名>_16k.wav`（#121 C2 修复的缺口）。修复：normalize 包进 try/finally。
- **B5 低 local/kuaishou ffmpeg 转 mp3 不响应取消（建议后续）** `subprocess.run(timeout=600)`（local_downloader:96 / kuaishou_downloader:123）——cancel 后 worker 槽被占最多 600s（#132 B1 覆盖了 ytdlp/stream_download/`_extract_audio_from_video`，这两处漏网）。修复：Popen + poll 轮询，取消 terminate。
- **B6 低 Provider DAO 吞 DB 异常 → 静默假成功（建议后续）** provider_dao update/insert 全捕获只 logger.error 不 re-raise——锁/磁盘满时 `update_provider` 报「已更新」实际没更新（#96/#123 B8 同族收尾缺口）。修复：DAO 失败 re-raise 或返回 None 由 Service 显式报错。
- **B7 低 quality 校验在 provider 解析后（已实施）** 全新环境 quality 拼错报「需要 provider_id」误导（H6：参数错误先报）。修复：`DownloadQuality` 校验移到 provider 前，与 style/format/grid 同位。
- **B8 低 batch max_entries=0 被子 `or 10` 吞（已实施）** 显式 0 提交 10 条（#116/#130 A2 同族）。修复：`clamp_min=0`，0 与 list_tasks 同口径（提交 0 条仅展开）。

#### C 组（依赖/测试/文档/CI，1 中实施 + 2 低实施 + 5 建议后续）

- **C1 中 依赖声明漂移（已实施）** `from mcp.server.fastmcp import FastMCP` 由 mcp 包自带（1.29.0 实测），直接依赖却声明从未被 import 的独立 fastmcp 包——uvx 安装不读 lock，fastmcp 放开 mcp 上界/移除 server.fastmcp 即挂。修复：直接声明 `mcp>=1.29.0`，fastmcp/fastmcp-slim 从 lock 移除（-500 行）。
- **C2 中 VENDOR.md 分叉清单漂移（已实施）** #132 C10 删的三死模块仍在清单、url_safety/note_cache/json_store/douyin_helper/kuaishou_helper 未收录。修复：三处清单同步（按此同步上游会重引入死模块或漏最需人工合并的分叉点）。
- **C3 中 CI Actions 过期（已实施）** checkout@v4 / setup-uv@v3 被强制跑 Node24 + 弃用标注（第 17 轮 CI 修复时已升 v5/v7）；action-gh-release v2 官方停维护（final v2.6.2）→ 升 v3（纯 Node24 运行时，无输入变更）。
- **C4 低 test_url_safety 从未被 pytest 收集（已实施，测试基建 bug）** 6 个类名非 `Test` 前缀，pytest 默认 `python_classes=Test*` 全不收集——#132 A1 的 SSRF 回归测试从未执行（0 收集）；其余测试类均为 unittest.TestCase 不受影响。修复：重命名 `Test*` 前缀，36 用例进套件（691→727 的差额）。
- **C5 低 测试断言「整个 /tmp 无残留」全局状态（建议后续）** test_bilibili_downloader:137,156 `glob("/tmp/videonote_subs_*")==[]`——SIGKILL 残留/并行进程会让本测试无辜变红（CI 恒绿本地红的典型形态）。修复：before/after 快照差集。
- **C6 低 conftest 隔离不对称（建议后续）** `NOTE_OUTPUT_DIR`/`VIDEONOTE_DATA_DIR` 用 setdefault 而 DATABASE_URL 强制赋值——shell 已导出时测试写真实 note_results、stderr 灌真实 mcp_stderr.log。修复：同 DATABASE_URL 直接赋值。
- **C7 低 Dockerfile 构建 wheel 缺 skills/**（建议后续）** Docker COPY 不带 skills/，与 pyproject wheel include + release 门禁不一致（同一源码两种产物）。修复：COPY skills/ 或注明差异有意。
- **C8 低 测试硬编码 /tmp 无 pid 隔离（建议后续）** test_groq_compress/test_bilibili_comment/test_openai_client 固定 /tmp 路径（#66 盲区）。修复：tempfile.mkdtemp。
- **C9 低 单文件直跑写真实数据目录（建议后续）** test_126/127_batch docstring 教的 `.venv/bin/python tests/xxx.py` 直跑模式无 conftest，`NOTE_OUTPUT_DIR` 落真实 data/。修复：standalone 兜底补 setdefault。
- **C10 低 CLI transcriber download choices 缺 large-v1/v2（建议后续）** 内置 8 档 choices 只列 6 档（cli:63/1294），set 成功后把内置档误标「自定义尺寸无预下载命令」，CLI 与 MCP 行为分叉。修复：choices 与 `resolve_whisper_model` 内置表同源。

#### 测试

新增：test_server_contracts（SsrfEntryGuardTest 7 / ExplicitProviderKeyCheckTest 4 / InspectLocalFileUriTest 2 / GenerateNoteQualityOrderTest 1 + SummarizeTranscriptShapeTest 反转 full_text-only 契约）、test_batch_generate（BatchMaxEntriesZeroTest 2）、test_url_safety（ShortUrlResolverGuardTest 4，随类名修复进套件）。修复：test_url_safety 类名 `Test*` 前缀（36 用例开始执行）。**743 passed + ruff F/I-clean**。

### ✅ 134. 死代码清理（2026-08-18 登记；commit 8d207c8）

用户确认「清理死代码」后的专项轮（承接 #133 批 22 的审计结论：AST import 图 + 名字引用计数双扫描，模块级 + 公开函数级，无工具冗余）。

- **模块级（2）**：`app/validators/` 整个子包（video_url_validator 全仓零引用）；`app/exceptions/biz_exception.py`（零引用死类，exceptions 包剩 note/provider/task）。
- **函数级（~15）**：whisper_models 自定义模型写 API 6 个（add/remove/get_custom_models/is_known/_write_custom/_write_lock —— 写端改为手工编辑 `config/whisper_models.json`，读端保留）；note.delete_note + video_task_dao.delete_task_by_video（唯一调用链）；bilibili_downloader.delete_video；model_dao.get_all_models（+ 随之清掉的 Provider import）；provider_dao.get_enabled_providers；model_download_state.mark_downloading/get_error（is_downloading 保留，transcriber_config_manager 在用）；audio_preprocess.preprocess_pipeline；path_helper.get_app_dir；proxy_config_manager.apply_to_env；OpenAI_compatible_provider.test_connection（文件只剩 `__init__` + get_client property）；abogus convert_to_char_code/generate_result_unit/generate_result_end/generate_args_code（+ 悬挂注释）；task_manifest.record_task_meta（get_task_meta 恢复——list_task_files 在用，写端已删）。
- **工具冗余结论**：无实质冗余 —— wait_for_note 保留为 deprecated compat（SKILL 仍引用），_stage_label 保留为 micro-tool；**39 工具名单不变**。
- **误删恢复 2 处**：get_task_meta 初扫时误删（list_task_files 在用）→ 恢复并注释写端已删；model_dao 删 get_all_models 留 dangling `finally: db.close()`、OpenAI provider 删 test_connection 留 dangling `@staticmethod` → 均已修。
- **测试**：WhisperRegistryConcurrencyTest 随写 API 删除；WhisperModelsAtomicWriteTest 重写为 WhisperCustomJsonReadTest（读端契约：手工 JSON 合法生效、坏 JSON 回退空表）；test_record_meta_preserved 删除、meta 断言改恒 `{}`。**741 passed + ruff F/I-clean**（743 − 2：删 2 测试；重写 2→2 净 0）。
