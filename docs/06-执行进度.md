# 06 — Wave D + 大型改动执行进度（2026-08-17 启动，长时任务）

> 恢复锚点：任何中断/上下文压缩后，先读本文件 + `git log --oneline -15` 确认状态，从「进行中」的批次继续。
> 计划条目见 docs/05；每批完成：pytest 全绿 + ruff 干净 → 更新 docs/05 状态 + CHANGELOG → 提交。

## 批 1：安全快修（docs/05 #46-#49）
- [x] #46-#49 全部完成（commit c5c65d2）—— print 全清、tiktok→generic、requests timeout、惰性工厂 + atexit 兜底

## 批 2：发布路径（#50/#51/#67）
- [x] 全部完成（commit 6c3ffa1）—— Dockerfile/pyproject/wheel 修复、release test gate + 三处版本核对 + uv build + wheel 内容验证、fastmcp>=3,<4

## 批 3：状态机（#52-#57，server.py + config.py）
- [x] 全部完成（commit 5a9755b）—— 6 项状态机修复 + 3 个契约测试，172 passed

## 批 4：可靠性（#58-#63/#68）
- [x] #58 ffmpeg timeout（video_helper/groq/kuaishou/local）+ 失败反馈
- [x] #59 note_cache 媒体大小上限
- [x] #60 note_cache 键 Windows 安全
- [x] #61 时间戳 \d+
- [x] #62 下载器细节组（local cover 目录/douyin stream/aweme_id/generic exists/SRT CRLF）
- [x] #63 补 2 处 ytdlp_retry
- [x] #68 已随批 1 完成（c5c65d2，先算一次再返回）
- [x] 全部完成（commit 9ed1b47，172 passed）

## 批 5：分发/门禁/卫生（#64-#66/#69-#76）
- [x] #64 install.sh 回退修复；#76 步骤编号
- [x] #65 plugin.json userConfig 补 5 env + funasr
- [x] #66 测试 task_id 隔离（conftest 按 pid 隔离）
- [x] #69 文档代差（docs/03 工具数/小宇宙、docs/02 events、tools.md fetch_subtitles）
- [x] #70 CI 工具数断言/ruff pin（dependency-groups）/Python matrix 3.11-3.13/release 同步
- [x] #71 CLI 校验（export task_id、登录 URL 截断）
- [x] #72 server.py 细节组（_MAX_WORKERS/model_size/下限钳制/样板收敛/is_file）
- [x] #73 universal_gpt content None
- [x] #74 openai_client weakref.finalize 释放 http_client
- [x] #75 transcriber 重建防御性 close 旧实例
- [x] 全部完成（commit aaee3a8，172 passed + ruff clean）

## 大型改动（#30-#34/#39/#16/#24/#45/#42/#29）
- [x] 6a 引擎与默认模型（commit 8b78f65）：whisper 默认 small、HF 超时 60s、health_check 模型行区分 downloaded/downloading/failed(+error)、engine_advice；#39 语言自动选引擎评估后**不做自动切换**（需先跑推理才知语言、funasr 非默认安装），改显式建议
- [x] 6b 视频理解成本 #33（commit 3147ed3）：effective_frame_interval 帧组封顶（≤24 组自适应拉大间隔）；视频已下载时从本地 ffmpeg 提取音频，免第二次网络下载；帧落盘此前已做
- [x] 6c token 级切块 #32（commit 5e620d7）：RequestChunker 双约束（字节+token），OPENAI_MAX_TOKENS_PER_CHUNK 默认 12000；merge 分组同步；fix_markdown 确认无引用
- [x] 6d diarization 进主路径 #31（commit 5ade3ed）：pipeline.apply_diarization 转写后自动打 speaker（两路径接入，note.py 全自动生效）；prompt 渲染 [SPEAKER_00]（2+ 说话人时）
- [x] 6e 播放列表批量 #30（commit 35f900e）：batch_generate_notes 服务端展开+逐个排队，max_entries 截断；工具数 37→38
- [x] 6f 工具面收敛 #16（commit fa2c89c/3dca7b1）：transcript MCP Resource（videonote://task/{id}/transcript 时间轴纯文本）+ _load_task_transcript 公共函数；provider 省略/参数默认化/wait_for_note 废弃此前已落地
- [x] 6g 敏感信息加密（docs/05 #45 安全边角，含 plan 清单 #29 API key 明文；commit ce51514）：Fernet 机器级密钥（crypto.py，enc: 前缀，明文兼容迁移，key 丢失回退）；api_key/hf_token 落盘加密；Claude base_url 修正；CLI add key 交互输入；out_dir 数据目录外 warning；VENDOR 标注 abogus GPL-3.0
- [x] 6h vendor 边界 #42（commit 864fe9c）：docs/02 同步纪律 + 惰性 import 约定；docs/04 安装目录故事修正
- [x] 6i 插件更新路径 #24（commit 5745a84）：plugin.json 带 version；health_check 返回 plugin_version + 落后点名提示；release 四处版本核对

## 收尾
- [x] SKILL/tools.md/docs 与最终实现全面对齐（SKILL 无工具数引用；tools.md 的 Resource/38 工具说明已在 3dca7b1 同步；docs/02/03/04 已随 6a-6i 批次更新）
- [x] 全量回归（198 passed + ruff clean + 38 工具）+ CHANGELOG（Wave D 批 1-5 + 6a-6i 两章）+ docs/05 状态回写（#1-#76 全部 ✅ 标记，commit 0cab442）

## Wave E（2026-08-17 启动：Wave D 收尾后的下一轮，恢复锚点更新）

- [x] E2 大纲传递（commit af4a198）：多 chunk 时后续 chunk 注入已生成章节标题（extract_outline 提取 #~#### 标题、清 markdown 记号与 *Content 后缀、去重截断 15 条/40 字）；generate_base_prompt 新增 outline 块（沿用统一标题、并入已有章节）；MERGE_PROMPT 同义章节合并。tests/test_outline.py 11 项。**209 passed**
- [x] E1 可观测性收口（commit 1ced97a）：stderr 日志超限轮转（VIDEONOTE_STDERR_LOG_MAX_MB 默认 50MB → .log.1）；dup2 失败原因打到原始 stderr；atexit 退出摘要记录进行中任务数。tests/test_stderr_log.py 7 项。**216 passed**
- [x] E3 全库扫描（4 并行代理）+ 修复批次 F1-F9/G1-G4 + 收尾 G2/G3/H 组 —— 4 个 P0（#77-#80）+ 契约修正（#81-#85）全部落地，**225 passed + ruff clean + 39 工具**
  - F1 恢复 get_task_status 装饰器；F2 图片 token 结构感知估算（1105/图）；F3 merge 收敛保障；F4 CLI key 交互输入；F5 provider_dao 解密 None 保护；F6 GPTSource 默认；F7 并发门禁改 running()；F8 wheel include skills/**；F9 CI 精确 39 名单
  - G1 Fernet key 并发安全（O_EXCL）；G2 note_dir→gen/ + portable_note_dir（manifest 定位便携副本）；G3 素材包转写默认保留（raw 恒剥）；G4 退出摘要写 sys.__stderr__ + conftest 数据目录隔离
  - H 组 8 项：H1 docstring 死代码；H2 is_file；H3 started_at try；H4 absolutize 存量 no-op bug + 穿越防护；H5 stderr env 回退；H6 handoff 前置；H7 --no-dev；H8 _stage_label 文档
  - 文档：docs/02（39 工具 / _stage_label / Fernet / env 表）、docs/03（39）、docs/00（39）、docs/04（cookie 引导 CLI / 加密说明 / env 表 / funasr / 平台数 6）、README（wait_for_note 废弃）、skills tools.md + output-formats.md（note_dir/素材包契约）、docs/05 第三轮 #77-#85
- [x] G2 收尾（commit 06f011c）：video_tasks 列 note 任务 note_dir 指向 gen/（与 get_task_status 一致）+ NoteDirContractTest 2 项。**227 passed**
- [x] 参数校验（commit 1d5088b）：extract_frames interval 钳制 ≥1、grid_size 需两个正整数 + 契约测试 3 项。**229 passed**
- [x] G5 退出取消 + YouTube cookie：_exit_summary set 所有 cancel_event（缩短子进程残留窗口）；youtube_downloader 读 setup ③ 填的 cookie（Netscape 0600 + __del__ 清理 + cookiefile 注入）。**233 passed**
- [x] Assets/ absolutize 修复闭环（commit 781e991）：_absolutize_images 加 base_dir 参数处理便携笔记 `Assets/` 相对引用（#84 修复只覆盖 static/screenshots/ 旧全局模式，真实截图走 gen/Assets/ 是死路径）；get_task_status 传 base_dir=note_dir；旧路径保留兼容；+3 测试。**236 passed**
- [x] docs/04 补 get_task_status 的 G3 例外（素材包/transcribe 任务转写默认返回）；docs/05 登记 #89。**236 passed + ruff clean + 39 工具**
- [x] 下载器健壮性（commit f5bdc8b）：#36 剩余 4 子项（kuaishou TypeError→明确 RuntimeError、Bcut 指数退避+timeout、generic cookie 改 http_headers 注入、audio.json 悬空实体视为缓存失效）+ 顺带发现 `_extract_kuaishou_link` IndexError 一并修；tests/test_downloader_robustness.py 16 项。**252 passed + ruff clean**
- [x] DB 路径修复（commit 5ba8497）：`app/db/engine.py` 默认 DATABASE_URL 是相对 CWD 路径——裸脚本/单文件测试在仓库根分裂出 `video_note.db`（根目录残留垃圾的真实根因）；改 `get_data_dir()` 稳定路径；`cache_data` 弃用 vendored 旧 DATA_DIR env；清理根目录残留。**252 passed**
- [x] 下载器集成测试（commit 364e7ad）：#31 剩余——真实下载器类 + mock yt-dlp 的全流程集成测试 15 项（bilibili/youtube/local/generic 的字段契约、quality 映射、skip_download、多 P 缓存、ytdlp_retry 语义）；顺带修 download_video 提取失败 `None.mp4` 误导报错（改 ValueError）与 douyin tuple 错误形状。**267 passed + ruff clean**
