# 06 — Wave D + 大型改动执行进度（2026-08-17 启动，长时任务）

> ⚠️ **历史档案（只追加）** —— 各轮审计/实施批次的执行日志（含 commit hash）。
> 新 Agent **不要通读**。当前状态见 [00-新手上路.md](00-新手上路.md)；
> 查某批做了什么用 `grep -n "批 21" docs/06-执行进度.md` 定位。
> 新批次在文末追加，并按 `videonote-audit` skill 的流程登记。

> 恢复锚点：任何中断/上下文压缩后，先读本文件 + `git log --oneline -15` 确认状态，从「进行中」的批次继续。
> 计划条目见 docs/05；每批完成：pytest 全绿 + ruff 干净 → 更新 docs/05 状态 + CHANGELOG → 提交。
> 当前状态：第 15 轮收敛验证完成（#131，commit 4effbb229293a4a3c0ecdda780e04b64621217a2）。双代理一致判定「可停」——全库无 P0/P1、无值得做的 P2。长时任务收敛。

## 批 20：第 15 轮收敛验证（docs/05 #131，A 组 3 项，667 tests）
- [x] A1 release.yml wheel 门禁删 `app/events/__init__.py` 行（#130 B2 同波遗漏——events 包已删但门禁仍引用，打 tag 发布必失败）
- [x] A2 pyproject.toml 删 blinker 死依赖 + 过期注释（全仓零引用），uv lock 重新生成
- [x] A3 docs/05 #128/#129/#130 状态 ⏳ → ✅（回写纪律，与 #120-#127 一致）
- 代理 B 判定：app 流水线无新增 finding，可停；代理 A 判定：修完上述即收敛。**双代理一致「可停」——项目收敛。**
## 批 19：第 14 轮双代理扫描（docs/05 #130，A 组 6 项 + B 组 3 项，667 tests）
- [x] A1 bool truthy-swallow 收口：config.py 抽 `resolve_bool_config`（bool 直通 / 词表 / 垃圾值 warning 回退），server 五处 `video_understanding`×2 / `include_comments`×2 / `screenshot` 接入——字符串 "false" 不再恒 True 白开视频理解
- [x] A2 comments_limit 显式 0：`comments_limit or 20` 改 `_coerce_int(comments_limit if is not None else 20, 20, clamp_min=1)`——传 0（关评论）不再被 falsy 吞成 20 条
- [x] A3 CLI 导出契约：`--out-dir` help 改「缺省 note_results/{task_id}/gen/」+ file:// 解码/expanduser（URI 直传不再误报文件不存在）
- [x] A4 inspect 本地幻影路径：`Path(raw).expanduser()` 后 is_file 校验，不存在返回 ok:false + 明确错误（+1 测试 test_local_missing_rejected）
- [x] A5 merge 目录穿透：`os.path.exists` 改 `Path(p).is_file()` + FileNotFoundError（目录不再穿透到 ffmpeg 深处）
- [x] A6 死 import：server.py 移除无使用的 `env_bool`
- [x] B1 sqlite_client.py 整文件删除（全仓零引用 + CWD 相对 DB 路径死引信），VENDOR.md 同步
- [x] B2 events 死链整链删除：server 注册点 + events 包（signals/handlers）+ 4 个转写器 on_finish 方法——`transcription_finished` 永不触发的悬空接口全清，VENDOR.md / docs/02 同步
- [x] B3 RequestChunker 二分：`_largest_fitting_prefix` 最大可容纳前缀二分（_fits 调用 O(段数)→O(log 段数)，块边界与线性扫描一致）+ group_texts_by_budget 同款；**400 随机 trial 差分验证 diffs=0**（超长段 split / comments 首块注入 / max_tokens / 异常一致性）
## 批 18：第 13 轮双代理扫描（docs/05 #129，A 组 8 项 + B 组 7 项，666 tests）
- [x] A1 SKILL 红线反例：troubleshooting.md:10 改「填 key 一律走 `! videonote providers set`」（不再教 update_provider/add_provider 填 key）
- [x] A2 文档默认值矛盾：docs/04 `get_task_transcript` 改「默认空=前 50 段」（实现口径，不再误称全部）
- [x] A3 文档自相矛盾：docs/04 `cleanup_all` 去掉「/logs」（logs 刻意不清 #121 C3）
- [x] A4 文档漏改：docs/02 `cleanup_all` 改「同步清空全局索引（config/models 默认保留）」
- [x] A5 半收口残骸：health_check 模型列表改 `get_registry().visible_model_names()`（与 list_transcriber_models 同源）
- [x] A6 preflight 批量子句：detail 改「多集全出建议一条 batch_generate_notes」
- [x] A7 导出目录 docstring：改「缺省为 note_results/{task_id}/gen/」（与实际一致）
- [x] A8 ⚠ 自定义大写模型名：`download_transcriber_model` 改为「原 case 可解析则不动，仅内置档位小写容差」——注册名 "MyModel" 不再被 lower 成 "mymodel" 报 unknown
- [x] B1 默认值分叉残留：get_whisper_transcriber / get_mlx_whisper_transcriber 默认 base → small（与全层统一）
- [x] B2 文档死点（app 层）：bilibili_downloader/subtitle 日志文案改引导 `! videonote login bilibili`（不再教被拒的 set_downloader_cookie）
- [x] B3 缺省落 CWD：merge_audio 缺省改 `NOTE_OUTPUT_DIR/merged`（数据目录，不再不确定 CWD），docstring 同步
- [x] B4 静默吞异常：task_manifest delete_task 失败改 `logger.warning`（不再 except: pass 丢上下文）
- [x] B5 半收口：note.py 内 `screenshot=True` 自闭合并入 format（直接调 note.generate 不再残留 Screenshot 标记）
- [x] B6 空值裸崩：kuaishou 转写器 `(result_data.get('data') or {}).get('text', [])`（data:null 不再 None.get 崩）
- [x] B7 并发竞态残留：transcriber_provider 释放旧实例前持 `old._lock`（切换模型尺寸时进行中转写不再读空模型）

## 批 17：第 12 轮双代理扫描（docs/05 #128，A 组 8 项 + B 组 7 项，666 tests）
- [x] A1 SKILL 死点：troubleshooting.md 5 处 `set_downloader_cookie(cookie=…)` 改引导 `! videonote login bilibili`（不再教 Agent 撞红线）
- [x] A2 预下载白下：`download_transcriber_model` 仅内置档位名 lowercase（直通 HF repo_id / 本地目录保持原 case，不再 1.5GB 白下）
- [x] A3 formats 混型：`export_transcript` 未知格式去重前先 `str(f)`（`[1,"pdf"]` 不再 int/str 比较崩）
- [x] A4 cleanup_all docstring 同步：「数据库不动」改「同步清空全局索引 video_tasks」（cleanup_note include_note 副作用一并注明）
- [x] A5 快照接缺失分支：`get_task_status` status.json 缺失也先查 `_status_memory`（首写失败任务在跑不再误报 NOT_FOUND）
- [x] A6 模型列表同源：`list_transcriber_models` 遍历 `WhisperModelRegistry.visible_model_names()`（large-v1/自定义不再永远「未下载」）
- [x] A7 SKILL 指引矛盾：tools.md:22 改「多集全出用 batch_generate_notes；只一集用对应 url」（不再教逐条 subagent 撞并发上限）
- [x] A8 CLI/MCP 默认分叉：`transcriber set` 未带 `--size` 时保留现有配置（不再悄悄降级 small）
- [x] B1 groq 引擎可配置：`GroqTranscriber` 按名称 `get_provider_by_name('groq')` 查找（`providers add` 建的 uuid 供应商也能驱动）
- [x] B2 产物目录统一：note/pipeline/universal_gpt 三处 NOTE_OUTPUT_DIR 缺省统一 `get_data_dir()/note_results`（裸脚本产物不再落 CWD 对清理失明）
- [x] B3 whisper 默认统一：transcriber_provider/whisper/mlx 兜底统一 small（不再静默 base 降质）
- [x] B4 临时目录泄漏：`apply_diarization` created=True 在 mkdtemp 后立即置位（normalize 失败不再残留 /tmp/vn_dia_*）
- [x] B5 kuaishou 父级裸索引：`_extract_photo` 统一校验 visionVideoDetail.photo（视频被删/接口变更给可排查错误，不再 `None['photo']` TypeError）
- [x] B6 handler 数据删除炸弹：`cleanup_temp_files` 沙箱化（仅任务目录内清理，绝不连带删用户源目录）
- [x] B7 timeit 度量：print → `logger.info`（裸脚本/pytest 不再污染 stdout）

## 批 16：第 11 轮双代理扫描（docs/05 #127，A 组 8 项 + B 组 10 项，666 tests）
- [x] A1 note/material 提交即入全局索引（先 `insert_video_task(PENDING)` 再写状态；`_submit_step_task` 顺序修正）
- [x] A2 `delete_model` 先校验供应商（与 add_model 同款，不再误报「模型不存在」）
- [x] A3 `export_transcript` 转写来源改 gen 优先（与 CLI / `_load_task_transcript` 同口径）
- [x] A4 `add_provider` docstring + CLI help 标注 type 恒为 custom
- [x] A5 `validate_url` ValueError 分支补 `platform: "unknown"`（失败形状对称）
- [x] A6 `list_tasks(limit=0)` 返回空列表（不再钳成 1 误导「没有任务」）
- [x] A7 `get_task_files` 成功路径补 `ok: true`
- [x] A8 `get_task_transcript` 切片 full_text 统一空格分隔（与全量一致）
- [x] B1 FunASR `device="cuda"` 探测回退 cpu（无 CUDA 机器构造不再崩）
- [x] B2 `WhisperModelRegistry` class-level RLock（并发 add/remove 不互抹）
- [x] B3 whisper/funasr/mlx 实现 `close()`（transcriber_provider 防御性释放生效）
- [x] B4 kuaishou 转写器流式上传文件对象（不再整文件 read() 进内存）
- [x] B5 kuaishou caption null 守卫（`(photo_info.get('caption') or '')`）
- [x] B6 douyin `download_video` `.get()` 链 + 显式错误（不再多层裸索引天书）
- [x] B7 universal_gpt env 数值防御式解析（垃圾值回退默认，不再裸 int/float 崩任务）
- [x] B8 `ProviderService.add_provider` 失败改 `logger.error`（不再 print）
- [x] B9 `LocalDownloader.convert_to_mp3` 缺省落数据目录（源目录不污染）
- [x] B10 note.py 日志 `(title or '')[:40]`（None 不再 TypeError 误报「保存失败」）

## 批 15：第 10 轮双代理扫描（docs/05 #126，A 组 9 项 + B 组 9 项，643 tests）
- [x] C1 `export_transcript` 门禁：非 SUCCESS 任务不再导出成功（CLI/MCP 同口径）
- [x] C2 `cleanup_all` 成功路径补 `ok: true`
- [x] C3 `fetch_comments` limit 裸 int → `_coerce_int`（垃圾值回退 20）
- [x] C4 mlx 尺寸提交前校验 + `mlx_whisper` 惰性导入（未装 extras 也能预校验）
- [x] C5 `set_transcriber` diarization_speakers → `_coerce_int` 回退 None
- [x] C6 `get_app_config` 解密 per-key try/except（坏 key 只 drop 不整包清空）
- [x] C7 CLI `transcriber set --size` help 补全 large-v3-turbo
- [x] C8 CLI `export --out-dir` 规整 file:// URI（不再建字面 file: 目录）
- [x] C9 `validate_url` 本地分支存在性检查（不存在 → supported:false）
- [x] B1 预处理临时产物落独立 mkdtemp（vn_prep_/vn_dia_），并发任务互不覆盖
- [x] B2 youtube_subtitle `close()` try/finally（不再泄漏 Session）
- [x] B3 dm_img patch TypeError 降级无 fatal 重调（旧版 yt-dlp 兼容）
- [x] B4 b23.tv/v.douyin.com 短链解析 lru_cache（单任务不再 3-5 次 HEAD）
- [x] B5 bcut/kuaishou 段文本 None 守卫 + per_size=0 拒绝上传空块
- [x] B6 `_first_choice_content` 空内容错误同口径
- [x] B7 `cleanup_all` 索引失败不再静默吞：warning + `index_error` 字段
- [x] B8 `insert_video_task` 日志 `(title or "")[:40]`（None 不再崩 TypeError）
- [x] B9 `TranscriberConfigManager.update_config` class-level RLock（无锁 RMW 补全）

## 批 14：第 9 轮双代理扫描（docs/05 #125，A 组 11 项 + B 组 16 项，612 tests）
- [x] A1 `get_app_config` 损坏 JSON → warning + `.corrupt` 备份 + 空配置（不抹其余配置）
- [x] A2 CLI `export` 先剥离 `_errors` 再判空（不再假成功）
- [x] A3 `get_task_status` SUCCESS 但结果未落盘 → `result_pending: true`
- [x] A4 `inspect_video` docstring 改「多集全出笔记 → 一条 batch」
- [x] A5 6 处裸 `int()` 统一 `_coerce_int`（垃圾值 warning 回退默认）
- [x] A6 `fetch_subtitles` docstring 补 `{ok: true}` 成功形状
- [x] A7 `download_transcriber_model` 校验改 `resolve_whisper_model` 同源（自定义尺寸可预下载）
- [x] A8 转写配置兜底 `tiny` → `small`（docstring 同步）
- [x] A9 tools.md `preflight` 补 `need_provider` 签名
- [x] A10 `/videonote-setup` 改用 `read_app_config` 工具（不再 Read 原始 JSON）
- [x] A11 `cleanup_task_files` 成功路径补 `ok: true`
- [x] B1 抖音短链 `v.douyin.com` 解析回退（缓存键不再恒 None）
- [x] B2 kuaishou `download_video` 直接 `_download_mp4`（不白转 mp3）
- [x] B3 `_update_status` 写失败回退不截断原文件（保留已落盘终态）
- [x] B4 `cleanup_all` 清 `static/cover/` + `data/covers/`
- [x] B5 补 KuaishouTranscriber / BilibiliDmPatch / screenshot_marker 测试（test_b5_coverage.py）
- [x] B6 b23.tv 短链只 resolve 一次（BV/p 均取真实 URL）
- [x] B7 `ytdlp_retry` 排除 FileNotFoundError（不白等退避）+ 修 except 缺 `as e`
- [x] B8 `_first_choice_content` helper（空 choices 抛明确错误；merge/summarize 两处同源）
- [x] B9 cookie 旧格式字符串兼容（isinstance 守卫，垃圾值降级空串）
- [x] B10 5 个转写器 `full_text` 统一 `" ".join`（消除 O(n²) 累积）
- [x] B11 kuaishou ffmpeg 失败 `raise ... from e` + 退出码/超时区分
- [x] B12 `delete_all_tasks` 单条 DELETE 替代 N+1 循环
- [x] B13 video_reader duration<1s → 回退 t=0 单帧 + warning（不再静默 0 帧）
- [x] B14 diarization Pipeline 模块级单例（首次失败不缓存，token 变化重载）
- [x] B15 bcut 分片从文件分段读（多 GB 音频不再整读 + 切片复制）+ 修多分片 etag 丢失
- [x] B16 youtube_subtitle 代理 Session 显式 `close()`/`__del__`（连接池不再泄漏）

## 批 13：第 8 轮双代理扫描（docs/05 #124，A 组 12 项 + B 组 19 项，582 tests）
- [x] A1 转写向导默认值漏 funasr → 复用 `_TRANSCRIBER_ENGINES`
- [x] A2 `providers add` 重名裸 traceback → try/except + exit 1
- [x] A3 `add_model` 重复行 → `get_model_by_provider_and_name` 查重幂等
- [x] A4 CLI export 非 list 默认格式字符集误报 → 共享 `resolve_default_export_formats`
- [x] A5 `env_bool` 垃圾值回退 default（不再静默翻反）
- [x] A6 转写配置布尔解析同源 helper（大小写不敏感 + yes/on）
- [x] A7 模型下载去重原子化（`dl_state` 模块锁 + `try_mark`）
- [x] A8 `export_transcript(formats=[])` 显式报错
- [x] A9 `summarize_material` 外来 segment 清洗（未知键过滤/缺字段跳过）
- [x] A10 `get_task_status` 结果损坏加 `result_error` 字段
- [x] A11 `batch_generate_notes` 单集退化分支补齐 truncated/remaining/platform/kind
- [x] A12 `preflight` 加 `need_provider` 参数（material 流程跳过 LLM 检查）
- [x] B1 快手零字节 mp3 不被 exists 当成功产物（转前 unlink + size 校验）
- [x] B2 checkpoint 写失败只 warning 不 raise
- [x] B3 ytdlp_retry 补 DNS 类错误关键词
- [x] B4 抖音异常链保留（f-string + from e、删裸 re-raise）
- [x] B5 抖音音频请求用实例 headers（cookie 生效）
- [x] B6 抖音 duration 毫秒→秒归一
- [x] B7 快手 title 清洗统一（skip_download 与正常分支同款）
- [x] B8 快手 `_download_mp4` with 托管 + raise_for_status（连接不泄漏）
- [x] B9 快手 `get_video_details` raise_for_status 提位
- [x] B10 bcut Etag None 守卫
- [x] B11 封面文件名带输入路径短 hash（同名视频不互踩）
- [x] B12 单帧 ffmpeg 超时只跳过该帧（TimeoutExpired → None）
- [x] B13 note.py 9 处产物/缓存写点原子化（write_json_atomic / write_text_atomic）
- [x] B14 list_tasks limit/offset 下推 SQL
- [x] B15 CookieConfigManager 类级写锁（并发 set 不互抹）
- [x] B16 文件 handler 挂 root logger（第三方 WARNING+ 落盘，root 保持 WARNING）
- [x] B17 抖音 datetime.utcnow → timezone-aware
- [x] B18 whisper 仅 cache 类异常 purge（网络/404 不删模型）
- [x] B19 extract_frames 默认目录带 uuid 后缀（同名视频不互踩）
- [x] 核对后排除 1 项（merge concat 引号转义——concat list 仅含自生成 mkdtemp 路径）
- [x] 全部完成（commit 19ac155）

## 批 1：安全快修（docs/05 #46-#49）
- [x] #46-#49 全部完成（commit c5c65d2）—— print 全清、tiktok→generic、requests timeout、惰性工厂 + atexit 兜底

## 批 2：发布路径（#50/#51/#67）
- [x] 全部完成（commit 6c3ffa1）—— Dockerfile/pyproject/wheel 修复、release test gate + 三处版本核对 + uv build + wheel 内容验证、fastmcp>=3,<4

## 批 3：状态机（#52-#57，server.py + config.py）
- [x] 全部完成（commit 5a9755b）—— 6 项状态机修复 + 3 个契约测试，172 passed

## 批 4：可靠性（#58-#63/#68）
- [x] #58 ffmpeg timeout（video_helper/groq/kuaishou/local）+ 失败反馈
- [x] #59 note_cache 媒体大小上限
- [x] #60 note_cache 键 Windows 安全
- [x] #61 时间戳 \d+
- [x] #62 下载器细节组（local cover 目录/douyin stream/aweme_id/generic exists/SRT CRLF）
- [x] #63 补 2 处 ytdlp_retry
- [x] #68 已随批 1 完成（c5c65d2，先算一次再返回）
- [x] 全部完成（commit 9ed1b47，172 passed）

## 批 5：分发/门禁/卫生（#64-#66/#69-#76）
- [x] #64 install.sh 回退修复；#76 步骤编号
- [x] #65 plugin.json userConfig 补 5 env + funasr
- [x] #66 测试 task_id 隔离（conftest 按 pid 隔离）
- [x] #69 文档代差（docs/03 工具数/小宇宙、docs/02 events、tools.md fetch_subtitles）
- [x] #70 CI 工具数断言/ruff pin（dependency-groups）/Python matrix 3.11-3.13/release 同步
- [x] #71 CLI 校验（export task_id、登录 URL 截断）
- [x] #72 server.py 细节组（_MAX_WORKERS/model_size/下限钳制/样板收敛/is_file）
- [x] #73 universal_gpt content None
- [x] #74 openai_client weakref.finalize 释放 http_client
- [x] #75 transcriber 重建防御性 close 旧实例
- [x] 全部完成（commit aaee3a8，172 passed + ruff clean）

## 大型改动（#30-#34/#39/#16/#24/#45/#42/#29）
- [x] 6a 引擎与默认模型（commit 8b78f65）：whisper 默认 small、HF 超时 60s、health_check 模型行区分 downloaded/downloading/failed(+error)、engine_advice；#39 语言自动选引擎评估后**不做自动切换**（需先跑推理才知语言、funasr 非默认安装），改显式建议
- [x] 6b 视频理解成本 #33（commit 3147ed3）：effective_frame_interval 帧组封顶（≤24 组自适应拉大间隔）；视频已下载时从本地 ffmpeg 提取音频，免第二次网络下载；帧落盘此前已做
- [x] 6c token 级切块 #32（commit 5e620d7）：RequestChunker 双约束（字节+token），OPENAI_MAX_TOKENS_PER_CHUNK 默认 12000；merge 分组同步；fix_markdown 确认无引用
- [x] 6d diarization 进主路径 #31（commit 5ade3ed）：pipeline.apply_diarization 转写后自动打 speaker（两路径接入，note.py 全自动生效）；prompt 渲染 [SPEAKER_00]（2+ 说话人时）
- [x] 6e 播放列表批量 #30（commit 35f900e）：batch_generate_notes 服务端展开+逐个排队，max_entries 截断；工具数 37→38
- [x] 6f 工具面收敛 #16（commit fa2c89c/3dca7b1）：transcript MCP Resource（videonote://task/{id}/transcript 时间轴纯文本）+ _load_task_transcript 公共函数；provider 省略/参数默认化/wait_for_note 废弃此前已落地
- [x] 6g 敏感信息加密（docs/05 #45 安全边角，含 plan 清单 #29 API key 明文；commit ce51514）：Fernet 机器级密钥（crypto.py，enc: 前缀，明文兼容迁移，key 丢失回退）；api_key/hf_token 落盘加密；Claude base_url 修正；CLI add key 交互输入；out_dir 数据目录外 warning；VENDOR 标注 abogus GPL-3.0
- [x] 6h vendor 边界 #42（commit 864fe9c）：docs/02 同步纪律 + 惰性 import 约定；docs/04 安装目录故事修正
- [x] 6i 插件更新路径 #24（commit 5745a84）：plugin.json 带 version；health_check 返回 plugin_version + 落后点名提示；release 四处版本核对

## 收尾
- [x] SKILL/tools.md/docs 与最终实现全面对齐（SKILL 无工具数引用；tools.md 的 Resource/38 工具说明已在 3dca7b1 同步；docs/02/03/04 已随 6a-6i 批次更新）
- [x] 全量回归（198 passed + ruff clean + 38 工具）+ CHANGELOG（Wave D 批 1-5 + 6a-6i 两章）+ docs/05 状态回写（#1-#76 全部 ✅ 标记，commit 0cab442）

## Wave E（2026-08-17 启动：Wave D 收尾后的下一轮，恢复锚点更新）

- [x] E2 大纲传递（commit af4a198）：多 chunk 时后续 chunk 注入已生成章节标题（extract_outline 提取 #~#### 标题、清 markdown 记号与 *Content 后缀、去重截断 15 条/40 字）；generate_base_prompt 新增 outline 块（沿用统一标题、并入已有章节）；MERGE_PROMPT 同义章节合并。tests/test_outline.py 11 项。**209 passed**
- [x] E1 可观测性收口（commit 1ced97a）：stderr 日志超限轮转（VIDEONOTE_STDERR_LOG_MAX_MB 默认 50MB → .log.1）；dup2 失败原因打到原始 stderr；atexit 退出摘要记录进行中任务数。tests/test_stderr_log.py 7 项。**216 passed**
- [x] E3 全库扫描（4 并行代理）+ 修复批次 F1-F9/G1-G4 + 收尾 G2/G3/H 组 —— 4 个 P0（#77-#80）+ 契约修正（#81-#85）全部落地，**225 passed + ruff clean + 39 工具**
  - F1 恢复 get_task_status 装饰器；F2 图片 token 结构感知估算（1105/图）；F3 merge 收敛保障；F4 CLI key 交互输入；F5 provider_dao 解密 None 保护；F6 GPTSource 默认；F7 并发门禁改 running()；F8 wheel include skills/**；F9 CI 精确 39 名单
  - G1 Fernet key 并发安全（O_EXCL）；G2 note_dir→gen/ + portable_note_dir（manifest 定位便携副本）；G3 素材包转写默认保留（raw 恒剥）；G4 退出摘要写 sys.__stderr__ + conftest 数据目录隔离
  - H 组 8 项：H1 docstring 死代码；H2 is_file；H3 started_at try；H4 absolutize 存量 no-op bug + 穿越防护；H5 stderr env 回退；H6 handoff 前置；H7 --no-dev；H8 _stage_label 文档
  - 文档：docs/02（39 工具 / _stage_label / Fernet / env 表）、docs/03（39）、docs/00（39）、docs/04（cookie 引导 CLI / 加密说明 / env 表 / funasr / 平台数 6）、README（wait_for_note 废弃）、skills tools.md + output-formats.md（note_dir/素材包契约）、docs/05 第三轮 #77-#85
- [x] G2 收尾（commit 06f011c）：video_tasks 列 note 任务 note_dir 指向 gen/（与 get_task_status 一致）+ NoteDirContractTest 2 项。**227 passed**
- [x] 参数校验（commit 1d5088b）：extract_frames interval 钳制 ≥1、grid_size 需两个正整数 + 契约测试 3 项。**229 passed**
- [x] G5 退出取消 + YouTube cookie：_exit_summary set 所有 cancel_event（缩短子进程残留窗口）；youtube_downloader 读 setup ③ 填的 cookie（Netscape 0600 + __del__ 清理 + cookiefile 注入）。**233 passed**
- [x] Assets/ absolutize 修复闭环（commit 781e991）：_absolutize_images 加 base_dir 参数处理便携笔记 `Assets/` 相对引用（#84 修复只覆盖 static/screenshots/ 旧全局模式，真实截图走 gen/Assets/ 是死路径）；get_task_status 传 base_dir=note_dir；旧路径保留兼容；+3 测试。**236 passed**
- [x] docs/04 补 get_task_status 的 G3 例外（素材包/transcribe 任务转写默认返回）；docs/05 登记 #89。**236 passed + ruff clean + 39 工具**
- [x] 下载器健壮性（commit f5bdc8b）：#36 剩余 4 子项（kuaishou TypeError→明确 RuntimeError、Bcut 指数退避+timeout、generic cookie 改 http_headers 注入、audio.json 悬空实体视为缓存失效）+ 顺带发现 `_extract_kuaishou_link` IndexError 一并修；tests/test_downloader_robustness.py 16 项。**252 passed + ruff clean**
- [x] DB 路径修复（commit 5ba8497）：`app/db/engine.py` 默认 DATABASE_URL 是相对 CWD 路径——裸脚本/单文件测试在仓库根分裂出 `video_note.db`（根目录残留垃圾的真实根因）；改 `get_data_dir()` 稳定路径；`cache_data` 弃用 vendored 旧 DATA_DIR env；清理根目录残留。**252 passed**
- [x] 下载器集成测试（commit 364e7ad）：#31 剩余——真实下载器类 + mock yt-dlp 的全流程集成测试 15 项（bilibili/youtube/local/generic 的字段契约、quality 映射、skip_download、多 P 缓存、ytdlp_retry 语义）；顺带修 download_video 提取失败 `None.mp4` 误导报错（改 ValueError）与 douyin tuple 错误形状。**267 passed + ruff clean**
- [x] 插件 userConfig 补全（commit 05cb87a）：全仓 15 个 VIDEONOTE_* env 与 plugin.json 对齐——补 max_workers/stderr_log_max_mb（内部路径 3 个刻意不暴露）。
- [x] batch 参数面补全（commit 2c1ad9f）：batch_generate_notes 透传 generate_note 全部高级参数（link/视频理解/弹幕/notes_dir）；userConfig env 映射同步（_USER_CONFIG_MAPPED_ENV）。**268 passed + ruff clean**
- [x] 真实 stdio + 端到端冒烟（2026-08-17，手动验证）：官方 mcp client 库从 /tmp CWD 启动 server——initialize ✓、tools/list 39 精确名单 ✓、health_check 正常 ✓；`prepare_note_material(本地 wav)`（TRANSCRIBER_TYPE=mlx-whisper tiny 环境覆盖，数据目录隔离到临时目录）真实跑通 下载→mlx 推理转写→素材包，轮询 SUCCESS，result 含 G3 契约全字段（transcript/comments_danmaku/frames/audio_path/video_path/note_dir），正弦波无语音识别为空属正确行为。裸 JSON-RPC 手写协议无响应是客户端消息格式问题，非 server 缺陷。
- [x] 死代码清理（commit 3cf63ea）：`app/gpt/` 删 5 个全仓零引用模块（openai_gpt / deepseek_gpt / qwen_gpt / utils / tools——gpt_factory 走 OpenAICompatibleProvider 直连，上游直连类死代码）；VENDOR.md 分叉清单同步；AST 全仓死模块普查确认无其余残留（E3 批已清）。**268 passed + ruff F-clean**
- [x] CLI 契约测试 + update_provider 假成功修复（commit 6dad3b7，#96）：ProviderService.update_provider 对不存在供应商返回恒真 dict——CLI/MCP 的「不存在」判空永不生效（报成「已更新」），修复返回 None；tests/test_cli.py 18 项（providers 加密/掩码/交互/错误路径、transcriber 配置闭环、export 渲染、subprocess seed 验证）；另清 2 处旧 worktree 路径 docstring。**286 passed + ruff F-clean**
- [x] style/format 白名单 enum 化（commit fd9a5e2，#97）：4 工具签名 Literal 化（schema enum）+ `_check_style_and_format` 入口显式校验（FastMCP 无运行时参数校验，直接调用会静默无风格笔记）；校验在 provider 解析前（H6 原则）；+7 契约测试。**293 passed + ruff F-clean + 39 工具**
- [x] fetch_comments limit 钳制（commit db93dbe，#98）：limit<=0 令 fetcher `len(seen) >= limit` 恒真静默返回空评论；入口钳制 max(1, limit)；+3 契约测试。**296 passed + ruff F-clean**
- [x] notes_dir 数据目录外提示（commit 68d8a62，#99）：export/merge 已有「只提示不拦截」warning，#45 口径唯一漏网的 notes_dir 补上（参数 → app_config → env 缺省链解析后统一校验；便携笔记是显式用户意图，不拦截；batch 委托 generate_note 自动继承）；+3 契约测试；docs/05 Wave B 第 6 条过时 ⏸ 标记修正（Resource 6f 已落地）。**299 passed + ruff F-clean**
- [x] grid_size 入口显式校验（commit 6b68d3f，#100）：extract_frames 工具校验但 generate_note/prepare_note_material 直传——非法值（[0,0]/[1]/[1,2,3]）在 VideoReader 深处才炸「视频处理失败」泛化错误；提取共享助手 _check_grid_size 接入三处（与 style/format 同口径）；顺带清 note.py 2 处裸 except:。+7 契约测试。**306 passed + ruff F-clean**
- [x] num_speakers 无效值显式提示（commit 5736b47，#101）：diarize_audio 的 falsy 判断让 0/负值/非 int 静默回退自动检测；入口校验打 warning 后回退，合法值照常透传。+4 单元测试。**310 passed + ruff F-clean**
- [x] list_tasks 分页（commit a8ffb19，#102）：无上限全量返回（每行含 200 字 summary，长跑用户响应膨胀）；加可选 limit/offset（缺省全量向后兼容，limit 钳制 ≥1）；tools.md 同步。+4 契约测试。**314 passed + ruff F-clean**
- [x] set_transcriber 引擎白名单（commit 25cdd4e，#103）：未知 transcriber_type 持久化后运行时 get_transcriber 静默回退 fast-whisper（用户以为云端引擎实际本地）；入口白名单校验（与 TranscriberType 枚举同源防漂移）。+3 契约测试。**317 passed + ruff F-clean**
- [x] 静默错误族收尾（commit e5d0eaf，#104）：① summarize_note transcript 形状——垃圾/空 dict/传 fetch 结果外层曾静默成空素材让 LLM 凭空生成笔记，入口显式报错（H6，provider 解析前；只查字段存在不查内容，静音视频合法结果不拦）；② extract_frames video_interval 非数值静默回退 6——打 warning 后回退（#101 同口径）；③ export_transcript 未知格式曾只写 stderr 警告后静默丢弃——入口白名单（srt/vtt/json，与 exporter FORMATS 同源），缺省链解析后同样校验。+14 契约测试。**331 passed + ruff F-clean**
- [x] 误导错误收口（commit 5cab61f，#105）：① fetch_subtitles 的 platform 拼错——pipeline 把「不支持的平台」异常吞掉转成「该视频没有可用平台字幕」（误报成视频没字幕），入口白名单校验（与 SUPPORT_PLATFORM_MAP 同源）；② merge_audio 不认 file://——全工具面唯一漏网，URI 直传被 Path 判不存在误报「文件不存在」，入口统一 _coerce_local_path 规整；③ diarize_media 目录输入穿透（.exists() 对目录为 True）——改 is_file 与其余本地文件工具同口径。+9 契约测试。**340 passed + ruff F-clean**
- [x] app 层静默降级加固（commit b6e36cc，#106）：4 并行代理二轮全库扫描（app/ 8 目录 except/falsy/成功形状）→ 7 项——① 配置管理器损坏静默当空（cookie_manager.set 会把其它平台 cookie 永久抹掉）：新增 app/utils/json_store.py（损坏 warning+备份 .corrupt+空配置；原子写 tmp+replace+0600）接入三个管理器；② 模型名解析失败谎报「未下载」（区分 ValueError + warning）；③ chunk_duration_guess probe 失败无日志回退 1800s 时间轴漂移（warning）；④ note.py 全局索引同步失败 debug→warning；⑤ started_at 读取失败 except:pass→warning；⑥ universal_gpt checkpoint 损坏静默弃用白烧配额（warning）；⑦ inspect 播放列表非 http 条目成功形状（跳过+warning，全坏显式错误）。VENDOR.md 冻结清单同步。+17 测试。**357 passed + ruff F-clean**
- [x] 输出目录 file:// 规整 + 导出格式垃圾配置（commit 12ab0d4，#107）：① 三处输出目录参数（generate_note notes_dir / export_transcript out_dir / merge_audio out_dir）统一 _coerce_local_path 规整——URI 直传曾建字面 `file:` 垃圾目录且「数据目录外」提示误报；② app_config.default_export_formats 非列表垃圾值遮蔽 env 回退链——抽 _resolve_default_export_formats() 接入工具与自动导出两处缺省链（warning + 回退）。docstring/tools.md 标注 file:// 支持。+7 契约测试。**364 passed + ruff F-clean**
- [x] set_transcriber 尺寸入口校验（commit b380a08，#108）：#103 收口——引擎类型已白名单但 whisper_model_size 没有：非法尺寸被持久化后任务跑到 TRANSCRIBING 才因模型加载失败（或 preflight 报「未下载」）；用运行时同源 resolve_whisper_model（内置/自定义/含 "/" 的 HF repo_id/已存在本地目录）入口校验；CLI `transcriber set --size`（自由串，download 才有 choices）同口径拒绝。+7 测试。**371 passed + ruff F-clean**
- [x] merge 目录穿透 + batch 单集退化（commit 5f81093，#109）：① merge_audio 目录输入（merge.py 的 os.path.exists 对目录为 True）穿透到 ffmpeg 深处才炸「转换失败」泛化错误——入口 is_file（与 diarize_media 同口径）返回「文件不存在或不是文件」；② batch_generate_notes 单集退化路径 _submit raise 裸传中断调用（多条目有收集契约、单集没有）——收进 errors 返回同形状。+5 契约测试。**376 passed + ruff F-clean**
- [x] SKILL/文档与 batch 工具面对齐（commit 16c853a，#110）：6e 落地 batch_generate_notes 后 SKILL 仍教「多集用 subagent」——SKILL.md 规则 2 / 默认路径步骤 4 / 工作流 3 处 + troubleshooting.md 2 处 + docs/04 1 处改为「合集/分P/播放列表 → 一条 batch（服务端排队）；互相独立的链接 → 各一个 subagent」。纯文档批次。**376 passed + ruff F-clean**
- [x] cleanup 运行中任务防护（commit 23ac4f8，#111）：cleanup_note / cleanup_all 对 `_task_futures` 未完成的进行中/排队任务拒绝清理（返回 ok:false + 先 cancel_note 引导），避免删掉下载器/转写器正在写的目录致任务中途失败或状态残留；CLI 向导不动（交互式有 status 展示 + 双重确认，且独立进程看不到 MCP 注册表）。+4 契约测试。**380 passed + ruff F-clean**
- [x] cleanup_note 便携副本孤儿化修复（commit fd6ce47，#112）：include_note=True 此前只删 task_dir——数据目录内的便携笔记副本（notes_dir 指向数据目录内时）漏删，数据目录外的副本（用户指定 notes_dir 的典型场景）随 manifest 删除永久失联；现在目录内副本随删、目录外副本沙箱红线不删但经新增 `notes_kept_outside` 字段列出，CLI 向导黄字报告，tools.md 同步。+2 测试。**382 passed + ruff F-clean**
- [x] LLM/转写调用超时收敛（commit 6f3b977，#113）：build_openai_client 缺省 None → SDK 600s 默认（代理路径另写 600s），上游挂死时任务卡 SUMMARIZING 10 分钟×3 重试、worker 槽占死、cancel 无法生效；构造点单点收敛为 `httpx.Timeout(300, connect=10)`——连接 10s 快速失败 + 读写 300s 兜底，LLM 主路径与 groq 转写同时收敛，代理路径同源。+4 测试。**386 passed + ruff F-clean**
- [x] 「无转写」文案误报修复（commit 936a3c4，#114）：get_task_transcript / MCP Resource / export_transcript 拿不到转写时一律「尚未成功或已清理」——运行中任务被 Agent 误报成失败；新增 `_transcript_unavailable_reason` 读 status.json 区分运行中（建议等终态）/未成功/成功无转写/不可读，三处接入。+5 契约测试。**391 passed + ruff F-clean**
- [x] preflight 队列检查与门禁同源（commit 900b9c3，#115）：queue 检查此前用 len(_task_futures)（含排队）且详情写「N/3 进行中」——batch 提交后（3 运行+7 排队）对新视频 preflight 误报「已满」；改与 `_guard_concurrency` 同源只统计 running()，详情「N/M 运行中（另 K 排队）」，运行中占满才拦。+2 契约测试。**393 passed + ruff F-clean**
- [x] app_config 整数配置缺省链加固（commit bc61251，#116）：video_interval/comments_limit 的 `get(...) or env_int(...)` truthy 短路——垃圾值（"abc"）让 int() 裸 ValueError 遮蔽 env 回退（#107 同族）、0（显式关闭）被当 falsy 吞掉致 app_config 优先级倒挂给 env；抽 `_resolve_int_config`（is not None + 防御 int，垃圾值 warning 回退）接入 4 处。+6 契约测试。**399 passed + ruff F-clean**
- [x] format 字符串/混型校验（commit 108a3e6，#117）：`_check_style_and_format` 的 `set(formats)` 把字符串 "toc" 拆成字符集报「['c','o','t']」（合法格式被说成非法）；int/str 混排让 sorted() 裸 TypeError。入口显式要求字符串列表（与 export_transcript #104 同口径），未知元素字符串化报出。+3 契约测试。**402 passed + ruff F-clean**
- [x] 第三轮 app 扫描：转写失败不静默 + 状态写保护（commit 10940aa，,#118）：2 并行代理扫 transcriber 失败形状与 note.py 状态机 → ① P0 预处理分块转写全失败曾静默返回空转写 → 任务 SUCCESS 产空笔记：全失败 raise（FAILED 带块数与首个错误）、部分失败 warning + `truncated` 标记；② P0 收敛 `_write_status` 写盘无保护（磁盘满裸抛进后台线程被吞）：加保护 + `_status_memory` 快照，get_task_status 读盘损坏回退快照；③ P1 started_at 每次重打致终态 elapsed≈0：保留旧值；④ P1 funasr 空结果无留痕：warning；⑤ P2 ffprobe 探测失败/步骤任务入索引失败：warning。+6 测试。**408 passed + ruff F-clean**
- [x] 素材包 audio_path 悬空 + 分块连词 + 死代码（commit 1862c04，#119）：① skip_download 字幕路径的假路径（下载器拼出、文件不存在）原样透传素材包——媒体缓存 miss（常态）时 audio_path 指向不存在文件误导 Agent：miss 置 None + info 留痕；② 预处理分块 full_text `"".join` 无分隔 → 英文 chunk 边界连词（hellohello）：改空格连接；③ `_is_insufficient_quota_error` 全仓零引用死代码删除。+2 测试。**410 passed + ruff F-clean**
- [x] 回归链 + screenshot/format 闭合 + 向导加固（commit 619ae45，#120）：① R1 #119 置 None 的缓存加载分支 falsy 吞值 → Path(None) TypeError：None 视为失效重新下载；② G1 screenshot/link 布尔并入 _format（server）+ format 回并布尔（note 归一化，need_video 只认布尔）+ need_full_download 含 format 检查——双向闭合，矛盾输入有确定行为；③ G2 chunker 估算只对首 chunk 计评论（与注入语义一致），评论大不再 chunk 膨胀；④ G4 截图插入失败带异常详情；⑤ C1-C5/S4 向导加固：wizard 重名不崩、edit 失败如实报、resolve_int_config 上移 config.py（CLI 6 处接入）、export 格式白名单 + 转写源同源回退、login exit_on_fail、fallback 回车=保持；⑥ quota 判定先于 429 重试（不白等）、_merge_partials 组循环前检查取消、temperature 降级走 logger；⑦ 死代码：task_serial_executor 整文件删除（VENDOR 同步）+ universal_gpt 死属性/死代码。+28 测试（含新文件 test_gpt_resilience 6 项）。**438 passed + ruff F-clean**
- [x] 记忆沉淀：CLAUDE.md 引用的 `videonote-mcp-gotchas` 记忆补齐（DB 相对路径、stdout dup2、stdio 官方库调试、插件刷新、uvx --with 顺序、凭证规则）+ MEMORY.md 索引。
- [x] 第 5 轮双代理扫描 22 项（commit 84fd38c，#121）：① C1 batch 并发门禁 thread-local 旁路（worker 毫秒级置 running 致第 4 条起被拒；批量排队靠线程池，max_entries≤50 封顶）；② C2 diarize_media 归一化 wav finally 清理（成功/异常都清，不再残留 <原名>_16k.wav）；③ C3 cleanup_all 不再清 logs/（dup2 fd 已 unlink inode 陷阱：日志消失、磁盘不回收、无报错）；④ C4 export_transcript 失败补 ok:False；⑤ C5 cancel_note 运行中只发协作信号返回 CANCELLING（防覆盖 worker 刚写的 SUCCESS；排队中仍收尾写 CANCELLED + pop registry）；⑥ C6 batch inspect 失败归一为批量形状；⑦ C7 validate_url 死分支；⑧ C9 _write_status 终态落盘后弹内存快照；⑨ C10 模型下载大小写宽容 + 进行中去重；⑩ C12 delete_model 只清「删的就是默认」的 default_model；⑪ C13 update_provider 返回 changed；⑫ B1 短视频帧不足一组按单组拼接兜底（不再静默零帧）；⑬ B2 空转写（静音/黑屏）统一抛错不再当成功缓存；⑭ B3 local cover makedirs；⑮ B4 B 站 p 越界返回 None 走转写（不再静默取第 1 集）；⑯ B5 分 P 缓存精确 glob（{BV}.mp4 / {BV}_pN.mp4，不再前缀误配）；⑰ B6 字幕回退落专属临时目录 + finally 清理（不再污染数据根）；⑱ B7 groq compress_audio ffmpeg 失败清临时文件；⑲ B8 bcut 上传业务 code 检查（不再 KeyError 裸崩）；⑳ B9 youtube embed/{id} 匹配。+30 测试（新建 test_bilibili_subtitle / test_bilibili_downloader / test_groq_compress / test_bcut / test_url_parser）。**469 passed + ruff F-clean**
- [x] 第 6 轮双代理扫描 13 项全部实施（commit 59e01a4，#122）：A2 json 导出双修复（speaker 保留 + `transcript.export.json` 文件名隔离，转写缓存不再被覆盖）/ B1 置 None 前 is_file 检查（真实本地路径保留）/ B2 分 P 缓存精确匹配（p2 不命中 p1）/ A1 导出落盘失败 ok:False / B3 inspect 改 http_headers Cookie 注入（弃 Netscape 临时文件）/ A7 模型下载 mark 前移防 TOCTOU / A3 转写读取 SUCCESS 门禁 / A5 summarize 剥离 screenshot/link + strip_media_markers 清洗 / B5 正则消费闭合星号 / A4 formats=[] 零导出 / A8 cancel 终态措辞区分 / A6 CLI 清理向导运行中守卫（磁盘 status.json 判终态）+ _press_any_key 防非交互崩 / B4 ytdlp_retry 删死代码 + attempts 校验。+31 测试。**500 passed + ruff F-clean**
- [x] 第 7 轮双代理扫描 19 项实施（commit 87dee10，#123）：A1 cleanup_all include_models 模型下载守卫（server dl_state.downloading_keys 拒绝 + CLI .incomplete 残留扫描）/ A3 步骤任务 step_fn 后复查取消（cancel 承诺的「阶段边界停止」不再矛盾）/ A6 CLI export 缺省格式统一 ["srt"]（与 MCP 同源）/ A7 add_model 供应商存在校验（防孤儿行）/ A8 get_task_transcript 非法 segment_range 读真实状态（不硬编码 UNKNOWN，抽 _read_task_status）/ A9 _status_memory 上限 512 防无界 / A10 config 注释修正（锁只保证单进程）/ A4 summarize_note docstring 不宣传 screenshot/link / A5 cleanup_all/CLI/SKILL/docs logs 文案修正（日志不属任务产物）/ B1 skip_subtitle 透传防重复字幕 API / B2 lookup_media 过滤 .tmp 与非常见媒体后缀 / B4 本地文件 sha256 (path,mtime,size) 缓存 / B5 下载器注册表弱引用化（__del__ 可触发，cookie 不滞留）/ B6 whisper_models 原子写 / B7 便携笔记目录 mkdir(exist_ok=False) 原子占用防 TOCTOU / B8 update_provider 异常透传 ValueError（logger.error + CLI 如实报）/ B9 kuaishou mp3 命中 mp4 缺失补下 / B10 douyin %-格式化 bug 改 Path 拼接 / B11 bcut Session close/__del__。+32 测试。**532 passed + ruff F-clean**。A2（merge_audio 大改）/B3（RequestChunker O(n²)）按登记暂缓。

## 批 21：第 16 轮三代理全库扫描（docs/05 #132，A 组 5 项 + B 组 11 项 + C 组 4 项，691 tests）

三代理并行全库扫描（安全 / 正确性·健壮性 / 性能·依赖），关键项代码级复核并对照前 15 轮剔除已登记项。分三批实施（commit 76dd7d3）：

- [x] **批次 1 — 安全快修**：A1 SSRF 防护（新增 `app/utils/url_safety.py`：scheme 白名单 + ipaddress 私网/环回判定 + 域名解析校验，接入 generic/youtube downloader、inspect、validate_url；conftest `_mock_public_dns`）；A2 base_url scheme 校验 + 变更使 key 失效（MCP 返回 `key_invalidated`）；A3 Docker 非 root；A4 代理 URL 日志脱敏（3 处）；A5 敏感 URL 日志脱敏（2 处）。+27 测试（test_url_safety.py 21 + 契约 6）。
- [x] **批次 2 — 健壮性中危**：B1 cancel 打断下载/转码（ytdlp_cancel_hook + stream_download 循环检查 + ffmpeg Popen 化 + 6 下载器 cancel_event 参数）；B2 truncated 透传（TranscriptResult.truncated + 笔记标注）；B3 note_cache promote tmp 唯一后缀；B4 stream_download 公共函数（连接/读分离超时 + 退避重试）替换 douyin/kuaishou 直连下载。+12 测试（test_round16_batch2.py）。
- [x] **批次 3 — 低危收敛 + 性能**：C1 av 直接依赖移除（faster-whisper 传递依赖保留，注释修正）+ uv lock；C2 douyin fetch_video_info memo + msToken/ABogus 复用；C10 死代码三模块删除；B13 CookieConfigManager 惰性单例；B7 summary 占位清理；B8 json_store/config 原子写加固（唯一 tmp + os.open 0600）；B9 status.tmp 双写者唯一化；B10 exporter 原子写；B11 复用前非空校验（bilibili/youtube/douyin）；B14 bcut finally close；C4 bcut 轮询 10 分钟 deadline；C8 init_db 三组索引。B5/B6/B12 评估免改（#118 覆盖/窄窗口/固有边界）。**691 passed + ruff F/I-clean**

## 批 22：第 17 轮四维全库扫描（docs/05 #133，A 组 1 + B 组 6 + C 组 3，743 tests）

四维并行扫描（安全 / 正确性·健壮性 / 工具面·CLI 契约 / 测试·依赖·文档漂移），代码级核实并对照前 16 轮去重。**743 passed + ruff F/I-clean**（净增 52：+14 契约/批量 + 36 test_url_safety 收集修复 + 2 batch）。

- [x] **批次 1 — 安全 + 正确性**（commit fcf35d1）：A1 SSRF 入口覆盖（url_parser 短链解析器内置守卫 + inspect_video 非 local 统一守卫 + generate_note/prepare_note_material/fetch_subtitles 入口守卫）；B1 显式 provider_id key 校验（#52 只修默认分支）；B2 inspect 本地分支认 file://（`_coerce_local_path` 规整，全工具面最后一块）；B3 summarize_note full_text-only 拒绝（流水线只消费 segments，防 LLM 零素材凭空生成）；B7 quality 校验移到 provider 前；B8 batch max_entries=0 提交 0 条。**测试基建 bug 修复**：test_url_safety.py 类名非 Test 前缀、pytest 从不收集（#132 A1 的 SSRF 回归测试从未执行），重命名后 36 用例进套件。+50 测试。
- [x] **批次 2 — 依赖/文档/CI**（commit bda7c74）：C1 mcp 直接依赖声明（fastmcp 从未被 import，uvx 不读 lock 有挂载风险）；C2 VENDOR.md 分叉清单同步（#132 C10 三死模块 + url_safety/note_cache/json_store/douyin_helper/kuaishou_helper）；C3 action-gh-release v2 停维护 → v3（Node24）。
- **建议后续（登记未实施）**：A2 tmp/0644 写盘收尾（_atomic_write_json/task_manifest/universal_gpt checkpoint）、B4 diarize normalize 残留 wav、B5 local/kuaishou 转码不响应取消、B6 Provider DAO 吞异常假成功、C5 全局 /tmp 断言、C6 conftest setdefault 不对称、C7 Dockerfile 缺 skills、C8 测试硬编码 /tmp、C9 单文件直跑写真实数据目录、C10 CLI download choices 缺档。

## 批 23：死代码清理专项（docs/05 #134，743→741 tests）

用户确认「清理死代码」后执行（承接批 22 审计结论：AST import 图 + 名字引用计数双扫描，模块级 + 公开函数级）。**741 passed + ruff F/I-clean**。

- [x] **模块（2）**（commit 664fcf9）：`app/validators/` 整包（video_url_validator 全仓零引用）、`app/exceptions/biz_exception.py`（零引用死类）。VENDOR.md 同步两行。
- [x] **函数（~15）**：whisper_models 自定义模型写 API ×6（读端保留，写端改手工编辑 JSON）、delete_note + delete_task_by_video、delete_video、get_all_models、get_enabled_providers、mark_downloading + get_error、preprocess_pipeline、get_app_dir、apply_to_env、test_connection、abogus ×4、record_task_meta。
- **误删恢复 2 处**：get_task_meta（list_task_files 在用）恢复；model_dao dangling `finally`、OpenAI provider dangling `@staticmethod` 修掉。
- **工具冗余结论**：无实质冗余（wait_for_note = deprecated compat、_stage_label = micro-tool），39 工具不变。
- **测试**：删 WhisperRegistryConcurrencyTest + test_record_meta_preserved（−2）；WhisperModelsAtomicWriteTest 重写为 WhisperCustomJsonReadTest（2→2 净 0）→ 743 − 2 = **741 passed**。

## 批 24：工具面精简（docs/05 #135，39→18 工具，741→662 tests）

用户发起（「39 个工具是不是太多了？配置 CRUD 没必要加；步骤工具删掉，主流程够用」）。**662 passed + ruff F/I-clean**。

- [x] **删 21 个工具**（commit 8dc90e1）：写配置 13（provider/model/transcriber/cookie CRUD——配置修改一律走 CLI，MCP 面零写配置入口）+ 流水线步骤 6（fetch_*/transcribe_media/extract_frames/summarize_note——端到端已含）+ wait_for_note + _stage_label（降级内部 helper）。
- [x] **新增 `get_config`**：只读汇总（默认值过滤敏感项 + providers 掩码 + transcriber 就绪 + cookie 平台名）；传 provider_id 附加连通性探测（用已存 key）。
- [x] **连带清理**：步骤任务链 5 函数 + `_coerce_transcript` + 7 个死符号；误删 `_index_step_task` 已恢复（generate_note 在用）。
- [x] **测试**：−79（已删工具契约）+5（GetConfigTest）= **662 passed**。
- [x] **文档全同步**：ci.yml 名单、SKILL tools.md/SKILL.md/troubleshooting、commands/setup、docs/02/04/00、README 中英。

## 批 25：工具聚合（docs/05 #136，18→16 工具，662→661 tests）

用户持续目标第二轮（六维评估驱动）。**661 passed + ruff F/I-clean**。

- [x] **A validate_url 并入 inspect_video**（commit 4976519）：inspect 已覆盖全部检查（SSRF/local/file:///平台识别）；合并后一次调用 = 识别 + 检查 + 拆多集。
- [x] **B get_task_files 并入 cleanup_note(dry_run)**：dry_run=True 返回原 get_task_files 形状，不删任何东西。
- [x] **C 参数策略 docstring**：generate_note / batch_generate_notes 开头引导「除 url 外全可选，套 setup 默认」。
- [x] **D 暂缓**：prepare_note_material mode 合并（收益不确定）。
- [x] **测试/文档全同步**：契约测试 5 类改写（mock bilibili 网络）、ci.yml 16 名单、SKILL/commands/docs/README 中英。

## 批 26：cleanup_all dry_run（docs/05 #137，661→664 tests）

用户持续目标第三轮（行为维度：破坏性最强工具缺预览机制）。**664 passed + ruff F/I-clean**。

- [x] **cleanup_all(dry_run=True)**（commit bedf789）：预览将清理/保留目录 + 运行中任务，不删任何东西；dry_run 不拒绝运行中（预览无副作用），执行仍拒绝。
- [x] **测试**：+3 CleanupAllDryRunTest = **664 passed**。
- [x] **同步**：tools.md / docs/04（先 dry_run 再执行引导）/ commands/setup。

## 批 27：工具精简 16→10（docs/05 #138，10 工具 / 708 tests）

用户持续目标第四轮（工具精简：16 → 10）。**708 passed + ruff F/I-clean**（692 + 16）。

- [x] **server.py 四组合并**（commit e02296d）：`task` 收编 get_task_status / get_task_transcript / cancel_note；`cleanup` 收编 cleanup_note / cleanup_all；`process_media` 收编 export_transcript / merge_audio / diarize_media；`health_check` 扩展收编 preflight——不丢功能：分段转写 / dry_run 先查后清 / need_provider / hf_token 蜜罐 / 运行中拒绝全保留。
- [x] **契约测试迁移 + 新增 test_138_batch.py**（16 tests）：692 + 16 = **708 passed**。
- [x] **CI 名单更新**：ci.yml 工具名单 16 → 10。
- [x] **MCP 启动命令 uvx videonote@latest**（v0.1.16）：plugin.json 启动命令改为 `uvx videonote@latest`——每次会话启动自动取 PyPI 最新版，发版即生效（原 `uvx videonote` 走缓存不保证更新）；`_skill_refresh_advice` 文案同步校正；README 中英安装说明同步。708 passed + ruff clean。

## 批 28：#32 悬空 @staticmethod 修复（709 tests）

用户报障 issue #32（无字幕视频本地转写 100% 失败）。**709 passed + ruff F/I-clean**（708 + 1）。

- [x] **删除 note.py 悬空 @staticmethod**（commit 75571cb）：#134 死代码清理删 delete_note 时残留装饰器行，注释不打断绑定 → `_init_transcriber` 被误绑静态方法，`self._init_transcriber()` 抛 `missing 1 required positional argument: 'self'`（本地复现确认 + 红绿验证）。回归测试打桩 `get_transcriber` 而非 `_init_transcriber` 本身，强制走 914 行真实调用路径。709 passed + ruff clean。

## 批 29：第 18 轮专项扫描 #139（#32 同族装饰器/绑定回归，712 tests）

用户要求全库扫 #32 同类 bug（悬空装饰器/注释分割）。三代理并行 + 双确定性 AST 扫描交叉验证。**712 passed + ruff F/I-clean**（709 + 3）。

- [x] **批次 1 — 绑定守卫测试**（commit a3bcac4）：新增 `test_binding_guard.py` 四个 AST 守卫——装饰器与定义之间不得被注释/代码分割（#32 精确签名）、@staticmethod 方法体不得引用 self、@classmethod 首参须 cls、绑定敏感方法（`_init_transcriber` 起）不得被 `mock.patch.object` 整体打桩；`test_note_cache` 4 处 `_init_transcriber` 整体 mock 迁移到 `get_transcriber` 层。扫描结论：装饰器分割全库 0 命中（#32 为唯一一次）、历史清理 commit 删除侧零残留；真正风险是 mock 掩盖绑定漂移的防回归机制缺失（10+ 兄弟方法原样保留 #32 掩盖模式）。
- [x] **批次 2 — 死代码/死注释清理**（commit c62a5e8）：whisper 类内 `is_torch_installed` 死静态方法（裸名解析到模块 import，删除行为不变）、`transcriber_provider._init_transcriber` 改名 `_get_or_build_transcriber`（与 `NoteGenerator` 实例方法同名混淆，#32 事故现场即有）、异常类 `code` 注解 `ProviderErrorEnum → int`（实际传 `NoteErrorEnum.XXX.code`）、三处死注释（provider.py 旧 row 下标序列化 14 行 / abogus 注释参数 / kuaishou 注释硬编码 Cookie——恢复即凭证泄漏）。

## 批 30：第 18 轮建议后续实施（docs/05 #139 C1-C4，714 tests）

第 18 轮「建议后续」四项全部落地（用户指示优化）。**714 passed + ruff F/I-clean**（713 + 1）。

- [x] **批次 4 — mock 下沉 + 守卫 5**（commit 97ed5ef）：`_get_downloader` ×6 / `_update_status` ×5（直接删 mock）/ `_transcribe_audio` / UniversalGPT ×4 / Bcut backoff ×3 / `CookieConfigManager.get` ×4 全部下沉到依赖层；新增守卫 5（descriptor 断言，覆盖含保留 mock 的全部绑定敏感方法）。Douyin/VideoReader/`_load_checkpoint`/`__upload_part` 保留方法级 mock 并注明理由（守卫 5 兜底）。
- [x] **批次 5 — staticmethod 类名调用 + 守卫 6**（commit 87f1d6f）：11 组 16 处 `self.<staticmethod>()` 改类名调用（Kuaishou 测试 mock 同步升类级）；守卫 6 AST 断言「staticmethod 必须类名调用」全库强制（abogus 白名单）。
- [x] **批次 6 — 真实转写集成冒烟 + 文档**（commit 76dd7d3）：`tests/test_integration_transcribe.py`（`@pytest.mark.integration` 默认跳过；darwin mlx tiny → 兜底 fast-whisper small，真实引擎实例化+模型加载+推理，本地已验证通过）+ ci.yml `workflow_dispatch` 手动 job；pyproject 注册 integration marker。
- [x] #140 安全扫描收尾（commit 3cba873）：SSRF 逐跳校验（PublicOnlySession/public_get/public_head + stream_download 资源 URL 入口校验）、cleanup_all 越界拒绝（config/models 数据根外/符号链接不删）、cryptography>=50.0.0、代理/供应商 URL 凭据脱敏、三处固定 .tmp+0644 接入 _unique_tmp/0600、.gitignore 加 node_modules/ 与 scripts/（723 passed + ruff F/I-clean）。
- [x] #141 安全复扫收尾（commit 4b219e1）：stream_download 逐跳校验（302→内网第二跳不再发出）、cleanup_all 全部目标越界检查（note_cache symlink 等）、base_url 拒 user:pass@ + SQLite 0600 + 数据目录 0700、加密回退明文状态入 health_check、BilibiliDownloader 入口 SSRF 校验（738 passed + ruff F/I-clean）。

- [x] #142 安全扫描收尾（commit 8d6e9ab）：MCP 本地文件/危险操作边界（`VIDEONOTE_ALLOW_EXTERNAL_PATHS` + `VIDEONOTE_ALLOW_DESTRUCTIVE_CLEANUP` 默认拒绝，本地视频入口 / notes_dir / out_dir / merge / diarize / cleanup include_config·include_models 六处接入 + 插件 userConfig）、模型 revision 固定（15 个内置模型仓库钉 main commit，faster-whisper≥1.2.0 + `WhisperModel(revision=)` + `snapshot_download(revision=)`，映射单一来源归并）、弹幕 XML DOCTYPE/ENTITY 预扫 + 20 MiB 上限、YouTube EJS 远程组件开关（`VIDEONOTE_YTDLP_EJS`）、MD5 `usedforsecurity=False`（771 passed + ruff F/I-clean）。

## 小宇宙官方文稿（2026-08-30）

- [x] 小宇宙升为一等平台：`detect_platform` 识别 `xiaoyuzhoufm.com`，官方文稿 `episode-transcript/get` 优先（需 `x-jike-access-token`，refresh 自动续期），未登录回退 yt-dlp+ASR。CLI `login xiaoyuzhou` / `cookie set`，凭证不进 MCP。**802 passed + ruff F/I-clean**。
- [x] 小宇宙登录默认扫码（官网 `qrcode/create` + `qrcode/login`），`--token` 保留粘贴；凭证不打印。**805 passed + ruff F/I-clean**。

## 小红书原生支持（2026-08-31）

- [x] 小红书升为一等平台：`detect_platform` 识别 `xiaohongshu.com` / `xhslink.com` / `rednote.com`；笔记页 `__INITIAL_STATE__` 取视频直链；图文笔记明确拒绝。CLI `login xiaohongshu` 默认扫码（`--cookie` 粘贴），凭证不进 MCP。**841 passed + ruff F/I-clean**。
- [x] 扫码 406 修复：edith 拒旧 x-s，改本机 Chrome 打开官网出码（Playwright `channel=chrome`）；`--cookie` 仍可用。

## 批 34：第 19 轮全库扫描（docs/05 #143，2026-09-01，864 tests）

全库安全、正确性·健壮性、性能·依赖扫描：初始 **847 passed, 1 skipped, 10 subtests + ruff F/I-clean**；完成后 **864 passed, 1 skipped, 10 subtests + ruff F/I-clean**。对照 #140–#142 去重，A/C 组 5 项实施，4 项建议后续登记。

- [x] **批次 1 — 结果元数据白名单**（commit `53e56e4`）：B 站 yt-dlp 完整 `raw_info` 不再进入音频缓存或 MCP 任务结果；缓存序列化与 server 结果读取各有白名单，旧 `result.json` 也会在返回前投影；保留 tags/title/duration 等流水线与 Agent 必要字段。
- [x] **批次 2 — 错误信息脱敏**（commit `b84f33d`）：新增 `sanitize_error_text` / `sanitize_error_url`，清洗签名 URL、Authorization、Cookie、token/signature；统一 status、日志、inspect、batch 与主要下载器错误边界，后台主异常不再写原始 traceback。
- [x] **批次 3 — B 站官方请求有界重试**（commit `87a0857`）：字幕、元信息、弹幕/评论 GET 统一经 `public_get_retry`；只重试连接/超时、429、5xx，指数退避受总 deadline 约束，4xx 直接交业务层处理，逐跳 SSRF 守卫不变。
- [x] **批次 4 — SyntaxWarning 收敛**（commit `314bb5d`）：抖音正则改 raw string，两个测试文档字符串明确转义；`python -W error -m py_compile` 通过。
- [x] **文档漂移收尾**：`docs/00` 统一 864 基线；`docs/02` 删除当前流程中的已删除步骤工具引用；`skills/videonote/reference/tools.md` 与 troubleshooting 修正不存在的 `models` CLI；`docs/04` 删除已不存在的 `include_transcript` 参数；server 预检错误改为当前 `providers test` CLI 指引。
- [ ] **建议后续**：A3 yt-dlp 内部 egress/重定向 SSRF、A4 DNS fail-open 与 fake-IP 代理策略、A5 Docker/Actions/插件可变引用固定、C5 `pyannote.audio` 主版本上界；需产品/部署策略或兼容矩阵确认后再实施。

## 批 35：第 20 轮全库扫描 #144（小红书/小宇宙登录与文稿，864→876 tests）

#142 之后新平台路径：小红书 host/登录与小宇宙官方文稿。合入时保留批 34（#143）全文。**876 passed, 1 skipped, 10 subtests + ruff F/I-clean**。

- [x] **批次 1 — 安全 + 健壮性**（commit `13d8a3e`）：小红书 host/cookie 精确后缀、inspect 剥 `xsec_token`、Playwright 响应/二维码钉官方域、失败关浏览器；小宇宙扫码 `PublicOnlySession`；官方文稿失败抛 `OfficialTranscriptFetchError` 不回退 ASR；登录探测看首页 userId；`_to_mp3` 响应取消；缓存命中 fetch 失败用存根；MCP docstring 补两平台。

## 批 36：第 21 轮全库扫描（docs/05 #145，2026-09-01，876→902 tests）

全库安全、正确性·健壮性、性能·依赖扫描。初始 **876 passed, 1 skipped, 10 subtests + ruff F/I-clean**；完成后 **902 passed, 1 skipped, 10 subtests + ruff F/I-clean**。对照 #132–#144 去重，A/B/C 组 16 项实施，4 项本轮建议后续。

- [x] **批次 1 — 安全 + 健壮性 + 性能**（commit `ba4a7dd`）：B 站扫码跟随钉 `bilibili.com`/`biligame.com` + PublicOnlySession；必剪/抖音/快手出站改逐跳校验；`audio.json` 封面脱敏；inspect 回放 URL 与本地数据目录门禁；抖音 HEAD 失败仍解析 `/video/{id}`；供应商/任务索引失败不再假「不存在/零任务」；Whisper 非 cache 错误不双倍下载；小红书下载器 close Session；删 #144 暂缓死符号；YouTube 字幕默认超时；app.log 轮转；faster-whisper 惰性 import。
- [ ] **建议后续**：B5 CLI export 目录门禁（本机显式导出有意放宽）、C2 下载状态写 API 死代码、C4 FastMCP 同步工具占事件循环、C7 mlx 分块重复加载；#143 A3/A4/A5/C5 等仍待产品/部署决策。

## 批 37：第 22 轮重试扫描（docs/05 #146，A 组 1 项建议后续 + B 组 4 项，906 tests）

初始 **902 passed, 1 skipped, 10 subtests + ruff F/I-clean**；三代理独立复核后实施 B1-B4，A1 DNS rebinding 保留为建议后续。完成后 **906 passed, 1 skipped, 10 subtests + ruff F/I-clean**。

- [x] **批次 1 — 正确性·健壮性**：转写器重建先构造新实例再关闭旧实例；后台任务在 `_tasks_lock` 内完成 submit 与 Future/Event 登记；终态状态文件损坏且无快照时返回 UNKNOWN；batch 单集 `max_entries=0` 不提交任务。新增 `tests/test_146_batch.py` 4 项（commit `1a31169`）。
- [ ] **建议后续**：A1 DNS rebinding 解析—连接 TOCTOU；需连接器/部署 egress 策略与 fake-IP 代理兼容矩阵确认。

## 批 38：开放项收口（docs/05 #147，2026-09-01，906→920 tests）

#146 报告落盘后收口可落地开放项（不改 yt-dlp egress / Docker 浮动版本 / pyannote 上界等产品决策）。初始 **906 passed, 1 skipped, 10 subtests + ruff F/I-clean**；完成后 **920 passed, 1 skipped, 10 subtests + ruff F/I-clean**。

- [x] **批次 1 — 安全 + 健壮性 + 卫生**：`pin_public_host` 钉死校验 IP（#146 A1）；diarize normalize 失败清半成品 wav；local/kuaishou ffmpeg 可取消；Provider DAO 写失败 re-raise；CLI 下载接线 `try_mark/mark_done/mark_failed`；删 `delete_model` / `TaskStatus.description` / `GPT.list_models`；mlx `close()` 清 ModelHolder。新增 `tests/test_147_batch.py`（commit `affae86`）。
- [ ] **建议后续**：#145 C4 FastMCP 同步工具占事件循环、#145 B5 CLI export 外部目录；#143 A3/A4/A5/C5 仍待产品/部署决策。

## 批 39：第 23 轮全库代码审查与修复（docs/05 #148，2026-09-01，920→948 tests）

在 `dev` / `183510671329e98658ff54c54adacf27afc1668e` 工作树上完成全库安全、正确性·健壮性、性能·依赖三维审查与人工核对。本批修复已合入 commit `8b0aed8`，并持久化报告到 `docs/07-全库代码审查报告.md`。

- [x] **任务发布与并发**：SUCCESS 延迟到 `result.json` 与最终 manifest 落盘后；自动导出产物纳入 manifest；普通任务 admission/submit/Future/Event 登记同锁完成；提交失败回滚任务目录和索引；损坏状态返回 UNKNOWN；batch 单次最多 50 条并由线程池排队。
- [x] **取消与媒体健壮性**：取消事件贯穿字幕、转写、媒体下载；ffmpeg 提取音频采用同目录临时文件和原子替换；取消/失败不覆盖完整缓存、不留最终半成品。
- [x] **SQLite/DAO**：连接开启外键；旧 `models.provider_id` schema 迁移并保留孤儿模型；DAO 写失败 rollback/re-raise；provider/model 一致性与 api_key 加密失败整笔回滚。
- [x] **加密/manifest/ffprobe**：Fernet fail-closed；strict manifest 写后回读；ffprobe 120 秒 timeout + NaN/Inf/负数校验。
- [x] **文档同步**：活跃文档统一到 948 tests；补充 batch admission 与 SUCCESS 持久化语义；索引新增审查报告；VENDOR 冻结清单登记本批语义分叉。
- [ ] **开放 P2**：yt-dlp 内部 egress SSRF、manifest 清理 TOCTOU、stderr 日志路径 no-follow/owner/mode 校验；分别需要网络适配/部署隔离、受控文件系统原语或部署信任模型确认。

验证目标：全量 `948 passed, 1 skipped, 10 subtests passed`；Ruff F/I clean；`git diff --check` clean；不执行真实网络、模型下载或外部服务调用。

## 批 40：#149 流水线协作式取消、生命周期与 SQLite 迁移边界（2026-09-03）

#149 已合入当前 `dev`（实现 commits `71cf266`、`4ca2fd3`、`62334ec`）。本批文档回填不重写批 39；验证记录区分 #148 全量基线与本批 focused validation，不虚报全量通过。

- [x] **任务 admission 与回滚**：普通任务在同一 `_tasks_lock` 内完成 admission reservation、线程池 submit、Future/Event 登记；reservation 覆盖排队与执行。batch 通过 thread-local bypass，不占普通 admission，由线程池排队；submit/参数准备失败回滚目录、manifest、内存注册表与索引，清理不完整时附加结构化诊断且保留原始异常。
- [x] **取消传播**：`cancel_event` 贯穿字幕、下载、ASR、预处理、ffmpeg、VideoReader 抽帧、说话人分离、GPT chunk 与后处理；可控子进程 terminate/kill，第三方阻塞调用不能硬中断。B 站弹幕/评论 helper 仍不接收事件；`process_media` 保持同步且不入任务注册表。
- [x] **SQLite/DAO 边界**：兼容迁移仅限 SQLite；缺失 `video_tasks` 表时 helper 安全 no-op；迁移失败 re-raise。DAO insert/update/delete rollback 后 re-raise，完成阶段索引写入失败不再伪造 SUCCESS。
- [x] **manifest 与 ffprobe**：同进程 manifest RLock 收口读-改-写与删除，跨进程检查—删除 TOCTOU 仍列 P2；VideoReader 使用严格 120 秒 ffprobe，音频预处理探测失败则 best-effort 回退 1800 秒并 warning。
- [x] **聚焦验证**：新增/更新 `tests/test_cancellation_pipeline.py`、`tests/test_149_migration.py`，并覆盖 `test_146_batch.py`、`test_review_regressions.py`、`test_success_publication.py` 的相关回归；#148 的 948 passed 全量结果作为前序基线，本批不宣称重新执行全量套件。

### 批 40 最终合并验证补充（2026-09-03）

- [x] #149 最终实现与文档均已落位 `dev`；全量 `pytest`：**963 passed, 1 skipped, 10 subtests passed**；Ruff F/I 与 `git diff --check` 均通过，import/CLI smoke 通过。

## 批 41：#150 本机 Agent 缓存治理与弹幕取消（2026-09-04）

确认部署模型为本机 Agent 后实施，不是全库扫描。batch 旁路与 CLI 导出到桌面按产品决定保留。

- [x] **缓存 TTL/LRU**：`VIDEONOTE_CACHE_TTL_DAYS` 默认 30（滑动）、`VIDEONOTE_CACHE_MAX_MB` 默认 2048；promote 后淘汰过期与超量身份目录；命中刷新 mtime。
- [x] **弹幕/评论取消**：helper 接 `cancel_event`，取消错误不吞掉。
- [x] **契约**（commit `1eb6308`）：插件 userConfig、`get_config.note_cache`、CLI `--out-dir` help、活跃文档与 Skill 同步。全量 **973 passed, 1 skipped, 10 subtests passed**；Ruff F/I 与 `git diff --check` 通过。

## 批 42：#151 Skill 默认由当前 Agent 写笔记（2026-09-04）

产品决定：默认当前对话 Agent 写笔记；仅非多模态（或用户要求配置 LLM）才走 `generate_note` / `batch_generate_notes`。

- [x] **SKILL / reference / 手册**：默认 `prepare_note_material`；合集逐条 prepare；`batch_generate_notes` 降为后备。
- [x] **health_check**：`need_provider` 默认 False；合集 duration 提示改默认 prepare。
- [x] **CLI 向导**：Agent 写笔记确认项默认开；忽略旧 `agent_direct=false` 作为分流依据。
- [x] **契约**（commit `f027229`）：`tests/test_151_batch.py` + PreflightNeedProviderTest 默认跳过供应商检查。全量 **981 passed, 1 skipped, 10 subtests passed**；Ruff F/I 与 `git diff --check` 通过。
